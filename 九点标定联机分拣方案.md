# 九点标定 · 视觉引导分拣联合方案（v1.2：**人工在 VisionPro 标定 + 输入框交接 + 把坑写进系统**）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms + VisionPro 9.0 CR2）
> 依据：`20260909-note/visionPro09.md` §四（机械臂运动）、§五（九点标定）
> 关联：`机械臂TCP控制方案.md`（v3.0 桥接进程）、`主运行页方案.md`（§4.5 明确把标定留给"这一期"）
> 状态：**只写了方案 + 跑了验证性探针；一行产品代码都没改。**
> 本次探针证据在 **附录 A**（原始输出），探针文件跑完已删。

---

## 〇、你的拍板（v1.2）

| # | 决定 | 对方案的影响 |
|---|---|---|
| **Q1** | **丙：人工在 VisionPro 上点九个点** | 程序**不做**采点界面、不做标定物、不做自动找点；标定在 QuickBuild 里人工完成 |
| **Q2** | 既然人工在 VisionPro 上点，**不管标定物的事** | 不引入"标定物吸在吸盘上"那一套；标定图用什么由图的人自己定 |
| **Q3** | **甲：存 `config\calib.json`**，与 vpp 解耦（存点对） | 程序里仍要有一份标定数据 + 换算逻辑 |
| **Q4** | **加"人工把 VisionPro 输出的坐标数据递给机械臂"的输入框**（框里装**标定数据：9 对点**） | 不新开 Tab；输入区放进设备调试页（机械臂就在那一页）→ **见 §4.5** |
| **Q5** | **甲：没标定就拒绝分拣** + 报警 + 备注写进库 | 安全优先，不悄悄退回示教点 |
| **Q6** | **甲：`rHead` 固定 0** | 不引入旋转 |
| **Q7** | **只做手动** | 不做"程序自动走九格" |

**你另外指定的两件增补（v1.2 加入，见 §4.10）**

| # | 增补 | 落到哪 |
|---|---|---|
| **N1** | **把「模板原点默认 (0,0)」这个坑写进系统** | 不只是文档里写一段 —— 训练模板时提示、标定计算前强制核对、界面上把原点显示出来、代码注释里抄一份实测证据（§4.10.2） |
| **N2** | **把人工标定的方法写进项目，降低学习成本** | 一份 `九点标定操作指引.md`（单一真相源）+ 设备调试页一个「操作指引」按钮弹出同一份文字（编译进 exe，现场不用带文件）（§4.10.1） |

**这一版比 v1.0 少做的东西**（因为标定搬到 VisionPro 里人工做了）：
不做标定页、不做采点按钮/自动九格、不做标定物 PMA、不做 `CogRecordDisplay` 交互点图。
省下来的代码全部投到"数据交接 + 校验 + 指引"这一件事上。

---

## 一、一句话

九点标定**在 VisionPro 里人工点**完成；点出来的数据由操作工**填进一个输入框**交给程序；
程序算一次、存一份 `calib.json`，之后每轮把「PMA 给出的像素坐标」换算成「机械臂 mm 坐标」，
让 NG 件**停在哪儿都能抓**。

---

## 二、现状基线（已核对，每条都有出处）

| 事实 | 出处 / 证据 |
|---|---|
| 现在的分拣是**示教固定点**：取料点 → 吸 → 放料点 → 放 → 回取料点 | `Services\RobotPickPlace.cs:39-54`；`Services\RobotArmSettings.cs:68-83` |
| `RobotPickPlace` 注释写着"PMA 只给一个匹配分数，没有缺陷坐标可用" | 同文件 `:14-15`。**这句话只对"缺陷位置"成立**——工件自身的像素位置 PMA 是给的（§三.2 实测），这一期正是用它，注释要改 |
| 主运行页状态机里已有 `Sorting` 阶段，NG 才动机械臂 | `Services\ProductionRunner.cs:332-340` |
| 机械臂 API 齐：`MoveJAsync / WaitArriveAsync / SuctionAsync / GetPoseAsync / JogAsync` | `DobotArm\Client\DobotArmClient.cs`（v3.0 已实测） |
| 判定用的 PMA 是**彩转灰后**再跑 | `Services\VisionScheme.cs:208`；vpp 里也带 `CogImageConvertTool1`（§三.5） |
| 设备调试页已经有完整的**点动/位姿显示/软件队列** | `Views\DeviceDebugView.Robot.cs`——用户拍板的输入框就放在这一页最顺手（§4.5） |
| 本机**没有相机 / 机械臂 / 标定板** | 自测只能靠合成点对 + 假桥接器（§六） |

---

## 三、本次实测到的硬事实（探针；这些数字决定了代码怎么写）

### 3.1 九点标定的算法在本机是通的（`CogCalibNPointToNPoint`）

用**已知答案**的合成数据验证（机械臂 mm 网格 0/40/80，像素 = 旋转 3° + 缩放 5.2 + 平移 (320,240)）：

```
AddPointPair(像素x, 像素y, 机械臂x, 机械臂y) ×9   →  NumPoints = 9
Calibrate()                                      →  Calibrated = True
                                                    ComputedRMSError = 1.01725260416667E-05
GetComputedUncalibratedFromRawCalibratedTransform()  平移(320,240) 旋转3.0000° 缩放X/Y 5.200000/5.200000
    .Invert()（＝像素 → mm）                        平移(-63.8696,-42.8699) 旋转-3.0000° 缩放X/Y 0.192308
九点回代（把 9 个像素点喂回反变换）              最大误差 = 7.816E-14 mm
随机点 像素(640,480) → mm(63.869631, 42.869920)   手算真值 (63.8698, 42.8696) ✓
```

**三条关键结论（写代码时直接照这个来）：**

1. **API 给的方向是反的**：`GetComputed...Transform()` 返回的是 **未校正 ← 已校正**（像素 ← mm）；
   要「像素 → mm」必须 `.Invert()`。不取反就会把像素当 mm 用。
2. **反变换的 `Scaling` 就是 mm/px**（实测 `0.192308`，正是 1/5.2）；正变换的 `Scaling` 是 px/mm（实测 `5.200000`）。
   **显示给操作工时别用错一个。**
3. **反变换的 `MatrixDeterminant` 当"手性检查"**：实测 `0.036982 > 0` ＝ 两坐标系同手性（没翻转）。
   若为负，说明 X 或 Y 有一个轴反了 —— 这种标定会镜像，**必须当场拒绝保存**。

默认参数（实测，落盘时代码里显式写死，别靠默认值）：
`DOFsToCompute = ScalingAspectRotationSkewAndTranslation`、`ComputationMode = Linear`、
`CalibratedOriginSpace = RawCalibrated`、`CalibratedOriginX/Y = 0`、`SwapCalibratedHandedness = False`。

> 顺带实测：默认调整参数下，`...From**Calibrated**Transform()` 与 `...From**RawCalibrated**Transform()`
> 在 5 个测试点上**差 0**（完全相同）。落盘时用 **RawCalibrated** 那支（语义精确：回到人输的 mm），
> 并加一条断言"两者一致" —— 万一以后有人动了调整参数，断言会立刻报出来。

### 3.2 PMA 的结果本来就是像素坐标（实测）

自训练一个 PMA（真图 `20260908-素材\齿轮缺齿检测\2.bmp`，1280×1024，8 位灰度），把**模板原点**设到 (630,530)：

```
设 Origin=(630,530)  → 同一张图跑：Score=0.98938  位姿平移=(629.9839, 529.9862)   差 0.016 px
把输入图整体平移 (+60,-35) 再跑              位姿平移=(689.9843, 494.9865)   期望 (690,495) ✓
```

**结论**：`CogPMAlignResult.GetPose()` 的 `TranslationX/Y` ＝ **模板原点在运行图中的像素位置**。
工件在视野里挪多少像素，它就跟着挪多少 —— 这就是喂给标定变换的那个 `(u, v)`。

### 3.3 ★ 模板原点默认是 **(0,0)**，不是训练区域中心（实测，最容易踩的一条）

```
区域 SetXYWidthHeight(500,400,260,260) → 中心 (630,530)
Pattern.Train() 之后   Pattern.Origin 平移 = (0.0000, 0.0000)      ← 注意！
不设 Origin 直接跑    位姿平移 = (-0.0161, -0.0138)                ← 这是个没意义的坐标
把 Origin 设成 (630,530)  再跑   位姿平移 = (629.9839, 529.9862)   ✓
再 Train() 一次      Origin 仍然是 (630,530)（不会被训练重置）
```

| 性质 | 实测结果 |
|---|---|
| `CogPMAlignPattern.Origin` 类型 | `CogTransform2DLinear`（可读**可写**） |
| 程序化 `Train()` 之后的默认值 | **(0, 0)** —— 不是区域中心 |
| 设成任意点能否生效 | 能，位姿立刻跟着走 |
| 重新 `Train()` 会不会被重置 | 不会 |

**这条对你的人工标定流程同样致命**：你在 QuickBuild 里点的那九个"未校正点"，**必须和 PMA 报出来的点是同一个点**。
如果模板原点在 (0,0)（＝图像左上角），那 PMA 报的像素是"图像左上角的位置"，你拿它去标定，标出来的东西全错**而且不报错**。
→ 见 §4.2 第 6 步的核对办法。

### 3.4 当前 vpp 里的 PMA **还没训练**（实测）

```
vpps\QuickBuild1.vpp → CogPMAlignTool1  Pattern.Trained = False
RunStatus = Error / "未训练。"        Results = <null>（不是空集合）
```

**影响**：分拣依赖"PMA 先找到工件"，所以**训练模板是前置条件**（§4.2 第 6 步顺带做掉）。
这与 `主运行页方案.md §17.4 (A)`"判 OK 分支还没验过"是同一个坑，两个一起解决。

### 3.5 当前 vpp 的工具树（实测）

```
CogJobManager
└─ CogJob1                      (CogToolGroup，2 个工具)
   ├─ "Image Source"            (CogInputImageTool)
   └─ CogToolBlock1             (CogToolBlock)
      ├─ CogImageConvertTool1   (彩转灰)
      └─ CogPMAlignTool1        (定位/判定)
```

`CogCalibNPointToNPointTool`（＝笔记 §五 截图里那把 `CogCalibNPointToNPointTool1`）在
`Cognex.VisionPro.CalibFix.dll` 里，**装着的**，你在 QuickBuild 里加得出来。

---

## 四、方案设计

### 4.1 分工：谁干什么

```
【VisionPro / QuickBuild —— 人工】
  打开图 → 加 CogCalibNPointToNPointTool → 人工在图上点 9 个点
         → 机械臂逐个点动到位、读位姿 → 人工填"原始已校正 X/Y"
         → 点「校正」
                    │
                    │  9 对坐标（像素 ↔ 机械臂 mm）
                    ▼
【VisionSort —— 输入框（人工交接）】
  表格填 9 行 → 「计算」→ 显示 RMSE / mm·px⁻¹ / 旋转 / 手性 → 「保存」→ config\calib.json
                    │
                    ▼
【VisionSort —— 生产（全自动，无人值守）】
  每轮：PMA 找到工件 → 像素 (u,v) → 换算成 (X,Y) mm → 范围校验 → MovJ 抓取 → 放废料区
```

人工只出现在**标定那一次**；生产运行时**不需要人在键盘前**（这是 Q3甲 + Q5甲 的意思）。

### 4.2 VisionPro 里怎么人工标（照笔记 §五，写成可照做的步骤）

1. QuickBuild 打开**生产用的那个方案**（`vpps\QuickBuild1.vpp`），或者新建一个专门标定用的方案。
2. 在 `CogToolBlock1` 里加一把 **`CogCalibNPointToNPointTool`**（工具箱 → 标定 → N 点标定）。
3. 给它一张**标定图**：`InputImage` 选当前相机实时图或一张已存的图
   （图里能看清你打算点的九个位置即可；标定板、工件、随手摆的标记都行——**Q2 说了不管标定物的事**）。
4. 在工具的「点偶」表里**人工在图上点九个"未校正点"**：这九个点是图像上的像素坐标。
5. 机械臂逐个**点动到位**（用 VisionSort 设备调试页：`X±/Y±/Z±` 按住点动），
   每到一个点，把**当前位姿**读出来填进「原始的已校正 X/Y」。
   - **九个点的分布**：要**覆盖工件可能出现的位置范围**——九点标定的有效范围就是这九个点的包围盒，
     盒子外面是外推，误差涨得很快。别九个点挤在画面中间一小块。
   - **九个点不要共线**，也不要在一条直线上退化。
6. **⚠ 两件必须同时做到的事：**
   - **高度一致**：标定的时候机械臂的 Z、以及图上那个面，要和生产抓取时**同一个高度**。
     九点标定是 **2D 线性**，管不了高度；高度一变像素比例就变（小孔成像）。
   - **核对 PMA 报的点和你在图上点的是同一个点**（§3.3 实测的坑）：
     标定完，把生产模板训练好，**拿同一张图跑一次 PMA**，看它报的位姿平移是不是你预期的那个工件点。
     如果报出来是 (0,0) 附近，说明**模板原点没设**，去把 `Pattern.Origin` 设到工件上要被吸住的那个点。
     → 这一步我们用程序帮你查（§4.5 的「核对」按钮）。
7. 点工具里的「校正」→ 表里出现换算结果（`Calibrated = True`）。

> **人工点图的精度是这套方案的既定上限**：手点 2–3 像素 ≈ **0.4–0.6 mm**（估算，按实测 0.192308 mm/px）。
> 这是 Q1丙 选定的代价，写在这里是让后面的人知道"抓不准时先怀疑这一步"。
> 想提高只有两条路：手更稳（点完放大复核），或者让程序算像素（v1.0 的甲方案，已否）。
>
> **机械臂那侧的坐标不要手抄** —— 调试页读数加人工抄写会再引入一次误差，
> 而 §4.5 的输入框能**一键读当前位姿**（`GetPoseAsync()` 的原始值）。这一半的数字必须让程序读。

### 4.3 为什么标定用 VisionPro 的界面、数据却存我们自己的 JSON

用 QuickBuild 里的 `CogCalibNPointToNPointTool` 做**人机界面**（点图、看点偶表、看校正结果），
但**不把标定留在 vpp 里**：

| 理由 | 说明 |
|---|---|
| **归属** | 标定是"现场/设备属性"，不是"视觉判定方案"的一部分。留在 vpp 里，换个方案标定就丢了 —— 现场最常出的就是"我换了个方案，怎么抓不准了" |
| **可读** | `calib.json` 存**点对**（不是矩阵），出问题能人工核对每一个数字；vpp 是二进制，看不见摸不着 |
| **一致性** | 程序每次启动用 `calib.json` 里的点对**重算**变换 —— 点对和矩阵永远不会打架 |
| **不动方案** | `VisionScheme.Load()` 现在只从 vpp 取 PMA、不改结构。标定数据外置之后，**生产用的 vpp 一个字都不用动**（那把 NPoint 工具标完就可以留在方案里，也可以删掉，程序都不读它） |

> 所以：**VisionPro 只是标定的计算器 + 点图界面**，标定结果由人工搬进程序（§4.5）。

### 4.4 新增 / 改动清单

| 文件 | 内容 | 预估 |
|---|---|---|
| `Services\CalibrationService.cs`（新） | 点对管理 / `Calibrate` / 像素→mm / JSON 存取 / 质量指标（RMSE、mm/px、旋转、手性）；注释里抄一份 §3.3 的实测证据（§4.10.2 #4） | ~230 行 |
| `Views\DeviceDebugView.Calib.cs`（新 partial） | 标定输入区：可增删行表格 + 「读当前位姿」「粘贴像素列」「计算」「保存」「**核对 PMA 原点**」「**操作指引**」 | ~230 行 |
| `Views\DeviceDebugView.Designer.cs`（改） | 加一组 `grpCalib` 控件（普通控件，**不引入 Cognex 控件**）+ `lblCalibStatus` | ~70 行 |
| `Views\CalibHelpForm.cs`（新，小） | 「操作指引」弹窗：只读等宽文本 + 「复制到剪贴板」「打开所在文件夹」 | ~90 行 |
| **`九点标定操作指引.md`（新，项目文档）** | **附录 B 全文**；同时作为嵌入资源编译进 exe（§4.10.1） | 文本 |
| `VisionSort.csproj`（改） | 加一行 `<EmbeddedResource Include="..\九点标定操作指引.md" />` | 1 行 |
| `Views\VisionConfigView.Pma.cs`（改） | 训练后检查并显示 `Pattern.Origin`；等于 (0,0) 时黄字警告 + 「把原点设到区域中心」（§4.10.2 #1 #2） | 改 ~35 行 |
| `Services\RobotPickPlace.cs`（改） | 固定取料点 → **换算出来的动态取料点**；范围校验；没标定时的行为（Q5） | 改 ~40 行 |
| `Services\RobotArmSettings.cs`（改） | 加工作范围四个字段（`PickAreaMinX/MaxX/MinY/MaxY`）；**与 `PickZ` 一起存 `calib.json`**（不落 `sysparam.json` —— 见 §11.1 的说明），界面上放在 `grpCalib` 标定区里 | ~15 行 |
| `Services\ProductionRunner.cs`（改） | `Sorting` 阶段把 PMA 的像素坐标交给分拣；换算失败/越界按 §4.8 处理 | ~20 行 |
| `Services\VisionScheme.cs`（改） | `InspectionResult` 增加 `PixelX/PixelY`（取 Score 最高的那条结果的位姿） | ~10 行 |
| `Views\MainRunView.Run.cs`（改） | 状态行加「标定 0.1923mm/px·RMSE 0.031」/「标定 无」（§4.10.2 #5） | 改 ~8 行 |

**MainForm 不动**（不新开 Tab，按 Q4 放设备调试页）。

> **一个待落盘时确认的小技术点**：`Pattern.TrainRegion` 在 QuickBuild 里人工训练出来的形状不一定是
> `CogRectangle`（可能是 `CogRectangleAffine` / 多边形），所以「把原点设到区域中心」这个按钮
> **只在能取到中心时可用**，其它形状只给提示让人去 QuickBuild 手设（§4.10.2 #1 已写明）。
> 取中心的准确写法我落盘时用小探针确认一次，不猜。

### 4.5 ★ 数据怎么从 VisionPro 进程序（Q4 的输入框）

放在**设备调试页**（`Views\DeviceDebugView`）—— 机械臂的点动、位姿显示、连接都在这页，填数时不用来回切页。

```
┌ grpCalib  九点标定 ───────────────────────────────────────────────────┐
│ 说明：在 VisionPro 的 CogCalibNPointToNPointTool 里点完九个点后，       │
│       把「未校正 X/Y」（像素）填到左两列，机械臂坐标点『读当前位姿』自动填│
│                                                                       │
│  ┌─ 表格（9 行 × 4 列，可编辑，可增删行）─────────────────────────┐ │
│  │ 序号 │  像素 X  │  像素 Y  │ 机械臂 X │ 机械臂 Y │              │ │
│  │  1   │          │          │          │          │              │ │
│  │ …    │          │          │          │          │              │ │
│  └────────────────────────────────────────────────────────────────┘ │
│  [读当前位姿 → 填到第 N 行]  行号：[_]      [加一行] [删末行] [清空]    │
│  [从剪贴板粘贴像素列]                          [操作指引] ← 附录 B 全文   │
│                                                                       │
│  [计算]  → RMSE 0.031mm │ 0.1923 mm/px │ 旋转 -3.0° │ 手性 ✓         │
│  [保存 calib.json]   [重新载入]   标定于 2026-09-17 14:05:22          │
│  [核对 PMA 原点]  → "当前 PMA 报的位姿平移 = (630.0, 530.0)，与第 5 行│
│                      的像素点相差 2.1px ✓"                            │
└───────────────────────────────────────────────────────────────────────┘
```

按钮各自干什么：

| 按钮 | 行为 |
|---|---|
| **读当前位姿 → 填到第 N 行** | `GetPoseAsync()` 的 X/Y 原样写进第 N 行的「机械臂 X/Y」。**这一半的数字绝不手抄** |
| **加一行 / 删末行 / 清空** | 表格行数不写死（现场有人点 12 个点也能用） |
| **从剪贴板粘贴像素列** | 解析剪贴板文本，按行取前两个数填进「像素 X/Y」。VisionPro 的点偶表**能不能复制我没有实测**（见下），能复制就省一半手活，不能就手填 |
| **操作指引** | 弹出附录 B 全文（嵌入资源，只读等宽字体 + 「复制到剪贴板」）——**N2 的落点** |
| **计算** | 先**强制核对 PMA 原点**（不通过直接拒绝），再 `CalibrationService.Calibrate()` → 显示 RMSE / mm·px⁻¹ / 旋转角 / 手性；失败给中文原因（点数不足、共线、镜像…） |
| **保存** | 写 `config\calib.json`（存点对）。**手性不通过时拒绝保存** |
| **重新载入** | 从 `calib.json` 重新读（改了文件不用重启） |
| **核对 PMA 原点** | 跑一次 PMA（或读上次结果），把它的位姿平移和表格里的像素点比一比，抓 §3.3 那个"模板原点默认 (0,0)"的坑 |

> **一个我没实测、但不影响设计的问题**：VisionPro 里那个"点偶"表格是 Cognex 自己的编辑器控件，
> **能不能 Ctrl+C 复制出文本，我没有实测过**（它那类表格控件通常不支持复制）。
> 所以设计成"**粘贴是加分项，手填是保底**"，不依赖它 —— 手填 18 个数字是可接受的，
> 而机械臂那 18 个数字由程序读，把抄错风险砍掉一半。

> **框里装什么已经定了**：**标定数据（9 对点：像素 ↔ 机械臂 mm）**。
> 也就是说人工只在**标定那一次**出现；填完、算完、保存完，**生产运行时不需要人在键盘前** ——
> 每轮由程序自己把 PMA 的像素换算成 mm 去抓（这正配上 Q3甲 和 Q5甲）。
> 不做"每件人工填一个目标点"那一套（如果哪天要演示"人工点一下机械臂就过去"，再加一个小框大约 30 行）。

### 4.6 `CalibrationService` 接口（草案）

```csharp
public sealed class CalibrationService
{
    public static CalibrationService Shared { get; }        // 单例，与其它服务一致

    public bool   IsCalibrated { get; }                     // 算过且算成功
    public int    PairCount    { get; }
    public double RmsError     { get; }                     // mm
    public double MmPerPixel   { get; }                     // 反变换的 Scaling
    public double RotationDeg  { get; }                     // 相机轴相对机械臂轴的夹角
    public bool   HandednessOk { get; }                     // 行列式 > 0

    public void Clear();
    public bool AddPair(double pixelX, double pixelY, double robotX, double robotY, out string message);
    public bool Calibrate(out string message);              // ≥3 点、不共线、手性检查
    public bool PixelToRobot(double pixelX, double pixelY, out double robotX, out double robotY);

    public bool Load(out string message);                   // config\calib.json
    public bool Save(out string message);                   // 手性不通过拒绝保存
}
```

校验规则（**都给中文原因，不许静默失败**）：

| 情况 | 行为 |
|---|---|
| 点数 < 3 | 拒绝：「至少要 3 对点（推荐 9 对）」 |
| 所有点共线 / 退化 | 接住异常 → 「九个点不要在一条直线上」 |
| `HandednessOk == false` | **拒绝保存**：「标定出现镜像（X/Y 有一个轴反了），请检查九个点与机械臂坐标是否对错轴」 |
| 反变换含 NaN/Inf | 拒绝：「标定结果无效」 |
| 没有标定就换算 | 返回 false：「还没标定」 |

### 4.7 存储（Q3甲）

`config\calib.json`（与 `sysparam.json` 同目录）：

```json
{
  "savedAt": "2026-09-17 14:05:22",
  "rmsError": 0.031,
  "mmPerPixel": 0.192308,
  "rotationDeg": -3.0,
  "handednessOk": true,
  "pickZ": -50.0,
  "pairs": [
    { "px": 500.1, "py": 380.2, "mx": 160.0, "my": -20.0 },
    { "px": 660.4, "py": 379.8, "mx": 190.0, "my": -20.0 }
  ]
}
```

- **存点对、不存矩阵**：矩阵是派生数据，每次启动 `Calibrate()` 重算 —— 参数一改就自动一致。
- `PickZ` 跟着标定一起存（同一批现场数据，方便整体重放）。
- 记 `savedAt`，界面显示"标定于 …"，防止拿着去年的标定跑生产。

### 4.8 `RobotPickPlace` 怎么改

```csharp
// 现在（写死）
await arm.MoveJAsync(s.PickX, s.PickY, s.PickZ, 0);

// 改成（换算 + 校验）
if (!CalibrationService.Shared.PixelToRobot(pixelX, pixelY, out double px, out double py))
    return "没标定或换算失败：…";                        // 交给调用方写进 note，不丢事件

if (!WithinPickArea(px, py, out string areaMessage))
    return "换算点超工作范围：" + areaMessage;            // 拒动！不许硬走
await arm.MoveJAsync(px, py, s.PickZ, 0);
```

- **Z 与 `rHead` 仍然固定**（Z = 示教好的抓取高度；`rHead` 恒 0，Q6）。
- **工作范围校验是保命的**：手点标定精度只有 0.4–0.6mm、还可能配成镜像，机械臂会往墙上走。
  范围默认值给保守区间（示教点 ±80mm），现场在**标定区**里按工件实际可能出现的范围调
  （`PickAreaMinX/MaxX/MinY/MaxY`，与 `PickZ` 一起存 `calib.json`）。
- 越界/失败一律**拒动 + 备注 + 报警**，不允许"夹到边界继续跑"。

### 4.9 主运行页时序（有没有变化）

时序**不变**（`主运行页方案.md §3.2` 那套照旧）：`等工件 → 停带 → 拍照 → 检测 → 分拣 → 恢复带`。

变的是 `Sorting` 那一格，多两步：

```
NG → 取 PMA 结果的像素 (u,v) → CalibrationService 换算 → 范围校验 → 机械臂
                                 └─ 任一步失败：不动机械臂，note 写原因，本轮照常写库
```

> 停带之后工件是**静止**的，所以"拍照那一帧里的像素位置"和"抓取时刻的位置"是同一个 —— 这是本方案成立的前提
> （改成"飞行抓取"就不成立了，本期不做）。

### 4.10 「操作指引」与防呆：把经验写进项目，不是写在聊天里（你指定的 N1 / N2）

目标很实在：**下一个人（或三个月后的你）不用重新踩一遍坑。**
所以分两条路写 —— 一条给人看（文档 + 程序内帮助），一条给程序执行（防呆检查）。

#### 4.10.1 N2：人工标定方法写进项目（降低学习成本）

**单一真相源**：新建 `20260914-资料/04-WinForms/九点标定操作指引.md`，全文见 **附录 B**。
内容面向"第一次做这件事的人"，六节：

| 节 | 内容 |
|---|---|
| 这份东西干什么 / 什么时候要重做 | 为什么要标定；相机动过、换产品、改高度就得重标 |
| 开始之前 3 件事 | 机械臂上电可点动、VisionPro 能出图、**Z 高度 = 生产抓取高度** |
| 一、VisionPro 里怎么做（9 步） | 一步一步照做：加工具 → 点像素 → 点动读位姿 → 校正 → 抄回程序 |
| 二、算完必须看的四个数 | RMSE / mm·px⁻¹ / 旋转 / 手性 —— 各自"好的样子"和"不对怎么办" |
| 三、★ 模板原点默认 (0,0) | 现象、原因、怎么查、怎么修（＝ N1 的人话版） |
| 四、三个"必须一致" | 高度 / 相机 / 成像设置 |
| 五、做完必须验证（反查） | 拿没参与标定的位置验，目标 < 0.5mm；每天开机做一次 |
| 六、抓不准先查什么（对症表） | 六种现象 → 各自先查哪一条 |

**同一份文字进程序**（不用现场带文件、不会和文档打架）：

- 设备调试页 `grpCalib` 里一个按钮「**操作指引**」，弹出一个只读窗口显示这份指引（等宽字体，可滚动）
- 文本以 **嵌入资源** 的方式编译进 `VisionSort.exe`：
  csproj 加一行 `<EmbeddedResource Include="..\九点标定操作指引.md" />`
  → **md 改了，重新编译程序里的文字就跟着变**，不存在"两份文字不同步"
- 弹窗里再给一个「复制到剪贴板」（方便发微信/打印）和一个「打开所在文件夹」

> 为什么不把文字硬编码在 `Calib.cs` 里：硬编码的第二份文字迟早会和文档分叉，
> 而"以文档为准"这句话在实践中没人会去核对。嵌入资源是唯一真相源。

#### 4.10.2 N1：把「模板原点默认 (0,0)」写进系统（防呆，四处一起做）

文字提示只能救一半，另外一半必须让程序拦住。四处：

| # | 位置 | 做什么 | 为什么在这儿 |
|---|---|---|---|
| **1** | **视觉配置页（坑的源头）** | 训练模板后检查 `Pattern.Origin`：平移 ≈ (0,0)（阈值 0.5px）→ 黄字提示「**模板原点还在 (0,0)：判定不受影响，但拿它去驱动机械臂会全错**」；并给一个「把原点设到区域中心」按钮（`TrainRegion` 是 `CogRectangle` 时可用，其它形状只提示去 QuickBuild 手设） | 模板是在这页训练的，**在源头拦最省钱** |
| **2** | **视觉配置页（看得见）** | 常显一行 `模板原点：(630.0, 530.0)`；等于 (0,0) 时整行标红 | 原点是个隐藏状态，不显示出来就等于不存在 |
| **3** | **标定区（使用侧强制）** | 「计算」前**强制核对**：跑一次 PMA，把它的位姿平移与表格里第 N 行的像素点比一比，偏差 > 5px → 红字 + **拒绝计算/保存**；另外保留一个「核对 PMA 原点」按钮随时手动查 | 标定这东西错了不报错，只能靠强制检查 |
| **4** | **代码注释** | `CalibrationService` / `VisionScheme` 里把 §3.3 的实测数据抄一段（`Train()` 后 `Origin=(0,0)`、设成 (630,530) 后位姿 (629.9839,529.9862)） | 防止以后有人觉得这个检查"多余"顺手删掉 |

另外顺带一条小的（同源思想，一起做）：

| # | 位置 | 做什么 |
|---|---|---|
| **5** | **主运行页状态行** | 现有状态行里加一段「标定 0.1923mm/px·RMSE 0.031」或「**标定 无**」——生产时用的哪份标定，界面上必须说真话（和「库」指示灯那条做法一致：不让界面撒谎） |

#### 4.10.3 这四条防呆各自能拦住什么

| 拦得住 | 拦不住 |
|---|---|
| 模板原点没设就去标定（#1 #3） | 标定点本身点错了位置（手点误差、顺序错位）——只能靠 RMSE/手性 + 反查验证 |
| 用一份过期/不存在的标定跑生产（#5） | 相机在两次生产之间被碰过（只能靠每天反查验证） |
| 改了模板忘了原点（#1 #2） | 高度不一致（只能靠人守 §4.2 第 6 步） |
| 有人以后"顺手"删掉检查（#4） | —— |

> 一句话：**能程序拦的程序拦，拦不住的写成人能照着做的检查表**，两者都在项目里，不在对话里。

### 4.11 安全与互锁

| 项 | 做法 |
|---|---|
| 标定 ↔ 生产互锁 | 生产运行中（`ProductionRunner.IsRunning`）标定区的按钮全灰，不许采点/计算/保存 |
| 机械臂没连 | 「读当前位姿」弹「请先点『连接机械臂』」，不静默失败 |
| 手性不通过 | 拒绝保存（§4.6），并把可疑的两行标红 |
| 越界 | 拒动（§4.8） |
| 急停 | 标定不涉及机械臂自动运动（人工点动），急停按钮照旧可用 |

---

## 五、落盘顺序（分四步，每步独立验收）

1. **地基（不碰界面）**：`CalibrationService` + `RobotArmSettings` 范围字段 + 探针跑数学/校验用例（§六 C1–C4）
2. **写进项目（你指定的 N2）**：`九点标定操作指引.md` 全文 + 嵌入资源 + `CalibHelpForm` 弹窗 + csproj 一行
   —— 这一步**不需要机械臂也能验收**（打开弹窗、复制、文字完整）
3. **输入区 + 防呆（含 N1 的 #3/#5）**：`DeviceDebugView.Calib.cs` + Designer 加 `grpCalib` + 全部按钮 + 主运行页状态行
   （C5/C6 界面真点一遍）
4. **接线 + 源头防呆（N1 的 #1/#2/#4）**：`VisionScheme` 出像素 → `ProductionRunner` 传 → `RobotPickPlace` 换算 + 校验
   （C7 假桥接器）+ 视觉配置页原点检查显示 + 五个老页面回归（C8）

> 每步落盘前先给你看改动清单。第 2 步放这么前，是因为它**不依赖机械臂、不依赖相机**，
> 而且它是"降低学习成本"这件事的全部 —— 先把它做完，后面谁接手都能照着做。

---

## 六、自测计划（本机无相机 / 无机械臂 / 无标定板）

| 编号 | 用例 | 办法 |
|---|---|---|
| C1 | 标定数学：合成点对（含旋转/缩放/平移）→ RMSE ≈ 0、九点回代 < 1e-9 mm、mm/px 正确 | 探针（§3.1 已预跑，落盘时做成正式用例） |
| C2 | 手性检查：故意把 X 取反 → `HandednessOk == false` 且**拒绝保存** | 探针 |
| C3 | 退化输入：2 点 / 共线 9 点 / 空表 / 有空格和空行 → 都要有中文原因且不抛未捕获异常 | 探针 |
| C4 | JSON 存取：正常 / 文件不存在 / 字段缺 / 数值非法 / 点数不足 | 探针 + 手工改文件 |
| C5 | 范围校验：把范围收窄 → 换算点越界时**不发出 MoveJ** | 探针（断言假桥接器没收到命令） |
| C6 | 界面：真 GUI 点一遍（连机械臂 → 读当前位姿填 9 行 → 计算 → 保存 → 重新载入 → 核对），一个按钮都不漏 | 探针驱动 + 截图 |
| C6b | **指引（N2）**：点「操作指引」→ 弹窗打开；文字与 `九点标定操作指引.md` **逐字节一致**（读嵌入资源比对文件）；「复制到剪贴板」内容完整；程序目录被挪走也不受影响（嵌入而非外链） | 探针 + 文件比对 |
| C6c | **原点防呆（N1 #1/#2）**：故意用一个 `Origin=(0,0)` 的已训练模板 → 视觉配置页必须出黄字警告；把原点设上后警告消失；`lblPatternOrigin` 的数与 `Pattern.Origin` 一致 | 探针（真 vpp + 真图，用 §3.3/3.4 的自训练套路） |
| C6d | **原点防呆（N1 #3）**：`Origin=(0,0)` 时点「计算」→ **拒绝**且给中文原因；原点设对后同数据能算过 | 探针 |
| C6e | **主运行页（N1 #5）**：有标定 / 无标定两种情况下状态行文案正确，不撒谎 | 探针 |
| C7 | 端到端：真 VPP + 真图 + **假桥接器** → 断言发出去的 `MoveJ(x,y,z)` 是**换算后的坐标**而不是示教点；没标定时断言"没发 MoveJ" | 假桥接器（v3.0 §6.2 那套） |
| C8 | 回归：相机页 / 视觉配置页 / 设备调试页（点动/队列）/ 数据日志页 / 系统设置页原有自测重跑 | 老用例 |

**本机测不了的**（必须现场做，见 §七）：真实像素→真实 mm 的对应、机械臂实际抓得准不准、
「剪贴板粘贴」能不能用（取决于 VisionPro 那个表格控件）。

---

## 七、现场验证清单（T-live）

1. **先训练生产模板**（§3.4），并**核对 PMA 原点**（§3.3 / §4.5 的「核对」按钮）——
   这一步不做，标定再准也没用。
2. **九个点覆盖工件可能出现的位置范围**：九点标定的有效范围就是这九个点的包围盒，盒子外是外推。
   别九个点挤在画面中间。
3. **高度一致**：标定时的 Z 与生产抓取 Z 相同；标定图上那个面与生产工件上表面同高。
4. **反查验证（最关键）**：找**一个没参与标定的位置**，用 VisionPro 读出像素、人工填表算 mm，
   让机械臂走过去，量实际偏差。**目标 < 0.5mm**，超过就查（高度不对/相机动过/点错了/PMA 原点没设）。
5. **手点重复性**：同一个点连点 5 次记下像素，看散差（这是 0.4–0.6mm 那个估算的实测版）。
6. **越界演练**：故意给一个视野边缘的工件，确认机械臂**拒动**并报警，而不是硬走。
7. **日常点检**：每天开机按第 4 条做一次反查验证。相机/镜头/支架动过 → 必须重标。
8. 抓取效果整定：`PickDelayMs`（吸住后等多久才抬）、`ResetDelayMs`（放开后等多久才走）——沿用现有参数。

---

## 八、风险与规避

| 风险 | 影响 | 规避 |
|---|---|---|
| **人工手点精度 0.4–0.6mm** | 抓取有系统偏差，重标一次结果就变一次 | 这是 Q1丙 的固有代价；现场按 §七.4 反查验证量化它；必要时放大点图复核 |
| **模板原点没设**（默认 (0,0)） | PMA 报的像素是图像原点，标定全错且**不报错** | **四处防呆（§4.10.2 #1–#4）**：训练时黄字警告 + 常显原点 + 计算前强制核对、不通过拒绝保存 + 代码注释留证据 |
| **标定高度与生产高度不一致** | 比例误差，离中心越远越偏 | 标定时 Z 与生产一致；界面红字提示 |
| **相机/镜头/支架被动过** | 整套标定作废 | 日常反查验证；界面显示"标定于 …" |
| **九个点配成镜像** | 机械臂往反方向走，撞设备 | `MatrixDeterminant > 0` 强制检查，负值**拒绝保存**（§3.1） |
| **机械臂坐标抄错** | 单点错 → 整张标定歪 | 「读当前位姿」按钮，**这半边的数字不允许手抄** |
| **换算点超工作范围** | 机械臂撞机 | 范围校验 + 拒动 + 报警（§4.8） |
| **PMA 找不到工件** | 分拣这一步失败 | 中文原因写进 `note`，工件留在带上，**不动机械臂** |
| **VisionPro 点偶表不能复制** | 人工要手填 18 个数字 | 设计成"粘贴是加分项、手填是保底"，不依赖它（未实测） |
| 生产运行中误改标定 | 用错的标定跑生产 | 运行中把标定区按钮置灰（§4.11） |
| **指引和程序里的文字分叉**（N2） | 两个人按两套说法做事 | 用**嵌入资源**：`九点标定操作指引.md` 是唯一真相源，程序显示的就是它（§4.10.1）；C6b 用例逐字节比对 |
| **有人以后"顺手"删掉原点检查** | 坑复发，而且不报错 | 代码注释里留实测证据（§4.10.2 #4）；`CalibrationService` 的检查是**拒绝保存**级别的，不是提示 |

---

## 九、拍板状态

| # | 问题 | 决定 |
|---|---|---|
| Q1 | 九点怎么采 | ✅ **丙：人工在 VisionPro 上点** |
| Q2 | 标定物用什么 | ✅ **不管标定物的事** |
| Q3 | 标定数据存哪 | ✅ **甲：`config\calib.json`（存点对，与 vpp 解耦）** |
| Q4 | 界面放哪 | ✅ **不新开 Tab，加"人工把 VisionPro 坐标递给机械臂"的输入框 → 放设备调试页；框里装标定数据（9 对点：像素 ↔ 机械臂 mm）** |
| Q5 | 没标定时怎么办 | ✅ **甲：拒绝分拣 + 报警 + 备注写进库** |
| Q6 | `rHead` 跟不跟工件角度 | ✅ **甲：固定 0** |
| Q7 | 采点方式 | ✅ **只做手动**（在 VisionPro 里人工点） |
| **N1** | 把「模板原点默认 (0,0)」写进系统 | ⬜ **方案已写（§4.10.2），等你确认** |
| **N2** | 把人工标定方法写进项目 | ⬜ **全文见附录 B，等你确认文字** |

> N1 / N2 是我按你那句"写到系统里 / 写到项目里"展开的具体做法，**不是既成事实** ——
> 特别是附录 B 那份指引的**措辞**，你过一眼；觉得哪句不像给操作工看的，我改。

---

## 十、我代为决定的项（不否决就照此执行）

- 标定算法用 **`CogCalibNPointToNPoint`（进程内）**，取 `GetComputedUncalibratedFromRawCalibratedTransform().Invert()` 当"像素→mm"，并断言它与 `...FromCalibratedTransform().Invert()` 一致（§3.1）
- 存**点对**不存矩阵；启动时重算
- 反变换的 `Scaling` 当 `MmPerPixel` 显示；`MatrixDeterminant > 0` 当手性检查
- 至少 3 对点才能算，推荐 9 对；界面不给"少于 9 点就算"的捷径
- 越界 / 没标定 / 换算失败 → **一律拒动**，写 `note`、报警，本轮照常写库（不丢事件）
- `PickZ` 与 `rHead` 本期仍用固定值；`rHead` 恒 0
- **输入区放设备调试页**（机械臂的点动/位姿都在这页，填数不用切页），用普通 WinForms 控件，**不引入 Cognex 控件**
- 输入表格**可增删行**（不写死 9 行 —— 现场有人会点 12 个点，让他能用）
- 机械臂坐标**只允许程序读**（「读当前位姿」），像素列允许手填/粘贴
- **生产用的 vpp 一个字都不用动**；那把 NPoint 工具标完留不留都不影响程序
- 不改 `DobotArm` 类库（机械臂侧接口够用）
- **指引文字以 `九点标定操作指引.md` 为唯一真相源**，程序用嵌入资源显示它，不另写一份（§4.10.1）
- **原点检查是"拒绝保存"级别**，不是一句提示（§4.10.2 #3）
- 指引弹窗放设备调试页（和标定区在一起），用等宽字体只读显示 Markdown 原文（不渲染成富文本 —— 少一个依赖，也少一处出错）
- 指引里**不放截图**（截图会过期、体积大）；要看图就看 `20260909-note/visionPro09.md` 里的

---

## 附录 A：本次探针原始输出（证据）

探针文件：`_tmp\calib_probe.cs`、`calib_probe2.cs`、`calib_probe3.cs`、`calib_probe4.cs`
（编译进 `bin\x64\Debug\net48`，跑完已连同 exe/txt 一起删除，**产品代码零改动**）

### A.1 九点标定数值验证

```
STEP 1: 九点标定数值验证
  点0: 像素(320.000,240.000)  <-  mm(0.000,0.000)
  点1: 像素(309.114,447.715)  <-  mm(0.000,40.000)
  …（共 9 点，像素 = 旋转3° + 缩放5.2 + 平移(320,240)）
STEP 2: Calibrate()
  Calibrated = True
  ComputedRMSError = 1.01725260416667E-05
STEP 3: GetComputedUncalibratedFromCalibratedTransform() -> Cognex.VisionPro.CogTransform2DLinear
  正向 MapPoint(0,0) -> (320.0000,240.0000)
STEP 4: 反变换（像素 -> mm）
  点0..点8 误差 ~1e-14
  >>> 九点回代最大误差 = 7.816E-014 mm
  相邻点像素距离 208.000px 对应 40mm -> 0.192308 mm/px
  随机点 像素(640.0,480.0) -> mm(63.8696,42.8699)

A 段（calib_probe2）默认参数与两条取法对比：
  DOFsToCompute              = ScalingAspectRotationSkewAndTranslation
  ComputationMode            = Linear
  CalibratedOriginX/Y        = 0 / 0
  CalibratedOriginSpace      = RawCalibrated
  CalibratedXAxisRotation    = 0
  CalibratedXAxisRotationSpace = RawCalibrated
  SwapCalibratedHandedness   = False
  UncalibratedFromCalibrated    = 平移(320.0000,240.0000) 旋转3.0000° 缩放X/Y 5.200000/5.200000
  UncalibratedFromRawCalibrated = 平移(320.0000,240.0000) 旋转3.0000° 缩放X/Y 5.200000/5.200000
  像素->mm（取 Calibrated 的反变换）: 平移(-63.8696,-42.8699) 旋转-3.0000° 缩放X/Y 0.192308
  像素->mm（取 RawCalibrated 的反变换）: 平移(-63.8696,-42.8699) 旋转-3.0000° 缩放X/Y 0.192308
  两条取法在 5 个像素点上差 (0.00E+000, 0.00E+000)
  从 invRaw 反读几何量：ScalingX/Y = 0.192308/0.192308  旋转 = -3.000000°  行列式 = 0.036982
```

### A.2 PMA 结果 = 模板原点的像素位置

```
图: 1280x1024 CogImage8Grey       TrainRegion 中心 = (630, 530)
Pattern.Origin 成员: CogTransform2DLinear Origin
Train() -> Trained = True
Origin 平移 = (0.0000,0.0000) 旋转=0.0000° 缩放=1.000000
RunStatus = Accept        Results = 1
  结果0: Score=0.98938 Accepted=True 位姿平移=(-0.0161,-0.0138) 旋转=0.0000° 缩放=1.000000
  >>> 与 Origin 的差：(1.615E-002, 1.376E-002) 像素
把输入图整体平移 (+30,+20) 像素后再跑：
  平移后结果: Score=0.99264 位姿平移=(29.9841,19.9866)
  >>> 相对原图结果的位移 = (30.0003,20.0004) 像素（期望 ≈ (30,20)）
```

### A.3 模板原点默认 (0,0)，且可以设

```
Origin 属性: 可读=True 可写=True 类型=Cognex.VisionPro.CogTransform2DLinear
训练后默认 Origin 平移 = (0, 0)
不设 Origin：Results=1  位姿平移=(-0.0161,-0.0138)
已把 Origin 设为 (630, 530)
设后读回 Origin 平移 = (630, 530)  Trained=True
再 Train() 后 Origin 平移 = (630, 530)          ← 不会被训练重置
设 Origin 后：RunStatus=Accept  Results=1
  位姿平移=(629.9839,529.9862)  期望≈(630.0000,530.0000)  差=(1.615E-002,1.376E-002)
  图平移(+60,-35)后：位姿平移=(689.9843,494.9865) 期望≈(690.0000,495.0000)
```

### A.4 当前 vpp

```
路径: D:\...\bin\x64\Debug\net48\vpps\QuickBuild1.vpp  存在=True
根类型: Cognex.VisionPro.QuickBuild.CogJobManager
VisionTool 类型: Cognex.VisionPro.ToolGroup.CogToolGroup    工具数=2
  - CogInputImageTool  "Image Source"
  - CogToolBlock  "CogToolBlock1"
    - CogImageConvertTool  "CogImageConvertTool1"
    - CogPMAlignTool  "CogPMAlignTool1"
PMA: CogPMAlignTool1  已训练=False  AcceptThreshold=0.5
  RunParams 里名字带 Find 的成员： Int32 ApproximateNumberToFind = 1
运行状态: Error / 未训练。    结果数 = -1（Results 为 null）
```

---

## 附录 B：人工九点标定操作指引

> **全文不在这里重复** —— 它是独立文件：`20260914-资料\04-WinForms\九点标定操作指引.md`。
> 这就是 §4.10.1 说的"单一真相源"：一份文字，给人看的是这个 md，给程序看的是它的**嵌入资源**。
> 在这里再抄一遍就等于自己造第二份，迟早分叉（这正是当初拒绝把指引硬编码进 `Calib.cs` 的同一个理由）。

程序里的入口：设备调试页 → 九点标定区 → 「操作指引」按钮（`Views\CalibHelpForm.cs`），
弹窗内容 = 该 md 的嵌入资源，逐字节一致（自测 C6b 就是比对这个）。

---


## 十一、实测记录

> 落盘后在这里追加（沿用其它方案的做法：改了什么、怎么验的、抓到什么问题、还没验什么）。

### 11.1 第 1 步：`CalibrationService` + 抓取几何字段（**已完成并验证**）

**改了什么**（2 个文件，都不碰界面、不碰现有流程）

| 文件 | 内容 |
|---|---|
| `Services\CalibrationService.cs`（新） | `CalibPair` + `CalibrationService`：`AddPair / Calibrate / Clear / PixelToRobot / Load / EnsureLoaded / Save / SnapshotPairs / Describe` + `IsCalibrated / PairCount / RmsError / MmPerPixel / RotationDeg / HandednessOk / SavedAt / LastError`；内含一个极简 JSON 读写（**不引新引用**，出错能报中文原因） |
| `Services\RobotArmSettings.cs`（改，+4 字段） | `PickAreaMinX/MaxX/MinY/MaxY`，默认 **120 / 280 / -80 / 80**（＝示教点 200/0 的 ±80mm） |

**几条写死在代码里的实测结论**（注释里也抄了一份，防以后被人"顺手优化"掉）：

- 取 `GetComputedUncalibratedFrom**RawCalibrated**Transform().Invert()` 当"像素→mm"，并**自检**它与
  `...From**Calibrated**Transform()` 是否一致；不一致（＝有人动了调整参数）就拒绝算，不悄悄算
- `MmPerPixel` ＝ 反变换的 `Scaling`；`HandednessOk` ＝ `MatrixDeterminant > 0`
- ⚠ **`Load`/`Save` 顺带把 `PickZ` 与四个范围值也存进 `calib.json`** —— 这**改了方案原来的归属**
  （原方案说落 `sysparam.json` 的 robot 组）。理由：它们是同一批"抓取几何"数据，跟着标定一起重放
  才不会对不上；而且省掉了改 `SysConfigView.Designer` + `SysConfigView.Robot.cs` + Modbus partial
  一整套存/读代码。**唯一真相源仍是 `RobotArmSettings.Shared`**，`calib.json` 只负责持久化。
  到第 3 步做 `grpCalib` 时，这四个框放**标定区里**（和产生这些数的东西在一起），不再进系统设置页。

**验证结果：探针 51 项断言全过（`过 51 / 失败 0`）**；探针跑完即删，`bin\config` 只剩 `sysparam.json`。

| 用例 | 验了什么 | 结果 |
|---|---|---|
| **C1 数学** | 合成 9 点（旋转 3°、缩放 5.2、平移 (320,240)）→ `Calibrate` 成功；`MmPerPixel = 1/5.2`（误差 < 1e-9）；`RotationDeg = -3.000000`；像素 (640,480) → mm (63.869631, 42.869920)（与手算真值一致）；**九点回代最大误差 < 1e-6 mm** | ✅ |
| **C2 手性** | 机械臂 X 取反 → 行列式 `-0.0369822 < 0` → `Calibrate` 返回 false、`HandednessOk=false`、`IsCalibrated=false`、`Describe()="镜像 ✗（不可用）"`、**`Save` 拒绝**、**`PixelToRobot` 拒绝** | ✅ |
| **C3 退化输入** | 空表 / 2 对点 / 共线 9 点 / 没算就 Save / `AddPair(NaN)` / 重复点 —— 全部返回 false 且给中文原因，**无未捕获异常** | ✅ |
| **C4 存取** | 存 → 文件结构正确（9 个点）→ 改坏内存值 → 读回：`PickZ`、`PickAreaMinX`、`PickAreaMaxY` 全部恢复；**9 个点对逐位原样往返**（`G17`）；坏文件（乱码 / 点数不足 / 某字段是字符串）各自给**精确到第几项**的中文原因；文件不存在 → 返回 true 且"未标定"、拒绝换算 | ✅ |

**自测中发现并改掉的两个问题**（都属于"提示会把人引到错方向"这一类）

1. **镜像标定原本被算成"已标定"** → `IsCalibrated` 改成 `_calibrated && _handednessOk`。
   否则主运行页的 Q5甲"没标定就拒绝分拣"会放一份镜像标定过去。同时 `Describe()` 增加"镜像 ✗（不可用）"。
2. **坏文件的报错说不清位置**：原来只说"第 0 个字符处不是数字："（0 起算 + 空串，看着像程序自己坏了）。
   现在说"第 N 个字符处应该是数字或引号，实际看到 '这'"；并且 `pairs` 里某一项不是数字时，
   直接报到 **`pairs` 里第 3 项的 px/py/mx/my 有一个不是数字** —— 原来会含糊地报成"至少要 3 对点"，
   把人往"点不够"的错方向引（这正是 `主运行页方案.md` 里记过的那类教训）。

**编译**：Debug x64 与 Release x64 均 **0 error / 0 warning**。

**这一步没做**（属于后面几步）：操作指引落盘（第 2 步）、`grpCalib` 输入区与那四个范围框（第 3 步）、
接进 `RobotPickPlace` / `ProductionRunner`、视觉配置页原点检查（第 4 步）。

### 11.2 第 2 步：操作指引落盘 + 嵌入资源 + 弹窗（**已完成并验证**）

**改了什么**（3 个文件）

| 文件 | 内容 |
|---|---|
| `九点标定操作指引.md`（新，**项目文档**） | 全文六节：这东西干什么/什么时候重做 → 开始前 3 件事 → VisionPro 里 9 步 → 算完必看的四个数 → ★模板原点 (0,0) → 三个必须一致 → 反查验证 → 抓不准对症表 |
| `VisionSort.csproj`（改，+1 ItemGroup） | `<EmbeddedResource Include="..\九点标定操作指引.md">` + `LogicalName` |
| `Views\CalibHelpForm.cs`（新） | 只读等宽文本弹窗 + 「复制到剪贴板」「打开所在文件夹」「关闭」；**手写代码搭、不建 .Designer.cs**（只读查看器，设计器只会多一份要同步的样板） |

**顺带把方案里的附录 B 改成"指针"**：原来那里抄了指引全文，那样等于自己造第二份文字 ——
和当初拒绝把指引硬编码进 `Calib.cs` 是同一个理由。现在附录 B 只有一句"全文在 `九点标定操作指引.md`"。

**验证结果：探针 24 项断言全过（`过 24 / 失败 0`）**，探针跑完即删。

| 验了什么 | 结果 |
|---|---|
| 程序集里有 `VisionSort.九点标定操作指引.md`（清单里 7 个资源，其余 6 个是老窗体的 .resources） | ✅ |
| **嵌入资源与磁盘文件逐字节一致**（3183 字符 = 3183 字符，`Ordinal` 比较） | ✅ |
| 内容抽查 7 处关键词（`模板原点默认在 (0,0)`、`读当前位姿`、`手性`、`反查`…）都在 —— 防"编进去了但是个空壳" | ✅ |
| 窗体标题、只读、多行、**不自动换行**（表格才不会被拆烂）、等宽 Consolas、文本框内容与指引一致 | ✅ |
| 三个按钮都在（复制到剪贴板 / 打开所在文件夹 / 关闭） | ✅ |
| `FindFileOnDisk()` 从 `bin\x64\Debug\net48` 往上 5 层正确找到 `04-WinForms\九点标定操作指引.md` | ✅ |

> 探针是用反射调私有构造函数造窗体、**不 ShowDialog**（否则探针会卡在那里等人点按钮）。

**编译**：Debug x64 **0 error / 0 warning**。

### 11.3 第 3 步：`grpCalib` 输入区 + 防呆 #3/#5（**已完成并验证**）

**改了什么**（6 个文件）

| 文件 | 内容 |
|---|---|
| `Views\DeviceDebugView.Calib.cs`（新，~600 行） | 输入区全部：表格读写、读位姿、粘贴像素列、计算、保存、重新载入、核对原点、操作指引、抓取几何四个框、状态/指标标签 |
| `Views\DeviceDebugView.Designer.cs`（改） | 加空的 `grpCalib` 占位；`grpRobot` Fill→Top(370)；`flpRb` 高 135→**46**；`Panel2` 先加 grpCalib 再加 grpRobot |
| `Services\VisionScheme.cs`（改） | 新增 `PatternOriginCheck` + `CheckPatternOrigin()`（读 `Pattern.Origin`、`Pattern.Trained`、训练区域中心） |
| `Views\MainRunView.Run.cs`（改） | 状态行加「标定 …」（N1 #5），放在**第二位**（末尾会被裁）；`OnLoad` 里 `EnsureLoaded` 一次 |
| `Views\DeviceDebugView.Conveyor.cs`（改，1 行） | OnLoad 里调 `InitCalibPanel()` |
| `Views\CalibHelpForm.cs`（改，1 行 + 注释） | `WordWrap` 关→**开**（见下面"抓到的问题"第 3 条） |

**改布局前先量了真数**（不靠肉眼估）：写了个探针把 `MainForm` 按 1280×774 跑起来，量出
`Panel2 = 798×656`、`grpConveyor` 子控件底边 437/444、**`pnlPos` 高 321 但内容只占 118**、
`flpRb` 高 135 但五个按钮只占一行。于是：

- `flpRb` 135→46 → 白捡 89px
- `grpRobot` Fill→Top(370)：`pnlPos` 由 321 缩到 124（内容 118，仍完整）
- 剩下的 **794×286** 全给 `grpCalib`

布局：左表格 374×250（9 行要 203，放得下）、右三行按钮 + 拟合指标 + 抓取几何 + 状态。
**探针逐个控件断言 `Right/Bottom ≤ ClientSize`，0 个越界。**
（第一版曾把 `grpCalib` 放左半区：只有 454×212，装不下；放右半区才够。）

**验证结果：探针 48 项断言全过（`过 48 / 失败 0`）**，另有真 GUI 走查一轮。探针跑完即删。

| 用例 | 验了什么 | 结果 |
|---|---|---|
| **C6 控件清单** | 表格 5 列 9 行、表头「像素X/像素Y/机械臂X/机械臂Y」、序号列只读、数据列可编辑；**10 个按钮全在**且文字正确；**没有控件超出边界**；表格放得下 9 行 | ✅ |
| **C6 真 GUI** | 无障碍树列出 `组 "九点标定（…）"` 下 9 行 × 5 个可编辑格 + 10 个按钮 + 5 个几何框，坐标各不相同 → 没有叠在一起；点「操作指引」真的弹出指引全文（含「复制到剪贴板」「打开所在文件夹」） | ✅ |
| **C6c 原点核对** | 造两个真方案（真图训练真 PMA）：原点没设的 → `OriginUnset=true, Ok=false`，提示里写明「**图像左上角**」和「**不报错**」；原点设成 (630,530) 的 → `Ok=true` | ✅ |
| **C6d 强制核对** | 原点没设时点「计算」/「保存」→ **都拒绝**，原因提到图像左上角，**且没有写出 calib.json** | ✅ |
| **C6 完整流程** | 原点设好后「计算」→ 可用；「保存」→ 写出 9 个点 + 抓取几何；像素(640,480) → mm(63.8696, 42.8699) | ✅ |
| **几何/状态** | 改 Z/范围框 → 写进 `RobotArmSettings.Shared`；状态标签显示「可用 0.1923mm/px·RMSE 0.000（9 对点，标定于 …）」；拟合标签有 RMSE/旋转/手性 | ✅ |
| **N1 #5** | 主运行页状态行显示「● 库 已连｜**标定 无**｜阈值 0.70｜方案 未加载｜工具 未加载」（真 GUI 截图确认） | ✅ |

**自测中发现并改掉的问题**

1. **探针一开始拿不到控件**：`DeviceDebugView` 在**没被选中的 TabPage** 里，句柄没创建 →
   `OnLoad` 没跑 → `InitCalibPanel` 没执行。这是探针的问题，但**顺带暴露了一个真问题**：
   不切到设备调试页，标定就永远不会被载入。于是给 `MainRunView.OnLoad` 加了 `EnsureLoaded` ——
   主运行页是第一页、必定显示，这样"标定"状态在开机就一定是真的。
2. **拟合指标标签会漏刷**：`RefreshCalibMetrics()` 原来只在按钮事件里调，任何绕过按钮的路径都会让它变成
   `拟合：－`（探针直接调 `TryCalibrateFromGrid` 就复现了）。改成由 `Try*FromGrid` 自己刷。
3. **指引弹窗不该关自动换行**：第一版为"表格别被拆"关了 `WordWrap`，真 GUI 一看整篇变成一条长龙、
   要横向拖着读。改成**开着**（指引是给人从头读到尾的）——这条只有真点开看才发现得了。

**编译**：Debug x64 **0 error / 0 warning**。

**这一步没做**（第 4 步）：`RobotPickPlace` 换算 + 范围校验、`ProductionRunner` 传像素、
`VisionScheme` 出像素、视觉配置页的原点检查与显示（N1 #1/#2/#4）。

### 11.4 第 4 步：接进分拣 + 源头防呆（**已完成并验证**）

**改了什么**（5 个文件）

| 文件 | 内容 |
|---|---|
| `Services\VisionScheme.cs`（改） | `InspectionResult` 加 `HasPixel/PixelX/PixelY`；`Inspect` 取**最高分那条**的 `GetPose()`；`CheckPatternOrigin` 抽出静态版 `CheckOrigin(tool)`；新增 `TrySetOriginToRegionCenter(tool)` |
| `Services\RobotPickPlace.cs`（改） | `ExecuteAsync(hasPixel, pixelX, pixelY, token)`：**像素→mm** → **工作范围校验** → 取料/吸/放料/放/回待机；拒动时返回中文原因（Q5甲） |
| `Services\ProductionRunner.cs`（改，1 处） | `Sorting` 阶段把 `inspection.HasPixel/PixelX/PixelY` 交给分拣 |
| `Services\CalibrationService.cs`（改，3 行） | `PixelToRobot` 里先兜一次 `EnsureLoaded`（防调用方忘加载而误报"还没标定"） |
| `Views\VisionConfigView.Pma.cs`（改，+~55 行） | N1 #1/#2：训练完**当场查模板原点**并弹窗（可一键设到区域中心）；`lblTplScore` 在原点没设时显示 `⚠ 模板原点(0,0)` |

**验证结果：探针 23 项断言全过（`过 23 / 失败 0`）**，用的是**假桥接进程**（回环 UDP 上说同一份线协议，
记下收到的每条 `moveJ`/`suction`）。探针跑完即删。

| 用例 | 验了什么 | 结果 |
|---|---|---|
| **C7b 真换算** | 标定好（合成 9 点）后分拣一次，假桥接收到的 3 条 moveJ 是：`1. 63.8696,42.8699,-50`（**换算出来的取料点**，＝像素(640,480) 换算值）`2. 200,-150,-50`（放料点）`3. 200,0,-50`（回待机位）；吸盘 `on/off` 各一次 | ✅ |
| **C7b 反证** | 断言换算点 ≠ 示教点 `PickX/PickY` —— 否则"测了等于没测" | ✅ |
| **C7a 没标定** | 拒动，原因提到「标定」，**一条 moveJ 都没发** | ✅ |
| **C5 越界** | 把范围收窄到 X 0~10 → 拒动，原因列出"算出来的点 / 允许范围 / 先查什么"，**一条 moveJ 都没发** | ✅ |
| **C7c 没像素** | `hasPixel=false`（PMA 一个结果都没有）→ 拒动，**没发 moveJ** | ✅ |
| **C7d 镜像** | 故意把机械臂 X 取反 → 标定本身就被拒 → 分拣也拒，**没发 moveJ**，原因「当前标定是镜像的（手性 ✗）」 | ✅ |
| **N1 #5 文案** | `Describe()` 三种状态：正常 `0.1923mm/px·RMSE 0.000` ／ 镜像 `镜像 ✗（不可用）` ／ 没标定 `无` | ✅ |

**这一步没验到的（说清楚，别当成验过了）**

- **视觉配置页那个弹窗没做 GUI 点击验证**：它调用的是 `VisionScheme.CheckOrigin(tool)`，
  与 C6c 验过的是**同一个方法**；但"训练完弹出来、点『是』把原点设到区域中心"这条交互路径
  没有真的点过（需要先采集训练图）。**现场第一次训练模板时留意一下**。
- **`ProductionRunner` 那一句传参**只做了编译验证 + 代码走查，没有跑整条生产循环（那要真 VPP 判出 NG）。
  `RobotPickPlace` 本身已经用假桥接端到端验过了。

**编译**：Debug x64 与 Release x64 均 **0 error / 0 warning**。

### 11.5 复查：「自测抓出来的问题改了没」（**19 项断言全过**）

第 3 步报告了三个问题。**其中第 1 个我只改了代码、没验正向路径** —— 补验如下（探针跑完即删）。

| # | 问题 | 改法 | 这次怎么验的 | 结果 |
|---|---|---|---|---|
| **1** | 不切到设备调试页，标定就永远不载入 → 主运行页那句「标定」会撒谎 | `MainRunView.OnLoad` 里 `EnsureLoaded`（它是第一页，必定显示） | **正向路径**：磁盘上放一份**真标定** → 新开窗（只有主运行页会被创建）→ 读 `lblSysReady.Text`。实测 `● 库 …｜**标定 0.1923mm/px·RMSE 0.000**｜阈值 …`；并断言**设备调试页的表格字段还是 null**（证明这次读取不是它干的）；再断言换算立刻可用（像素(640,480)→mm(63.8696,42.8699)） | ✅ |
| **2** | 拟合指标标签会漏刷 | `RefreshCalibMetrics()` 移进 `Try*FromGrid` | 第 3 步探针里的断言「拟合标签有 RMSE」——**改前 FAIL、改后 PASS** | ✅（已验证过） |
| **3** | 指引弹窗关了自动换行 → 整篇成一条长龙 | `WordWrap = true` | 探针断言 `WordWrap==true`；**并且这次真点开看了**：全文从上到下可读，不用横向拖 | ✅ |

**顺带把两条"上次说没验到"的补上了**

| 补验 | 怎么验的 | 结果 |
|---|---|---|
| **A. 真 PMA 的像素坐标有没有传进 `InspectionResult`**（生产那一句的数据源） | 真方案（真图训练的真 PMA）+ 真图跑 `VisionScheme.Inspect` → `HasPixel=True`，`Pixel=(629.98, 529.99)`（模板原点 (630,530)，±1px），并断言这个像素能换算成 mm | ✅ |
| **B. 视觉配置页的原点提示（N1 #2 常显那一行）** | 造 `VisionConfigView`，用真 PMA 调 `RefreshPatternOriginHint`：原点 (0,0) → 标签变 `⚠ 模板原点(0,0)` + 红色；原点设回 (630,530) → 标签恢复 `验证分数：--` | ✅ |

**仍然没验到的（就剩这一条）**

- 视觉配置页那个 **Yes/No 弹窗**里"点『是』一键把原点设到区域中心"的交互没真点过（要先采集训练图）。
  它调用的 `VisionScheme.TrySetOriginToRegionCenter` 是独立小函数，逻辑简单；
  **现场第一次训练模板时留意一下**，出问题我改。

**复查中踩到并记一笔的环境坑（不是程序 bug，但差点让我误判）**

补验探针里主运行页状态行显示成「库 **连不上**」，而真 GUI 明明是「库 已连」。查下来是：
探针 exe（`_fixcheck.exe`）**没有自己的 .config 文件**，MySqlConnector 缺绑定重定向 →
`“MySqlConnector.Core.ConnectionPool”的类型初始值设定项引发异常`。
把 `VisionSort.exe.config` 复制成 `<探针>.exe.config` 再跑，立刻变成「连接成功 / 8.0.44 / vision_sort」。
→ **同一个教训（`数据库方案.md` §二十已记过一次）：探针环境 ≠ 应用环境，探针报的错先怀疑探针。**
（这条不影响上面任何结论：标定那半边是纯 VisionPro + 文件 IO，与 MySqlConnector 无关。）

**编译**：Debug x64 与 Release x64 均 **0 error / 0 warning**；探针全部删除，`bin\config` 只剩 `sysparam.json`。

### 11.6 复验（2026-09-20）：模板训练 + 真像素驱动机械臂 + 又抓到一个真 bug

**起因**：你问了一句"PMA 模板未训练，不是可以在 PMA 模板训练里面训练吗" —— **问得对，是我把措辞说重了**。
查清后的结论：**"PMA 未训练"不是缺功能，是"这一步没人拍板做"**。

- 视觉配置页『采集训练图』本来就有**三级退化**：相机层注入 `TrainingImageProvider` → 方案自带的取像工具
  → **本地图片文件**（`VisionConfigView.Pma.cs:377-421`）。**所以没相机也能训**。
- `主运行页方案.md §17.4 A` 的原话就是"**待你决定**：要不要我在 `_tmp` 里复制一份 vpp 训一个模板"
  —— 这个决定一直没做，而我在后面几轮里一路把它当成"前置条件"来表述。**表述的问题，不是项目的问题。**
- 另外查到一个容易踩的事实：`bin\vpps\QuickBuild1.vpp`（1.0MB，9-15）与根目录
  `QuickBuild1.vpp`（14.5MB，9-9）**不是同一个文件**，csproj 也没有拷贝规则 ——
  **bin 这份是唯一副本**，就地训练没有源可还原（要训必须先备份）。

**探针证据（14 项断言全过，跑完即删；训练做在副本上，源 vpp 复核 `Trained=False`、大小未变）**

| 验了什么 | 结果 |
|---|---|
| 无相机训练（真图 `齿轮缺齿检测\2.bmp` + 区域 (500,400,260,260)） | `Trained=False → True` ✅ |
| 训练完原点确实是 `(0,0)`（那个坑） | ✅ 实测复现 |
| `TrySetOriginToRegionCenter` 一键设原点 | 设到 (630.0, 530.0) ✅ |
| **判 OK 分支**（`§17.4 A` 一直没验过的那条） | 生产阈值 0.70 → `Score=0.9601 → Accepted=True` ✅ |
| 判 NG 分支 | 阈值 0.999 → `Score=0.9601 → Accepted=False` ✅ |
| 无图时不崩 | `Ran=False, HasPixel=False` ✅ |
| **真 PMA 像素 → 九点标定 → 机械臂 mm（全链）** | 真像素 (629.99,529.99) → mm(62.4498,52.5703) → 假桥接收到的第 1 条 moveJ ＝ `62.4498,52.5703,-50.0000` ✅ |

> 最后一条把第 4 步的验证从"合成像素"提升成了"**真图 + 真 PMA + 标定 + 机械臂**"的整链。

**★ 探针又抓到一个真 bug（我自己在第 4 步引入的），已修**

现象：第 3 段里先 `Calibrate()` 算好了标定（内存里，**还没保存**），紧接着 `PixelToRobot` 却回
"还没标定（`config\calib.json` 不存在或没算过）"，换算全挂。

根因：第 4 步我给 `PixelToRobot` 加了"顺手 `EnsureLoaded()`"的兜底，而 `EnsureLoaded` 只看 `_loaded` 标志；
`_loaded` 还是 false 时它会调 `Load()`，而 `Load()` 的语义是"**清掉内存里的点对、按磁盘重读**" ——
磁盘上没有文件，于是**把刚算好的内存标定抹掉了**。

修法：`EnsureLoaded` 改成"**只在内存里什么都没有时才去读磁盘**"
（`_loaded || _calibrated || _pairs.Count > 0` 就直接返回）。

**为什么这个 bug 很难在界面上碰到、但必须修**：正常流程里 `MainRunView.OnLoad` 开机就把 `_loaded` 置上了，
所以点界面几乎撞不到；但"几乎撞不到"不等于"不会撞到"，而且它抹掉的是**操作工刚点完『计算』、
还没来得及点『保存』**的那份结果 —— 撞上一次就是一小时的排查。

**编译**：Debug x64 与 Release x64 均 **0 error / 0 warning**；探针全部删除。

### 11.7 真文件训练（2026-09-20，"训呗"）

`bin\vpps\QuickBuild1.vpp` **已训练好**（原件备份 `QuickBuild1.vpp.untrained.bak`）。

**怎么训的**：走**程序自己的训练路径**，不是我在探针里另写一套 ——
`PmaTemplateTrainer`：`SetInputImage(2.bmp)` → `ApplyTrainRegion(region, resetOrigin: true)` → `ApplyRunParams(默认)` → `Train()`
（＝视觉配置页『采集训练图』→『训练模板』那两个按钮背后的同一条链）。

| 项 | 值 |
|---|---|
| 训练图 | `20260908-素材\齿轮缺齿检测\2.bmp`（1280×1024 8 位灰度） |
| 训练区域 | 中心 **(630,530)**、300×300（**没用**默认的 `FitToImage(0.8)` —— 那等于拿整张图当模板，背景也进去、没有区分度；人在这里的动作就是"把框拖到工件上"） |
| 模式原点 | **(630, 530)**（＝区域中心；`resetOrigin:true` 自动设的） |
| 匹配阈值 | 0.70（`PmaTrainSettings` 默认，与生产判据同值） |
| 训练结果 | `Trained=True`，匹配数 1，接受数 1，最好分数 **0.9401** |
| 文件 | 1008991 → **2323473 字节**；重新从磁盘读回复核 `Trained=True / 原点 (630.0,530.0) / 阈值 0.70` |
| 生产路径三张样例图 | `1.bmp 0.9334` / `2.bmp 0.9401` / `3.bmp 0.9401` —— **全部 `Accepted=True`（阈值 0.70）**，且都给出了像素坐标 |

**★ 训练过程中实测到一个容易差几个毫米的细节（已写进操作指引第三节）**

```
模式原点（训练时设的） = (630.0, 530.0)      ← 区域中心
实际跑出来 PMA 报的点  = (620.2, 508.9)      ← 差 ~20 像素 ≈ 4mm（按 0.19mm/px）
三张图报的点几乎一样（620.0/508.7、620.2/508.9、620.2/508.9）→ 是**系统性偏移**，不是随机抖动
```

系统性偏移本身不可怕（标定会把它吸收掉，因为标定配的是"PMA 报的点 ↔ 机械臂位置"）。
**但它带来一条必须写进操作指引的实践结论**：
**标定时点的那九个像素，要以「PMA 报出来的位置」为准，不要以眼睛看着的工件中心为准**
—— 否则就是 4mm 的系统误差，而 RMSE 照样很小、手性照样 ✓，程序不会报任何错。
（指引第三节已补上这条，并给了实测数字。）

**嵌在程序里的那份指引同步更新并复核**：`九点标定操作指引.md` 与嵌入资源**逐字节一致**（3430 = 3430，含新增那段）。

**编译**：Debug x64 与 Release x64 均 **0 error / 0 warning**；探针全部删除。

---

## 十二、四步做完后的总账

| 阶段 | 状态 |
|---|---|
| 方案 v1.0（推荐自动采点） | ✅ 已被 v1.1 取代 |
| 方案 v1.1（人工在 VisionPro 标定 + 输入框交接） | ✅ 已被 v1.2 取代 |
| 方案 v1.2（+ N1 防呆 + N2 操作指引） | ✅ 已确认并全部落盘 |
| Q4 输入框装什么数据 | ✅ **标定数据：9 对点（像素 ↔ 机械臂 mm）** |
| **第 1 步 地基 `CalibrationService`** | ✅ **探针 51/51** |
| **第 2 步 操作指引落盘（N2）** | ✅ **探针 24/24（含嵌入资源逐字节比对）** |
| **第 3 步 输入区 + 防呆（N1 #3/#5）** | ✅ **探针 48/48 + 真 GUI 走查** |
| **第 4 步 接进分拣 + 源头防呆（N1 #1/#2/#4）** | ✅ **探针 23/23（假桥接端到端）** |
| **复查：三个问题改了没** | ✅ **探针 19/19**（问题 1 补了正向路径、问题 3 真点开看了；顺带补验两条"上次没验到"的） |
| **复验：模板可训 + 判 OK 分支 + 真像素全链** | ✅ **探针 14/14**；并修掉一个真 bug（`EnsureLoaded` 会抹掉内存里未保存的标定，见 §11.6） |
| **真文件训练**（`bin\vpps\QuickBuild1.vpp`） | ✅ **已训练**（§11.7）：原点 (630,530)、三张样例图全部 `Accepted=True`；原件备份 `.untrained.bak`；训练中实测的"PMA 报的点 ≠ 区域中心（差 20px≈4mm）"已写进操作指引 |
| 四步合计 | **146 + 19 + 14 项断言全过；Debug/Release 均 0 error / 0 warning；探针全部清理** |
| **全流程整机测试（真图+真库+假从站+假桥接）** | ✅ **29/29 过**：判 OK 轮不动机械臂、判 NG 轮机械臂按 `像素(620.2,508.9) → mm(60.36,48.61)` 去抓、连续+急停+复位都对。**并在这一轮挖出一个 P0（`AcceptThreshold` 会把结果滤掉 → 分拣从来没工作过）**，已修，详见 `主运行页方案.md` §二十一 |
| 唯一还没验的 | ⬜ 视觉配置页那个 **Yes/No 弹窗里点『是』一键设原点**（逻辑函数已在真 PMA 上验证过，只剩"点那个按钮"没点过） |

**现场第一次上手，按这个顺序**（详见 `九点标定操作指引.md`）：

1. 视觉配置页：采集训练图 → 训练模板 → **看有没有弹「模板原点没设」** → 保存方案
2. 设备调试页：连机械臂 → 在 VisionPro 里点九个点 → 填表 → 「核对 PMA 原点」→「计算」→「保存」
3. 按指引第五节做一次**反查验证**（目标 < 0.5mm）
4. 主运行页：状态行看「标定 0.1923mm/px·RMSE …」不是「无」，再开始检测
| 前置：训练生产 PMA 模板 + 核对原点（§3.3/§3.4） | ⬜ 未完成（`主运行页方案.md §17.4 (A)` 同一个坑） |
