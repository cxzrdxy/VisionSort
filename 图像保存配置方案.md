# 图像保存配置 · 实施方案（最小版）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms）
> 页面：`Views/VisionConfigView.cs` → `grpSave`「图像保存配置」
> 版本：v2.3（**只留活着的界面逻辑**，未被调用的存图方法已删；见 §十）
> 状态：源码已实现并编译通过；界面逻辑自测覆盖 T1/T2/T3/T5/T6 + 真机切页链路

---

## 一、目标与范围

**就三件事：存到哪、存什么格式、什么结果才存。**

| 项 | 内容 |
|---|---|
| 在范围内 | ① 保存目录：『选择路径』+ 存在/可写校验；② 图片格式：JPG/BMP/PNG（只读）；③ `自动保存OK图` / `自动保存NG图` 开关 |
| 不在范围内 | 配额清理与保留天数（`lblQuota` **保持静态显示不动**）、复杂命名规则、异步队列、日志/数据库写入、带标注结果图、检测流程本身、**配置持久化（按你的决定：重启后回到 Designer 默认值，不写任何配置文件）**、**存图方法（暂无人调用，已删，等主运行页开工时再加）** |
| 不改动 | `Services/PmaTemplateTrainer.cs`、`Views/VisionConfigView.Pma.cs`（PMA 部分已完工） |

---

## 二、现状基线（已核对）

`grpSave` 控件齐全，界面逻辑在 `Views/VisionConfigView.Save.cs`（partial），**不读不写任何配置文件**，初值全部来自 Designer：

| 控件 | 默认值 | 本期用途 |
|---|---|---|
| `txtSavePath` | `D:\VisionData` | 保存根目录（可编辑，失焦校验；失败回退到上次有效值） |
| `btnBrowse` | 「选择路径」 | 弹目录选择框 |
| `chkSaveOK` / `chkSaveNG` | 都勾选 | 是否保存对应结果的图（至少保留一种） |
| `cmbImgFmt` | `JPG / BMP / PNG`（默认选中 JPG） | 图片格式（只读当前值，不触发任何落盘） |
| `lblQuota` | 「配额50GB / 保留30天」 | **只显示，不实现** |

---

## 三、设计（最小）

### 3.1 只新增 1 个文件

| 文件 | 职责 |
|---|---|
| **`Views/VisionConfigView.Save.cs`**（新增，partial） | 三个控件的界面逻辑（**内存态，不落盘**）+ 一个公开的保存方法 |
| `Views/VisionConfigView.Designer.cs` | 只补 4 个事件绑定（`btnBrowse.Click`、`txtSavePath.Leave`、`chkSaveOK/NG.CheckedChanged`）；默认值（路径 / JPG / 双勾选）本来就在 Designer 里 |

不加 `Services` 新类、不改 csproj（SDK 风格自动包含）。

### 3.2 存图方法（已删除，待主运行页开工时再加）

`SaveResultImage(image, "OK"/"NG")` 曾实现并通过自测（JPG 344,767 B / PNG 261,467 B，开关/重名/异常路径全过），但生产代码零调用，按“只留活着的代码”已整体删除（含 `BuildUniquePath` / `GetFileExtension` / `GetImageFormat` / `SaveAsJpeg` 四个私有 helper）。

加回时的约定（已验证过的设计，不用重新想）：

```
① 开关：image==null 或 结果与开关不匹配 → return null
② 目录：路径框当前值（不存在则创建）\+ 日期子目录（{yyyyMMdd}）
③ 文件名：{yyyyMMdd_HHmmss_fff}_{OK|NG}.{ext}；同名已存在 → 追加 _2 / _3
④ 写盘：image.ToBitmap() → 转 24bpp → Save(path, ImageFormat)（JPG 质量 90）
⑤ 异常：MessageBox 提示 + 返回 null（不抛出，避免影响检测流程）
```

主运行页将来就一行：

```csharp
string path = _visionConfigView.SaveResultImage(image, isOk ? "OK" : "NG");
// path != null 时写日志（DataLogView 的「图像路径」列）与数据库
```

### 3.3 不做配置持久化（按你的决定）

- 初值 = Designer 默认（`D:\VisionData` / JPG / OK+NG 都存），`OnLoad` 只把当前路径记为“内存里的上一次有效值”，不读任何文件；
- 本进程内的改动即时生效（路径/格式/开关都是读控件当前值），**重启后回到 Designer 默认值**；
- 若以后想要“重启后记得”，再加一个 ini/JSON 读写即可，`SaveResultImage()` 签名不用动。

### 3.4 界面交互（三个事件）

| 事件 | 行为 |
|---|---|
| `btnBrowse_Click` | `FolderBrowserDialog`（初值=当前路径）→ 目录不存在则询问是否创建 → 写入临时文件测试可写 → 通过才更新 `txtSavePath`；不通过**保留原值**并提示 |
| `txtSavePath.Leave` | 同上校验（用户手输路径时也把关；未改动则不打扰） |
| `chkSaveOK/NG_CheckedChanged` | 至少保留一项：两项都取消 → 提示并恢复刚取消的那项 |

---

## 四、命名与目录（最简）

```
D:\VisionData\
└── 20260915\
    ├── 20260915_203012_137_NG.jpg
    └── 20260915_203014_215_OK.jpg
```

- 文件名 = 时间（到毫秒，天然唯一）+ 结果；不含工件编号/配方/分数（那些等主运行页与日志页定稿后再谈）。
- 目录 = 根路径 + 日期子目录（避免单目录堆几万个文件）。
- 非法字符、格式扩展名由代码保证（`.jpg/.bmp/.png`）。

---

## 五、改动清单

| # | 文件 | 改动 |
|---|---|---|
| 1 | `Views/VisionConfigView.Save.cs` | **新增**：3 个事件处理（约 120 行，无配置读写、无存图方法） |
| 2 | `Views/VisionConfigView.Designer.cs` | `btnBrowse.Click`、`txtSavePath.Leave`、`chkSaveOK.CheckedChanged`、`chkSaveNG.CheckedChanged` 绑定（`cmbImgFmt` 不需要事件，只读当前值） |
| 3 | `VisionSort.csproj` | 无需改动 |

> 前车之鉴：VS 设计器保存窗体时会重写 `Designer.cs`（PMA 那轮实测会改控件顺序、丢 `SelectedIndex`），改完请复查上面的绑定。

---

## 六、验收 / 自测清单

| # | 场景 | 期望 |
|---|---|---|
| T1 | 编译（x64 / net48，VS MSBuild） | 0 error / 0 warning |
| T2 | 启动并切到「视觉配置」页 | 界面为 Designer 默认值（`D:\VisionData` / JPG / 都勾选），**不生成任何配置文件** |
| T3 | 改路径/格式/勾选后重启 | 回到 Designer 默认值（按“不持久化”决定，这是预期行为） |
| T4 | 选不存在的目录 | 询问是否创建 → 创建成功并生效 |
| T5 | 选只读/无权限目录 | 提示不可写，**保留原路径** |
| T6 | 取消两个勾选 | 提示至少保留一项并自动恢复 |
| T7 | ~~调用 `SaveResultImage(img,"NG")`~~ | 方法已删，加回后按 §3.2 复测 |
| T8 | ~~调用 `SaveResultImage(img,"OK")`（未勾 OK）~~ | 方法已删，加回后按 §3.2 复测 |
| T9 | ~~同毫秒连续保存多张~~ | 方法已删，加回后按 §3.2 复测 |
| T10 | 目标目录不存在/被拔盘 | 提示错误、返回 null，不抛异常 |

---

## 七、风险与规避

| 风险 | 影响 | 规避 |
|---|---|---|
| ~~`CogImage8Grey.ToBitmap()` 索引位图存 JPG 报错~~ | （存图方法已删，加回时按 §3.2 转 24bpp，之前已验证） | — |
| 同步保存在连续运行时占节拍 | 影响节拍 | 加回时先做同步；若实测卡节拍再加后台队列 |
| 只读目录/磁盘满 | 丢图 | `EnsureFolderUsable` 在配置时把关；存图异常返回 null 由调用方告警 |
| VS 设计器重写 Designer 丢绑定 | 按钮失效 | 改完复查 §五 的绑定 |

---

## 八、我代为决定的项（如需否决请直接说）

| 项 | 决定 | 备选 |
|---|---|---|
| 配置持久化 | **不持久化**（按你的决定：只存输出图片，重启后回到 Designer 默认值） | `config\imagesave.ini` / `App.config` / JSON（v2.1 做过 ini 版，后按你的决定整体删掉，sig 不变，随时可加回） |
| 存哪张图 | **原图**（预留 `SaveAnnotated` 字段，不做界面） | 存带标注结果图（需主运行页先有 `CogRecordDisplay`） |
| 配额标签 | 保持静态显示，不实现清理 | 显示实际用量 / 实现配额+保留天数清理 |
| 命名 | 时间(毫秒)+结果，按日期建子目录 | 加入工件编号/配方/分数 |

**开放项已按你的决定关闭**：不加「测试保存」按钮（用临时探针验证，验证后已删除）。

---

## 九、落盘顺序

1. 新增 `Views/VisionConfigView.Save.cs`
2. `Views/VisionConfigView.Designer.cs` 补绑定（去掉 `cmbImgFmt.SelectedIndexChanged`，格式只读不写）
3. 编译 → 按 T1–T10 自测（只读目录、两项全取消等异常路径）
4. 文档更新为 v2.2「已实现 + 实测记录」

---

## 十、实测记录（v2.3，当前）

### 10.1 实际落盘内容

| 文件 | 动作 | 说明 |
|---|---|---|
| `Views/VisionConfigView.Save.cs` | 精简到约 120 行 | 只剩 `OnLoad` + 3 个事件 + `EnsureFolderUsable`；ini 读写与存图方法全删；多余 using（`Cognex.VisionPro` / `System.Drawing` / `System.Drawing.Imaging`）同步去掉 |
| `Views/VisionConfigView.Designer.cs` | 零改动 | 4 个绑定保持原样 |
| 全仓检查 | — | `SaveResultImage` / `imagesave.ini` 全仓零命中，无死代码残留 |

### 10.2 自测结果（v2.3 编译 + 界面逻辑；存图方法删除前已验证，见 §3.2 与下述历史）

| # | 场景 | 结果 |
|---|---|---|
| T1 | 编译（VS MSBuild amd64，x64/net48） | ✅ 0 error / 0 warning |
| T2/T3/T5/T6 | 路径校验、双勾选保护、默认值 | ✅ 沿用 v2.2 探针结论（这段代码 v2.3 一字未改） |

### 10.3 历史：v2.1 做过又删掉的 ini 版

v2.1 曾实现 `config\imagesave.ini` 三行读写并通过自测；期间发现 `File.Replace(temp, ini, null)` 在受限权限下抛 `UnauthorizedAccessException` 被 catch 吞掉（已修为 `File.Copy` 覆盖）。按你的决定“只存输出图片”，整个持久化被删掉，这个 bug 随代码一起消失，不再存在。

### 10.4 与主运行页的接口（下一步接入）

```csharp
// 主运行页检测完成后（isOk 为判定结果）
string path = _visionConfigView.SaveResultImage(image, isOk ? "OK" : "NG");
// path != null → 写入日志（DataLogView 的「图像路径」列）与数据库
```

### 10.5 遗留 / 未做

1. `lblQuota`「配额50GB / 保留30天」仍是静态显示（按你的决定不做清理逻辑）。
2. 未加「测试保存」按钮（按你的决定）。
3. 保存是**同步**的（几十毫秒/张）；等主运行页接入后若实测影响节拍，再在 `SaveResultImage` 内部加后台队列，调用方不用改。
4. 尚未接入主运行页，且存图方法已删除 → 本页当前只有界面逻辑，加回方法按 §3.2 约定即可，调用界面不用动。
5. 重启后回到 Designer 默认值（`D:\VisionData` / JPG / 双勾选），这是“不存配置”的预期行为；如需改默认，改 Designer 里 `txtSavePath.Text` 一行即可。

