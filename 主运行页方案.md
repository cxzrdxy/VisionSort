# 主运行页 · 实施方案（v1.1：**主流程已落盘，自测部分通过**）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms + VisionPro 9.0 CR2）
> 页面：`Views/MainRunView.cs` + `MainRunView.Designer.cs`（453 行布局已在）；对应草稿 `04-资料/01主运行页.png`
> 决策：**Q1–Q13 全部按推荐 A** 定稿（你："干吧，按你的默认来吧"）。
> 状态：共享层 + 状态机 + 界面**已落盘，编译 0 error / 0 warning**；整机探针 15 项过 12、聚焦探针 8 项过 7。
> **未完成与未验证的清单在 §十七.4，务必先看那一节**（其中 A 项会直接影响"能不能判 OK"）。

---

## 一、现状基线（已核对）

| 事实 | 证据 |
|---|---|
| 主运行页**只有布局没有逻辑** | `MainRunView.cs` 16 行（仅 `InitializeComponent()`），无 `OnLoad`、无事件 |
| 控件已按草稿摆好，**名字可以直接用** | `splitMain`（左预览/右控制）+ `grpStats`；`grpPreview` 里有 `pnlDisplay`（**Panel 占位**）+ `lblDisplayTip` + `lblFlow`（状态链文案） |
| 运行控制组 5 个按钮 + 3 盏灯 + 2 盏状态灯 | `btnStart` 开始检测 / `btnStop` 停止检测 / `btnSingle` 单次运行 / `btnCont` 连续运行 / `btnEstop` 急停（Crimson）/ `btnReset` 报警复位；`btnShot`「拍照中」/`btnOK`「检测OK」/`btnNG`「检测NG」；`lblSysReady`「● 系统就绪」/`lblTrigger`「● 光电触发」 |
| ⚠ `btnShot/btnOK/btnNG` **是灯不是按钮** | 草稿里它们是状态指示（而且 `btnShot.Enabled = false` 是初始态）。本期按"灯"实现，不接 `Click` |
| ⚠ `cmbRecipe` 标签是「工件编号」，但**是 `DropDownList`**、两项写死 `A001-通用件` / `A002-缺料检测`（像型号/配方，不像单件编号） | `MainRunView.Designer.cs:204-216`。**需要你定映射**（Q7/Q8） |
| 统计四个标签 | `lblTotal`「总检测数量：0」/`lblOKCount`「合格数量：0」/`lblNGCount`「瑕疵数量：0」/`lblRate`「合格率：0.00%」 |
| **相机对象拿不到** | `_camFifo` 是 `VisionConfigView` 的**私有字段**（`VisionConfigView.Camera.cs:59`）；取像统一走 `CogAcqFifoTool.Run()` + `.OutputImage` + `.RunStatus`（同文件 405–445 行 `TryRunOnce`） |
| **生产触发契约已经定了** | `视觉参数配置方案.md` §三：光电硬触发（`TriggerModel=Auto` + `TriggerEnabled=true`），软件侧"触发发生了"＝**相机有新帧到达**；时序＝新帧 →(停止延时)→ 发停带 →(拍照等待)→ 检测 → 机械臂分拣 → 恢复传送带 |
| 现场事实（重要，且有第二种解释） | 相机那轮探测时"只有 Manual 出图"。但 `ProbeOneFrame` 是**在没人挡光电的情况下**试的——若现场相机本来就是 Auto 等硬触发，那么"Manual 才有图"恰好是**预期现象**。两种解释必须在现场用手挡一下光电来区分（§八） |
| 视觉参数共享对象已就绪 | `VisionRunSettings.Shared`：`MatchThreshold / StopDelayMs / PhotoWaitMs / DetectTimeoutMs` |
| 传送带共享对象已就绪 | `ConveyorSettings.Shared`：`ForwardAsync / ReverseAsync / StopAsync / SetSpeedAsync / ReadRunStateAsync`，内部 `SemaphoreSlim` 串行化（并发踩过坑） |
| 机械臂 API 已就绪 | `DobotArm.DobotArmClient`：`ConnectAsync / HomeAsync / ClearAlarmsAsync / SuctionAsync(bool) / MoveJAsync(x,y,z,r) / WaitArriveAsync(x,y,z) / JogAsync / MoveRelativeAsync` + `ConnectionLost` 事件 |
| 调试页的动作序列可复用（但要抽出来） | `DeviceDebugView.Robot.cs:251` `MoveToAsync` = `MoveJAsync` + `WaitArriveAsync`；队列/`PickDelay` 都在 View 里，主运行页拿不到 |
| **存图方法当初删掉了，现在要加回来** | `图像保存配置方案.md` §3.2：`SaveResultImage` 已删（无调用方），"待主运行页开工时再加"。路径/格式/OK-NG 开关现在只活在 `VisionConfigView` 的控件里（`txtSavePath`/`cmbFormat`/`chkSaveOK`/`chkSaveNG`），**没有共享对象、不落盘** |
| **VPP 路径只在视觉配置页自己手里** | `VisionConfigView._vppPath`（私有）+ `lblVppPath.Text`；PM A 实测用的方案是 `bin\x64\Debug\net48\vpps\QuickBuild1.vpp` |
| PMA 工具在方案里的取法已有现成实现 | `PmaTemplateTrainer.FindTool(...)` / `ListPmaTools(...)` / `CogPMAlignTool Run()`；训练图素材 `20260908-素材\齿轮缺齿检测\2.bmp` |
| 写库契约已定 | `ResultRepository.Shared.Enqueue(record)`（异步、断网补写、`uuid` 幂等）；主运行页只需填 8 个字段 |
| 本机**没有**相机 / 机械臂 / 变频器 | 有虚拟串口对（ELTIMA）+ Modbus Slave/Poll；MySQL 8 可起一次性实例（上轮做法）；这意味着自测必须靠"图片源 + 假从站 + 假机械臂"（§七） |

---

## 二、范围

**本期做**

1. 生产主循环编排（状态机）：触发 → 停带 → 检测 → 分拣 → 记录 → 恢复带 → 等下一件
2. 四种操作：开始检测 / 停止检测 / 单次运行 / 连续运行；急停 / 报警复位
3. 取像、判定（PMA）、**存图**、**写库**、会话统计、状态灯、状态链高亮
4. 图像预览区由 `Panel` 换成 `CogRecordDisplay`（照 PMA 期已验证的赋值顺序）
5. 为此必需的**共享层**：相机取像、存图设置、VPP 路径、机械臂取放料序列

**本期不做**

- 9 点标定 / 抓取坐标随视觉结果换算（本期用**示教固定点**，见 §4.5）
- 配方管理页（`recipe_id` 仍恒 0）、报警历史、生产报表、OEE/节拍分析
- 视觉工具的在线编辑（那是视觉配置页的事）
- 与 MES/上位机的通信
- 机械臂气爪（Q5=B 只做吸盘，沿用）

---

## 三、生产流程与状态机（核心）

### 3.1 状态与迁移

状态机放在 `Services\ProductionRunner.cs`，界面只订阅事件、不做判断。

| 状态 | 含义 | 可在该状态下按 |
|---|---|---|
| `Idle` | 未开始（系统就绪灯灭） | 开始检测 |
| `Preparing` | 加载 VPP、连相机、连机械臂、连传送带、应用参数 | 停止检测 |
| `WaitingTrigger` | **等工件**（Auto：阻塞等新帧；Manual：按节拍取图） | 停止 / 急停 |
| `StoppingBelt` | 收到新帧 → 等 `StopDelayMs` → 发停带 → 等 `PhotoWaitMs` | 急停 |
| `Inspecting` | 检测这张图（`CogPMAlignTool.Run()`，看门狗 `DetectTimeoutMs`） | 急停 |
| `Sorting` | NG → 机械臂取走放废料区；OK → 不动它 | 急停 |
| `Recording` | 存图（按 OK/NG 开关）→ `Enqueue` 写库 → 更新统计/灯 | 急停 |
| `Resuming` | 恢复传送带（只写 `100←1` 正转；**不写速度**，见 §4.6 / §17.6） | 急停 |
| `Stopped` | 人为停止（停下时保证带停、机械臂不在动） | 开始检测 |
| `Faulted` | 出错锁存（中文原因 + 灯红） | 报警复位（个别错要复位后重开） |
| `Estopped` | 急停锁存 | 报警复位 |

单次模式：`Preparing→…→Recording` 跑一轮回到 `Stopped`。
连续模式：跑完 `Resuming` 直接回 `WaitingTrigger`。

### 3.2 一轮时序（照 `视觉参数配置方案.md` §三 的契约）

```
            ┌─ 连续模式 ─────────────────────────────────────────────┐
            ▼                                                        │
  WaitingTrigger ──[光电硬触发相机出图 / Manual 节拍取图]──▶ 新帧到手 │
        │                                                             │
        ├─ 等 StopDelayMs（新帧到发停带的延时）                        │
        ├─ ConveyorSettings.StopAsync()      ← 停止，安全              │
        ├─ 等 PhotoWaitMs（停稳/消抖）                                 │
        ├─ Inspecting：AcceptThreshold ← VisionRunSettings.MatchThreshold
        │              PMA.Run() → Score；超 DetectTimeoutMs 判异常    │
        ├─ Recording：存图（chkSaveOK/chkSaveNG）→ Enqueue 写库 → 统计  │
        ├─ Sorting：NG → 机械臂 取料点→吸→放料点→放→回取料点；OK 跳过   │
        └─ Resuming：SetSpeed + ForwardAsync → 回 WaitingTrigger ──────┘
```

**为什么先停带再检测、却用触发帧**：`PhotoWait` 的两种读法（①图已采好、只是停稳后再判；②停稳后重拍）在视觉参数那轮就标注过，当时按 ① 定稿。现场要验的是"触发瞬间工件是否已进入视野、5ms 曝光下画面糊不糊"。**如果糊**，本方案已留好口子（Q2 乙：停带后切 Manual 重拍一张）。

### 3.3 异常矩阵（每一格都要有确定行为，不许"看情况"）

| 异常 | 检测点 | 行为 |
|---|---|---|
| 相机未连/断线 | `Preparing` 连不上；运行中取像连续失败 3 次 | 提示 + `Faulted`，**不动机械臂**、停带 |
| 取像超时（无新帧） | `WaitingTrigger` 超过取像超时 | 连续模式下**不算故障**：记一次空转继续等（可配最大空转次数）；单次模式下提示 |
| 检测超时 | `Inspecting` 超过 `DetectTimeoutMs` | 本轮记 NG？**不**——记 `Faulted` 并写一行 `note=检测超时`、`score=NULL`（不丢事件），停线 |
| 机械臂超时/断桥 | `Sorting` 抛 `TimeoutException` / `ConnectionLost` | 本轮**仍写库**（`note=机械臂异常：…`），停带并 `Faulted`（NG 件留在原位，人工处理） |
| 传送带不通 | `StoppingBelt`/`Resuming` 抛错 | `Faulted` + 提示"确认变频器/串口"；**不允许**在传送带状态未知时继续取像 |
| 写库失败 | `Enqueue` 不抛（异步）；`LastError` 非空 | **不影响节拍**，界面上"库"灯变黄 + 悬停/标签显示 `LastError`；补写成功自动恢复 |
| 存图失败 | 磁盘满/权限 | 本轮 `note=存图失败：…`，继续跑（数据比图重要），界面提示一次 |
| 急停被按下 | 任意状态 | 见 §4.7 |

---

## 四、关键设计

### 4.1 相机归属（Q1，最要紧的一条）

一台 GigE 相机同时只能被一个 FIFO 打开。现在 FIFO 在视觉配置页手里，主运行页拿不到；而两页各开一个会**互相抢相机**（现场表现为"连不上"或"取不到图"，很难查）。

| 方案 | 做法 | 代价 |
|---|---|---|
| **甲 抽 `Services\CameraService.cs` 共享单例**（推荐） | 把"枚举相机 → 选视频格式 → 建 `CogAcqFifoTool` → 触发探测 → 参数下发 → 取一帧"整套搬进服务类；相机页的 `_camFifo` 改成引用它，页面只留 UI | 动了现场验证过的相机代码（v1.4–v1.6 那 20 项自测要重跑），但**一次性消除两页抢相机**，且以后加"标定页"也不用再抄 |
| 乙 主运行页借相机页已建好的 FIFO | 视觉配置页暴露一个 `internal` 取像方法给主运行页调 | 改动最小、相机代码零风险；但"设备对象挂在 UserControl 上"这件事会一直恶心人，且视觉配置页没打开时主运行页无相机可用 |
| 丙 主运行页自己 `new CogAcqFifoTool()` | 谁先连谁占着，另一页提示"相机被占用，请先断开" | 实现最快，但两页参数（曝光/包大小/触发）会各写各的，**现场一定会出"参数被另一页改回去了"这类鬼故事** |

### 4.2 取像与触发（Q2）

取像统一走 `fifo.Run()` + `fifo.OutputImage` + 失败时读 `fifo.RunStatus`（`TryRunOnce` 已验证的三个要点：`Timeout/TimeoutEnabled` 要临时设、`Run()` 可能不抛异常也不给图、`RunStatus` 才是真话）。

| 方案 | 触发 | 时序 | 评价 |
|---|---|---|---|
| **甲 Auto 硬触发**（推荐，＝已定契约） | `TriggerModel=Auto` + `TriggerEnabled=true`，光电接相机 Line0 | 新帧到达即"工件到了" | 与草稿状态链、`视觉参数配置方案.md` §三 完全一致；现场若 Auto 能出图（见 §一 的第二种解释），这是最省事最准的 |
| 乙 Auto 触发 + 停带后 **Manual 重拍一张** | 运行中切一次触发模式 | 新帧＝到位通知 → 停带 → 切 Manual → 重拍 → 检测 | 图一定清晰（停稳后拍），代价是每轮切两次触发模式（现场那套"切触发要恢复原配置"的坑要复用），节拍慢一点 |
| 丙 纯 Manual 节拍取图，不接光电 | `TriggerModel=Manual` | 连续模式按固定间隔取图（无到位概念） | 不需要任何接线就能跑，但**没有到位判断**：传送带要一直转（那就不叫分拣线了），只适合"停带人工摆件 + 点单次"的调试场景 |

**并且做一件事：图像源抽象**（Q12）。`IImageSource { ICogImage Grab(int timeoutMs, out string reason); }`，实现两个：`CameraImageSource`（生产）与 `FileImageSource`（本地图片轮播，用于自测与现场"回放跑一遍"）。没有这个抽象，本机（无相机）**根本无法自测主流程**。

### 4.3 VPP 与判定（Q3 / Q4）

- **判定路径**：直接 `CogPMAlignTool.Run()` 后读 `Results`（`Count` / `[0].Score` / `Accepted`），**不用** `PmaTemplateTrainer.Verify()`——后者会重建训练区域、是训练后自检用的，拿来跑生产既慢又可能改状态。
- **每轮都要写阈值**：`tool.RunParams.AcceptThreshold = VisionRunSettings.Shared.MatchThreshold`（`视觉参数配置方案.md` Q2=A 的职责分离：系统设置页那份是生产判据，视觉配置页那份只管训练/自检，两者**互不写穿**）。
- **VPP 生效时机**（Q3）：
  - **甲 每次「开始检测」重新加载 VPP 并从里面取 PMA 工具**（推荐）：改了模板/方案点一次开始就生效，且不会拿着过期对象跑；代价是开始时要等 1–2 秒。
  - 乙 进页面加载一次常驻：快，但**改了 VPP 不重开就不生效**（现场最容易出的"我明明保存了怎么还是老结果"）。
  - 丙 与视觉配置页共用同一份对象：省内存，但两个页面同时持有同一 `CogJob` 并各自 Run/编辑，状态会互相污染（视觉配置页还挂着 `CogToolGroupEditV2` 编辑器）。

### 4.4 存图（Q11）

被删掉的存图能力要加回来，但**不再写进 View**，放 `Services\ImageSaver.cs`（纯函数式：给图 + 路径 + 格式 → 写文件、返回相对路径），这样主运行页与将来的"手动补存"都能用。

- 设置来源：把 `txtSavePath` / `cmbFormat` / `chkSaveOK` / `chkSaveNG` 提成 `SaveSettings.Shared`（**仍不落盘**——按你之前的决定，图像配置不存配置信息）。
- 目录与文件名：`<根>\yyyy-MM-dd\` + `工件编号_yyyyMMdd_HHmmss_fff_OK|NG.<ext>`；库里存**相对根目录的路径**（数据库方案已定）。
- 格式：沿用草稿下拉（BMP/JPG/PNG）；`CogImage.ToBitmap()` 是 8bpp 索引色，**必须转 `Format24bppRgb` 再存**（图像保存那期踩过）。

### 4.5 机械臂取放料（Q5 / Q6）

本期用**示教固定点**：`待机点(取料点)` → `吸` → `放料点` → `放` → 回`取料点`。不做标定换算（那是 9 点标定那一期的事，且 PMA 只给一个匹配分数，没有缺陷坐标可用）。

坐标从哪来（Q5）：

| 方案 | 做法 |
|---|---|
| 甲 系统设置页「机械臂配置」加 6 个框（取料 X/Y/Z、放料 X/Y/Z），落盘沿用 `sysparam.json` 的 robot 组 | 与现有 4 个字段同组同套路（失焦校验 + 即时写 Shared），改动小、可手抄调试页「当前X/Y/Z」的读数 |
| 乙 调试页加「记取料点」「记放料点」两个按钮（当前位姿一键写进 `RobotArmSettings`） | 操作最省事、不会抄错；要动调试页 Designer（那轮刚收尾） |
| 丙 甲 + 乙 都做（设置页能看能改，调试页能一键记） | 最好用，改动最多 |

顺序与延时：`RobotArmSettings` 里已有 `PickDelayMs`（吸住后等多久才抬）与 `ResetDelayMs`（放开后等多久才走），沿用；每步之间用 `WaitArriveAsync` 到位（`MotionPollMs`/`ArriveToleranceMm`/`ArriveTimeoutMs` 已在类库内）。

### 4.6 传送带

- 停带 = `StopAsync()`（**只写 `100←0`**，即停止；与调试页「停止」同语义。寄存器语义以 `传送带Modbus方案.md` §二 为准）
- 恢复 = **只发 `ForwardAsync()`（写 `100←1`）**；**不写速度**（`102`）
  —— 速度由设备调试页的调速滑杆负责设定，生产只负责起停。
  理由：停带不碰速度寄存器（100/101），速度值保持原样，**少一次写就少一个"把速度改回去"的风险**（§17.6 已记）
  > 2026-09-20 复核：全流程测试里从站实收 `写100=0 / 写100=1`，**没有写 102** —— 与本节现在的写法一致。
  > （本节以前写的是"`SetSpeedAsync(速度)` + `ForwardAsync()`"，那是**没实现过的旧说法**，已按 ② 更正。）
- **不**用反转（分拣靠机械臂，反转是调试页的事）

### 4.7 急停与报警复位（Q10）

**急停**（任一状态下都要能按，且必须"立刻就停"）：
1. 停带（写 `100←0`）——失败就忽略异常并记录（急停路径不允许被 IO 卡住）
2. 让机械臂**当前动作停下**：调 `SuctionAsync(false)`? 不——先 `JogAsync(axis,0,false)` 停 JOG、并停队列；正在 `MoveJ` 的行程**无法中断**（Dobot 文本协议没有"急停运动"以外的可用命令，桥接层也没有），所以文档要写明：**急停 = 停带 + 停队列 + 不再下发新动作 + 锁存**，机械臂当前那一小段行程会走完
3. 循环标志置停、状态置 `Estopped`、灯红
4. **不自动恢复**：必须按「报警复位」

**报警复位**：
- 甲（推荐）：解锁存 + `ClearAlarmsAsync()`（清机械臂报警）→ 回 `Stopped`；**不自动回零**（回零是大幅动作，让人在调试页确认安全后自己做）
- 乙：复位时顺带回零（省事，但机械臂会突然大范围移动，安全上不推荐）

### 4.8 统计、灯与状态链

- **统计＝本次会话累计**（`lblTotal/lblOKCount/lblNGCount/lblRate`），与日志页的**库内统计**是两套口径（数据库方案 §10.1 已写明）；「停止检测」不清零，「开始检测」时清零。
- 三盏灯：`btnShot`（取像期间亮）、`btnOK`/`btnNG`（本轮判定结果，保持到下一轮）。实现用 `FlatStyle.Flat` + `BackColor/Enabled` 切换（灭=灰、亮=绿/红/黄），**不接 Click**。
- 两盏状态灯：`lblSysReady`（就绪=绿/未就绪=灰）、`lblTrigger`（本轮触发到=亮一下）。
- `lblFlow` 状态链：在原文案里给当前步骤加 `▶` 标记（例如 `状态链：等待工件 → ▶ 拍照检测 → 机械臂分拣 → 恢复传送带`），不改布局、不加控件。

### 4.9 写库与预览

- 写库：`ResultRepository.Shared.Enqueue(new ResultRecord { ... })`，字段映射见 `数据库方案.md` §17.10；界面上给一个"库"状态提示（`LastError`/`QueuedCount`/`Pending.Count`）。
- 预览：`pnlDisplay` 换成 `CogRecordDisplay`，赋值顺序 **Image → 结果图形 → InteractiveGraphics → `Fit(true)`**（`Record` 赋值会清 `StaticGraphics`，PMA 那期踩过）。

---

## 五、新增 / 改动文件清单（预估）

| 文件 | 内容 | 预估 |
|---|---|---|
| `Services\CameraService.cs`（新，Q1=甲时） | 相机枚举/连接/断开/取一帧/参数下发/触发设置（从 `VisionConfigView.Camera.cs` 搬） | 搬运 + 精简 |
| `Services\IImageSource.cs`（新） | `CameraImageSource` / `FileImageSource`（Q12） | ~120 行 |
| `Services\ImageSaver.cs`（新） | 存图（格式转换 + 目录/命名 + 返回相对路径） | ~110 行 |
| `Services\SaveSettings.cs`（新） | 存图根目录/格式/OK·NG 开关（共享，不落盘） | ~40 行 |
| `Services\VisionScheme.cs`（新） | VPP 加载 → 取 PMA 工具 → 生产 Run 一次 → 返回分数/图形；`VppPath` 共享 | ~180 行 |
| `Services\RobotPickPlace.cs`（新） | 取放料序列（取料点→吸→放料点→放→回）× `DobotArmClient` | ~150 行 |
| `Services\ProductionRunner.cs`（新） | **状态机 + 主循环 + 异常矩阵 + 统计** | ~420 行 |
| `Views\MainRunView.cs` / `.Run.cs`（新） | 按钮接线、状态订阅、灯、统计、状态链、库状态 | ~300 行 |
| `Views\MainRunView.Designer.cs`（改） | `pnlDisplay` → `CogRecordDisplay`；按钮补 `Click` 绑定 | ±20 行 |
| `Views\VisionConfigView.Camera.cs`（改，Q1=甲） | `_camFifo` 改走 `CameraService` | 大改但机械 |
| `Views\VisionConfigView.Save.cs`（改） | 三个控件 → `SaveSettings.Shared` | ±30 行 |
| `Views\VisionConfigView.cs`（改） | 加载/另存 VPP 时写 `VisionScheme.Shared.VppPath` | +4 行 |
| `Views\SysConfigView.Robot.cs` + Designer（改，Q5） | 取放料 6 个坐标框 + 校验 + 落盘 | ±60 行 |
| `Views\DeviceDebugView.Robot.cs` + Designer（改，Q5=乙/丙） | 「记取料点」「记放料点」 | ±40 行 |

---

## 六、界面映射表（每个控件 → 行为，防漏）

| 控件 | 本期行为 |
|---|---|
| `btnStart` 开始检测 | 清零统计 → `Preparing`（加载 VPP/连相机/连机械臂/连传送带）→ `WaitingTrigger`；失败给中文原因并回 `Idle` |
| `btnStop` 停止检测 | 停循环 → 停带 → 回 `Stopped`（不退出连接，下次开始更快） |
| `btnSingle` 单次运行 | 以单次模式启动一轮（跑完停） |
| `btnCont` 连续运行 | 以连续模式启动（跑完自动等下一件） |
| `btnEstop` 急停 | §4.7：停带 + 停队列 + 锁存 + 灯红，**立即返回** |
| `btnReset` 报警复位 | 解锁存 + `ClearAlarmsAsync()` → `Stopped` |
| `cmbRecipe` 工件编号 | 每轮写进 `workpiece_no`（Q7/Q8 定它是否可编辑、是否影响 `recipe_id`） |
| `pnlDisplay` | `CogRecordDisplay`：最新图 + PMA 结果图形 |
| `lblDisplayTip` | 未连接/无图时显示原因（复用相机那套"一次说全"的思路） |
| `lblFlow` | 状态链，当前步加 `▶` |
| `btnShot` / `btnOK` / `btnNG` | 三盏灯（不接 Click） |
| `lblSysReady` / `lblTrigger` | 状态灯（就绪 / 本轮触发） |
| 四个统计标签 | 会话统计（开始检测时清零） |
| （新增，若 Q5 选乙/丙） | 调试页「记取料点 / 记放料点」 |
| （新增，若 Q11 需要） | 主运行页不需要新增控件——库状态复用 `lblDisplayTip` 或 `lblFlow` 尾部文本 |

---

## 七、自测与验收（本机无相机/机械臂/变频器，靠"真视觉 + 假设备"）

**能真跑的部分**（不用 mock）：

| 环节 | 办法 |
|---|---|
| 视觉判定 | **真 VPP**（`bin\x64\Debug\net48\vpps\QuickBuild1.vpp`）+ **真图**（`20260908-素材\齿轮缺齿检测\2.bmp`）→ 走 `VisionScheme` 跑真 PMA，验分数/OK-NG 与 `AcceptThreshold` 生效 |
| 图像源 | `FileImageSource` 轮播真图（含"故意无图"的用例） |
| 写库 | 一次性 MySQL 8（`--port=3307`，上轮做法）→ 每轮落一行，验字段/幂等 |
| 存图 | 真写盘到 `_tmp`，验命名、相对路径、24bpp 转换、OK/NG 开关 |
| 界面 | 真实 `MainRunView` + 探针驱动（对话框看门狗沿用上轮） |

**假设备**（探针，跑完即删）：

| 环节 | 办法 |
|---|---|
| 传送带 | **NModbus RTU over socket 回环**（传送带那轮用过的办法）：假从站记录收到的写寄存器顺序 → 断言"停带→恢复"真的发生了、顺序与次数对 |
| 机械臂 | **假桥接器**（v3.0 的 S1/S2 套路）：假 Dobot 服务器记录收到的命令序列 → 断言取放料五步顺序、`PickDelay/ResetDelay` 生效、超时进 `Faulted` 且 `note` 有内容 |

**用例清单（草案，落盘时按此编号）**

- S1 单次：一轮完整跑通（真 VPP → 分数 → 存图 → 写库一行 → 假机械臂走完 → 假传送带收到停/恢复）
- S2 连续：连跑 5 轮，统计 5/OK/NG/合格率正确，`WaitingTrigger` 能回到
- S3 判定：同一张图把阈值改成必 NG / 必 OK，验 OK-NG 灯与统计跟着变（**阈值只认系统设置页那份**）
- S4 取像超时：`FileImageSource` 故意"无图" → 连续模式不判故障、单次模式给提示
- S5 检测超时：把 `DetectTimeoutMs` 设成 1ms → `Faulted` + 写一行 `note=检测超时`、`score=NULL`
- S6 机械臂异常：假桥接器不回包 → 本轮**仍写库**（`note` 带原因）+ `Faulted`
- S7 传送带不通：假从站不回 → `Faulted` + 中文提示
- S8 写库断网：把连接串指到坏端口 → 节拍**不受影响**、`Pending` 增长、界面显示 `LastError`，恢复后补写
- S9 存图失败：目录设成不可写 → `note=存图失败`，流程继续
- S10 急停：运行中按 → 停带/停队列/锁存；按复位 → 回 `Stopped`；两个按钮在任意状态下都不抛异常
- S11 灯与状态链：每轮结束 `btnOK/btnNG` 颜色正确、`lblFlow` 的 `▶` 落在正确步骤
- S12 无硬件启动：相机/机械臂都不接时的提示文案（不崩、不卡、能退出）

---

## 八、现场验证清单（本机测不到的）

1. **先用手挡一下光电**，在 Auto 硬触发下看 `Run()` 是否返回一帧 —— 这一步决定 Q2 到底走甲还是乙（也顺带解释掉"只有 Manual 出图"的历史现象）。
2. 触发瞬间的图**糊不糊**（5ms 曝光、传送带当时的速度）：糊就切 Q2 乙（停稳后 Manual 重拍）。
3. `StopDelayMs`（默认 300）/`PhotoWaitMs`（默认 100）现场整定：工件不在画面里 → 调延时，**不要**靠改代码。
4. 机械臂取放料坐标示教：先用调试页 JOG 到位、抄读数（或一键记录），再验**吸得住/放得下**（`PickDelayMs`/`ResetDelayMs` 现场整定）。
5. 急停演练：按下后确认带停、机械臂停下、界面锁存；复位能回到可运行状态；**机械臂当前行程会走完**这一点现场确认可接受。
6. 节拍实测：单件从触发到恢复带的秒数，与变频器速度配合是否会让工件堆积。
7. 连续跑 100 件：统计与库内条数一致、无漏记、图像按日期分目录、无内存增长（预览图形是否累积）。
8. 数据库断网演练（数据库方案 §17.9 已列）。

---

## 九、风险与规避

| 风险 | 影响 | 规避 |
|---|---|---|
| 动相机代码（Q1=甲） | 现场已验证的取像/触发逻辑回归 | 搬运不改逻辑；重跑相机那 20 项自测；甲方案保留"相机页独立可用"（未连也可用页面按钮连） |
| Auto 触发在现场出不了图 | 主流程卡在"等工件" | 已备 Q2 乙（停带后 Manual 重拍）；且 `IImageSource` 抽象让切换只改一个类 |
| 机械臂运动中无法中断 | 急停"看起来没停" | 文档与界面写明"急停=停带+停队列+不再下发"；复位不自动回零 |
| 预览图形累积 | 长跑内存涨 | 每轮重建 `CogRecord`（或 `Record = null` 后重赋），S7 长跑用 100 轮看内存 |
| 两页同时操作设备 | 传送带/机械臂命令互相插队 | Q13：运行时置 `IsRunning`，调试页刷新时据此置灰功能按钮（共享单例已串行化，但语义上仍要互锁） |
| 阈值/路径来源混淆 | "我改了怎么不生效" | 界面把生效值显式写出来（状态链附近显示"阈值 0.70｜方案 xxx.vpp"），与视觉参数那期"实际：…"回读同思路 |
| 会话统计与库内统计对不上 | 被当成 bug | 文案区分：「本次运行」/「库内」；数据库方案 §10.1 已写明 |

---

## 十、落盘顺序（分三步，每步独立验收）

1. **共享层**（不碰主运行页逻辑）：`CameraService`（Q1=甲时）+ `IImageSource`/`FileImageSource` + `ImageSaver` + `SaveSettings` + `VisionScheme` + `RobotPickPlace` + 机器人坐标字段（Q5）→ 编译 + **回归**：相机页/视觉页/调试页原有功能自测重跑
2. **主运行页状态机与界面**：`ProductionRunner` + `MainRunView`（按钮/灯/统计/状态链/预览）→ 探针跑 S1–S7、S10–S12（真 VPP + 假设备）
3. **联动收尾**：连续模式、写库/存图接线、库状态提示、Q13 互锁 → S2/S8/S9/长跑

> 每步我都会先给你看改动清单再落盘。第 1 步做完就能确认"共享层不动坏已验证的四个页面"。

---

## 十一、需要你拍板（Q1–Q13，回 A/B/C 即可；没答的按【】执行）

| # | 问题 | 选项（**＝推荐**） |
|---|---|---|
| **Q1** | 相机归属 | **A 抽 `CameraService` 共享单例（相机页也改成用它）** ／ B 主运行页借相机页的 FIFO ／ C 主运行页自己 new 一个（互相抢相机） |
| **Q2** | 生产取像/触发 | **A Auto 硬触发（＝已定契约，光电→相机出图）** ／ B Auto 触发 + 停带后切 Manual 重拍一张 ／ C 纯 Manual 节拍取图，不接光电 |
| **Q3** | VPP 生效时机 | **A 每次「开始检测」重新加载并取 PMA 工具** ／ B 进页面加载一次常驻 ／ C 与视觉配置页共用同一份对象 |
| **Q4** | 生产判定路径 | **A 直接 `CogPMAlignTool.Run()` 读 `Results`** ／ B 复用 `PmaTemplateTrainer.Verify()` |
| **Q5** | 取放料坐标怎么来 | **A 系统设置页加 6 个坐标框（落盘沿用 robot 组）** ／ B 调试页加「记取料点/记放料点」一键记录 ／ C 两者都做 |
| **Q6** | 机械臂动作序列 | **A 固定五步：取料点→吸→放料点→放→回取料点** ／ B 界面可配顺序（复杂，本期不建议） |
| **Q7** | 工件编号 / 配方映射 | **A `cmbRecipe` 文本写进 `workpiece_no`，`recipe_id` 恒 0** ／ B 下拉项当配方号写 `recipe_id`，`workpiece_no` 另找来源 |
| **Q8** | `cmbRecipe` 是否可编辑 | **A 改成可输入（`DropDown`），保留原两项**（将来接扫码枪就喂这个框） ／ B 保持 `DropDownList` |
| **Q9** | 四个按钮的语义 | **A 开始/停止＝系统使能开关；单次/连续＝运行模式（点了就按该模式启动）** ／ B 四个都是独立启动 |
| **Q10** | 报警复位语义 | **A 解锁存 + 清机械臂报警，不回零** ／ B 复位同时回零 |
| **Q11** | 存图设置的归属 | **A 提成 `SaveSettings.Shared`（仍不落盘）+ 存图放 `Services\ImageSaver.cs`** ／ B 顺便落盘进 `sysparam.json` |
| **Q12** | 图像源抽象 | **A 做 `IImageSource`（相机 / 本地图片回放两个实现）**——没有它本机无法自测，现场也能"回放跑一遍" ／ B 只做相机，自测留现场 |
| **Q13** | 主运行页 ↔ 调试页互锁 | **A 运行时置 `IsRunning`，调试页据此把传送带/机械臂功能按钮置灰** ／ B 不做互锁，文档写明"生产时别去调试页乱点" |

---

## 十二、我代为决定的项（不否决就照此执行）

- 停带＝`StopAsync()`（写 `100←0` 停止）；恢复＝**只发 `ForwardAsync()`（写 `100←1`），不写速度**（§4.6 / §17.6）；**不用反转**
- 状态机放在 `Services\ProductionRunner.cs`；界面只订阅事件，不做判断
- 每次「开始检测」清零会话统计；「停止检测」不清零
- 三盏灯用 `BackColor/Enabled` 切换，不接 `Click`；状态链用 `▶` 标记当前步，不加控件
- 预览：`pnlDisplay` → `CogRecordDisplay`，赋值顺序 Image → 图形 → InteractiveGraphics → `Fit(true)`
- 文件名规则：`<根>\yyyy-MM-dd\工件编号_yyyyMMdd_HHmmss_fff_OK|NG.<ext>`；库里存相对路径
- 写库/存图失败**不打断节拍**，只记 `note` + 界面提示
- 取像超时在连续模式下不算故障（可配最大空转次数）；检测超时才 `Faulted`
- 急停路径上的每个 IO 调用都吞异常（急停不允许被设备卡住）
- 不新增主运行页控件（除了 `pnlDisplay` 换类型）；坐标框加在系统设置页
- 不做：标定换算、配方管理、报警历史、报表、MES 通信、气爪

---

## 附录 A：本方案依赖的既有实现（都已实测）

| 能力 | 位置 | 契约 |
|---|---|---|
| 取像（含触发探测） | `Views\VisionConfigView.Camera.cs`（`_camFifo`、`ProbeOneFrame`、`TryRunOnce`、`ApplyOrAdoptParams`） | 取一帧＝`fifo.Run()` + `.OutputImage`；失败读 `.RunStatus` |
| 视觉参数 | `Services\VisionRunSettings.cs` | `MatchThreshold / StopDelayMs / PhotoWaitMs / DetectTimeoutMs` |
| PMA | `Services\PmaTemplateTrainer.cs`（`FindTool` / `ListPmaTools` / `SetInputImage`） | 生产用 `CogPMAlignTool.Run()` + `RunParams.AcceptThreshold` |
| 传送带 | `Services\ConveyorSettings.cs` | `StopAsync`（写 `100←0` 停止）/ `SetSpeedAsync` / `ForwardAsync` / `ReadRunStateAsync` |
| 机械臂 | `DobotArm\Client\DobotArmClient.cs` | `ConnectAsync / HomeAsync / ClearAlarmsAsync / SuctionAsync / MoveJAsync / WaitArriveAsync` + `ConnectionLost` |
| 数据库 | `Services\ResultRepository.cs` | `Enqueue` + `Written/QueuedCount/Pending/LastError` |
| 训练图素材 / 方案 | `20260908-素材\齿轮缺齿检测\2.bmp` / `bin\x64\Debug\net48\vpps\QuickBuild1.vpp` | PMA 那期实测用过，自测可直接复用 |

## 附录 B：被否掉的做法（留档）

| 做法 | 否掉的理由 |
|---|---|
| 主运行页直接读视觉配置页的控件取值 | 跨页读别人的控件＝隐性耦合；设备与设置都必须走共享单例（本项目已有 4 个 `Shared` 的约定） |
| 复用 `PmaTemplateTrainer.Verify()` 做生产判定 | 它是训练后自检：会按设置重建训练区域、可能改工具状态，生产中用它既慢又危险 |
| 靠 9 点标定把缺陷坐标换算成抓取坐标 | 本期没有缺陷坐标数据源（PMA 只给一个分数），先把"固定点取放料"跑通 |
| 让 MainForm 负责接线与生命周期 | 现有约定是 `MainForm` 只 new 视图；主运行页自己懒初始化（与 `ResultRepository` 的后台泵同思路），避免多一处装配代码 |
| 把存图设置写进 `sysparam.json` | 你在图像保存那期明确决定"不落盘、只存输出图片"；本期沿用 |

---

## 十七、实测记录（v1.1）

### 17.1 落盘清单

| 文件 | 内容 | 行数 |
|---|---|---|
| `Services\CameraService.cs`（新） | 相机共享单例：FIFO 归属、`GrabOne`（含 `RunStatus` 诊断）、生产触发读写与回读 | 214 |
| `Services\IImageSource.cs`（新） | `IImageSource` + `CameraImageSource`（生产）+ `FileImageSource`（自测/回放，可模拟"取不到图"） | 176 |
| `Services\ImageSaver.cs`（新） | 存图（8bpp→24bpp、JPG 质量 90、按日期分目录、返回相对路径） | 150 |
| `Services\SaveSettings.cs`（新） | 存图根目录/格式/OK·NG 开关（共享，不落盘） | 60 |
| `Services\VisionScheme.cs`（新） | VPP 加载 → 取 PMA 工具 → 生产 `Inspect`（每轮写阈值 + 检测超时对表） | 208 |
| `Services\RobotArmService.cs`(新) | 机械臂共享客户端 `Client` + `EnsureConnectedAsync` + `OpResult` | 96 |
| `Services\RobotPickPlace.cs`（新） | 取料点→吸→放料点→放→回取料点 | 74 |
| `Services\ProductionRunner.cs`（新） | **状态机 + 主循环 + 异常处理 + 统计**（后台线程跑，不碰控件） | 511 |
| `Views\MainRunView.Run.cs`（新） | 按钮、灯、统计、状态链 ▶、预览、生效值回读 | 331 |
| 改 `MainRunView.cs` | 构造函数里 `WireRunEvents()` | +10 |
| 改 `VisionConfigView.Camera.cs` | `_camFifo` 字段 → **代理到 `CameraService.Shared.Fifo`**（本文件其余 1000 行一行没动） | ±8 |
| 改 `VisionConfigView.cs` | 注册 `ConnectHook`（主运行页连相机走本页已验证的例程）+ 加载/另存时写 `VisionScheme.VppPath` | +13 |
| 改 `VisionConfigView.Save.cs` | 三个存图控件 → `SaveSettings.Shared`（新增 `PushSaveToShared`，路径/勾选/格式三处触发） | +25 |
| 改 `VisionConfigView.Designer.cs` | `cmbImgFmt.SelectedIndexChanged` 绑定 | +1 |
| 改 `RobotArmSettings.cs` | 取料点/放料点 6 个坐标 + `GrabTimeoutMs` | +26 |

新增逻辑代码合计约 **1 800 行**。

### 17.2 自测一：整机探针（真 VPP + 真图 + 假从站 + 假桥接器）

`probe_run.exe`：真 `ProductionRunner`/`VisionScheme`/`ImageSaver`；先**真端口**走假 Modbus RTU 从站（虚拟串口对 COM1↔COM2）与假机械臂桥接器（回环 UDP 18899），数据库故意指坏端口。**15 项过 12**。

| 通过 | 结果 |
|---|---|
| S1a 单次跑到 `Stopped` | 阶段=Stopped、无错误 |
| S1b 统计 | 总 1 / OK 0 / NG 1 |
| **S1c 传送带真的收到停带+恢复** | `读 102`（验活）→ `写 100=0[停带]` → `写 100=1[恢复/正转]`（每个动作只一条报文；寄存器语义见 `传送带Modbus方案.md` §二/§5.4） |
| **S1d 机械臂五步顺序** | `moveJ(200,0,-50)` → `suction(1)` → `moveJ(200,-150,-50)` → `suction(0)` → `moveJ(200,0,-50)` |
| S1e NG 图落盘 | `自测-0001_20260917_183958_363_NG.bmp` |
| S4 取不到图 → 提示不判故障 | 阶段=Stopped、总数 0、无错误 |
| S10 连续 + 急停 + 复位 | 起来→急停后 `Estopped`/`IsRunning=False`、急停停了传送带、复位回 `Stopped` |

### 17.3 自测二：聚焦探针（阈值边界 + 断网补写）

| 项 | 结果 |
|---|---|
| F1 真 VPP 能加载出 PMA 工具 | **`已加载方案：QuickBuild1.vpp，判定工具：CogPMAlignTool1`** |
| F2 真图能读 | 1280×1024 |
| F3 阈值 0.999 | Ran=True，Accepted=False |
| **F4 阈值 0.01 → 应判 OK** | **FAIL：分数 0.0000、Accepted=False（见 §17.4 A）** |
| F6 `Enqueue` 断网不抛 | 3 条入队成功 |
| F7 后台泵转本地补写 | 补写=3、排队=0 |
| F8 补写文件落盘 | 3 行，`_tmp\probe_focus\pending.jsonl` |

### 17.4 未完成 / 未验证（**先看这一节**）

| # | 事项 | 现状与影响 |
|---|---|---|
| **A** | `bin\vpps\QuickBuild1.vpp` 里的 PMA 未训练 | ✅ **已训练（2026-09-20，"训呗"）**，原来那条"判 OK 分支从没被真验过"也一并关掉。① 视觉配置页『采集训练图』本来就有**三级退化**（相机层注入 → 方案自带取像工具 → **本地图片文件**，`VisionConfigView.Pma.cs:377-421`），所以没相机也能训；② 走**程序自己的训练路径**（`PmaTemplateTrainer`：`SetInputImage` → `ApplyTrainRegion(region, resetOrigin:true)` → `ApplyRunParams` → `Train`）在真文件上训练：`Trained=True`、**模式原点 (630,530)**、阈值 0.70，保存后 **1008991 → 2323473 字节**，重新从磁盘读回复核一致；③ 生产路径 `VisionScheme.Inspect` 跑三张样例图：`1.bmp 0.9334 / 2.bmp 0.9401 / 3.bmp 0.9401`，**都在阈值 0.70 之上 → Accepted=True**。**原件备份在 `bin\vpps\QuickBuild1.vpp.untrained.bak`**（1008991 字节、时间戳 9-15 未变）。训练区域没有用默认的 `FitToImage(0.8)`（那等于拿整张图当模板，背景也进去、没有区分度），而是套住齿轮中心那块有特征的地方 300×300 —— 人在这里会做的动作就是"把框拖到工件上" |
| **B** | 整机探针第二轮（S3）没跑完 | ✅ **2026-09-20 全流程测试重跑并全过（29/29，见 §二十一.2）**：判 OK / 判 NG / 连续+急停三轮都跑通，判 NG 那一轮机械臂真的按"视觉像素→九点标定→mm"去抓了。顺便挖出并修掉一个 **P0**（见 §二十一.3） |
| **C** | Q5 取放料坐标的**界面** | ✅ **2026-09-20 已补（见 §二十一.1）**：标定区「抓取几何」第二行 = `待机 X/Y` + `放料 X/Y/Z`，与标定一起存 `calib.json`；`PickX/PickY` 同时改名 `StandbyX/StandbyY`（语义澄清）。**不需要 Designer**，所以当初"甲放不下"的结论不再成立 |
| **D** | Q13 主运行页↔调试页互锁 | ✅ **已补（2026-09-20，见 §二十）**：`ProductionRunner.IsRunning` 一变就经 `PhaseChanged` 把调试页的设备按钮全部置灰，组标题写「生产中，已锁定」；探针 37 项断言过 |
| **E** | 调试页还在用自己的 `DobotArmClient` | ✅ **已补（2026-09-20，见 §二十）**：`DeviceDebugView.Robot.cs` 的 `_arm` 改成属性、代理到 `RobotArmService.Shared.Client`；探针断言两者是**同一个实例**（原来各 new 一份会让两个客户端抢同一条回环 UDP） |
| **F** | 整机探针 S1f/S3a/S3c 三项失败 | ✅ **2026-09-20 由全流程测试覆盖**（S1f 的"等后台泵"用 `WaitUntil` 轮询解决；S3a/S3c 随 B 一起过了）。**并且这次真的抓到了产品 bug** —— 见 §二十一.3 的 P0 |
| **G** | **Q8=A（工件编号下拉改成可输入）漏做了** | 你问"工件编号的下拉栏怎么会有数据"才发现：下拉里那两项是**草稿截图里的示例文字**，硬编码在 `MainRunView.Designer.cs` 的 `Items.AddRange` 里，不来自配置也不来自数据库。已按 Q8 决定改成**可输入**（`DropDownList`→`DropDown`）并**删掉那两项假数据**（详见 §17.9）。这是我上一版漏列的一项，感谢你问 |

### 17.5 实测抓到并修掉的真问题

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| ① | 真 VPP 一律报"方案里没有找到 PMA 工具（CogPMAlignTool）" | 我把 `ICogTool` 直接 `as IEnumerable` —— **它并不是可枚举的**；视觉配置页用的是 `CogToolGroup.Tools` / `CogToolBlock.Tools` | `VisionScheme.Load` 改成按类型取 `.Tools`（与视觉配置页 `GetSchemeTools()` 完全一致）。修后 F1 立刻打印出 `CogPMAlignTool1` |
| ② | 第一轮跑完后马上启动第二轮 → "已经在运行了" | 终态事件先发、`IsRunning` 后落，调用方在事件里重启就撞上 | `Fail()` 与两处"单次结束"都改成**先落标志再发事件** |
| ③ | 探针用 `MatchThreshold = 0.0` | 阈值语义是 `0 < 阈值 ≤ 1`（`视觉参数配置方案.md` §4.2 就是这么校验的）→ 这是**探针**的错，不是产品的错 | 探针改用 0.01；同时说明设置页的校验本来就是对的 |
| ④ | 未训练的 PMA 表现为"分数 0.000 → NG"，看着像"检测不合格" | `Inspect` 只看 `Results.Count`，把 VisionPro 真正想说的 `RunStatus`（`Error / 未训练`）吞了 | 结果为空时去读 `RunStatus`，翻成中文："PMA 模板还没训练（VisionPro：未训练）。请到「视觉配置」页点『训练模板』，再点『保存方案』。" 探针实测该提示已生效 |

### 17.8 把 VPP 打开看过（2026-09-17 追加）

你让"打开来看看"，探针（`probe_open.exe`）逐层 dump 了两个同名文件，结论是**我上一版 §17.4 A 的猜测不对，真因更简单**：

```
======== bin\x64\Debug\net48\vpps\QuickBuild1.vpp   1,008,991 字节（2026-09-15 18:56）
  根节点: CogJobManager   JobCount=1   Job: CogJob1   视觉工具: CogToolGroup
    - CogToolGroup
      - CogInputImageTool
      - CogToolBlock
        - CogImageConvertTool
        - CogPMAlignTool          ← PMA 在这里（嵌套在 ToolBlock 里，所以 FindTool 要 nestDepth≥2）
  PMA: CogPMAlignTool1  Pattern=CogPMAlignPattern  AcceptThreshold=0.5
  喂 1280×1024 灰度图后：
    A) tool.InputImage + tool.Run()  → 0 个结果   RunStatus=Error/未训练
    B) group.InputImage + group.Run()→ 0 个结果   RunStatus=Error/未训练
    C) job.Run()                     → 0 个结果   RunStatus=Error/未训练

======== D:\visionPor\QuickBuild1.vpp                14,583,475 字节（2026-09-09 14:51）
  根节点: CogJobManager   JobCount=9   视觉工具: CogToolGroup
    - CogInputImageTool / CogImageConvertTool / CogCaliperTool / CogCaliperTool
  → **树里根本没有 PMA 工具**（这是另一个工程，重名而已，不是"源文件"）
```

三点结论：

1. **`CogPMAlignTool1` 确实在方案里**（所以 §17.5 ① 那个 `ICogTool` 不是 `IEnumerable` 的 bug 是真的，修法也对）；
2. **它的模板是未训练的**——三种喂图方式（工具直喂 / ToolGroup / 整 Job）全都返回 `RunStatus=Error/未训练`、0 结果。所以分数恒 0 与"喂图方式"无关，**不需要去查输入绑定**；
3. 两个 `QuickBuild1.vpp` 不是同一个工程（一个 1 Job 带 PMA，一个 9 Job 带卡尺），只是文件名撞了。

因此现场动作很明确：**在视觉配置页加载这份 vpp → 选『训练模板』→『保存方案』**，之后再开主运行页看分数。

### 17.9 「工件编号」下拉里那两项数据的来历（2026-09-17 追加）

你问"工件编号的下拉栏怎么会有数据"，全仓查过，答案是：**没有数据源，是写死在 Designer 里的**。

```
MainRunView.Designer.cs:208  this.cmbRecipe.DropDownStyle = ComboBoxStyle.DropDownList;
MainRunView.Designer.cs:210  this.cmbRecipe.Items.AddRange(new object[] {
MainRunView.Designer.cs:211  "A001-通用件",
MainRunView.Designer.cs:212  "A002-缺料检测"});
（全仓只有这两行给它塞数据；MainRunView.Run.cs:65/97 只读 cmbRecipe.Text）
```

来历：主运行页的 Designer 是照 `04-资料/01主运行页.png` 一比一搭的，草稿里那个下拉就显示这两项，
当时只做布局不做逻辑，示例文字就留在代码里了。**它既不是配置、也不是数据库、也不是训练结果。**

为什么必须处理掉：按 Q7=A，`cmbRecipe.Text` 会被写进 `vision_result.workpiece_no`。
那两项看着像型号/配方（A001-通用件 / A002-缺料检测）而不是单件编号，
若留着，操作工一次都没输过，库里却会记着 `A001-通用件` —— **追溯时这是假数据**。

已改（Q8=A）：

| 项 | 改前 | 改后 |
|---|---|---|
| 控件风格 | `DropDownList`（只能选，不能输） | `DropDown`（**可输入**，将来扫码枪喂这个框） |
| 候选项 | 写死的两项假数据 | **清空**（默认空串） |
| 默认值 | 隐式选中第一项 | `Text = ""`（`workpiece_no` 存空串，数据库允许） |

留口（等你的决定，我没擅自做）：如果现场希望"从固定列表里选型号"，正确做法是把列表放进
`config\sysparam.json`（或将来的配方表）由界面加载，而不是写死在 Designer 里；要做说一声。

### 17.6 与方案的偏差

| 偏差 | 方案原本 | 实际 | 理由 |
|---|---|---|---|
| 相机搬家程度 | Q1=甲"整套搬进 `CameraService`" | 设备对象与取像归服务类；**连接例程仍留在相机页**，以 `ConnectHook` 注册 | 那 1000 行是现场返修 6 轮的代码，整体搬家只有回归风险没有收益。效果等价：一台相机一个 FIFO，两页共用 |
| 机械臂也共享 | 方案只说了相机 | 多做了 `RobotArmService.Shared.Client`（**但调试页还没切过来，见 §17.4 E**） | v3.0 只有一个桥接进程，客户端也必须只有一份 |
| 预览图形 | 方案说"最新图 + 检测框" | 目前只显示最新图（`CogRecordDisplay.Image`），**没画 PMA 结果图形** | 结果图形要动 `CogRecord`+`StaticGraphics`，手上没有已验证的画法；先把图显示出来，图形留到下一步（不猜 API） |
| 恢复传送带 | "SetSpeed + Forward" | 只发 `ForwardAsync` | 停带不碰速度寄存器（100/101），速度值保持原样，少一次写就少一个"把速度改回去"的风险 |
| 离线"仅视觉"模式 | Q12 提到可切图片回放 | `ProductionRunner.Source` 是公开属性，探针用它；**界面没有切换入口** | 不加控件；文档里写明怎么换（方案 §七） |

### 17.7 现场必做（在 §八 之上）

1. **先解决 A**：确认现场那份 vpp 里有训练好的模板，且主运行页显示的分数 >0（这是"判 OK"的前提）。
2. 用手挡一下光电，确认 Auto 硬触发能出图（§八.1）；图糊就切 Q2 乙。
3. 示教取放料坐标并把数值填进 `sysparam.json` 的 robot 组（界面入口见 §17.4 C）。
4. 生产前先在调试页确认传送带/机械臂单独可用，再开主运行页（在 E 补完之前，**别两页同时连设备**）。

---

## 十八、「库」指示灯改成开机真连一次（2026-09-17，你指定）

### 18.1 原来那句"正常"是假的

原来 `UpdateReadback()` 写的是 `Pending.Count == 0 && LastError 为空 → "库 正常"`，
那只是"**本进程内最近一次写库有没有报错**"：新进程一次库操作都没做过时**恒显示"正常"**——
2026-09-17 GUI 实测里，库其实根本连不上（`App.config` 那个 P0，见 `数据库方案.md` §二十），状态行照样写着"库 正常"。

### 18.2 改成什么

启动时（主运行页 `OnLoad` 末尾）调 `CheckDatabaseOnce()`：后台跑一次 `ResultRepository.Shared.TestConnectionAsync()`
（连接串自带 `ConnectionTimeout=3`，不卡界面），结果按下面的规则显示：

| 情形 | 状态行显示 |
|---|---|
| 结论还没回来 | `库 连接中…`（不谎报"正常"） |
| 连上且结果表在 | `库 已连`；预览区提示「数据库已连接（服务器／库名／结果表名 共 N 行）」 |
| 连上但结果表不存在 | `库 缺结果表`；提示去「系统设置」点『初始化建表』 |
| 连不上 | `库 连不上`（红字）；中文原因（**口令打码**）+「生产不受影响：结果先写本机补写文件，数据库恢复后自动补写」 |
| 之后运行期间 | 有排队 → `待补写 N`；最近一次写库报错 → `异常`；本次探测之后**真写成功过**（`Written` 增长）→ 以新证据为准显示 `已连` |

取值优先级（`DbLampText()`，证据新→旧）：`待补写` ＞ `异常` ＞ `连接中…` ＞ `写入成功过→已连` ＞ `探测失败→连不上` ＞ `缺结果表` ＞ `已连`。
配色集中到 `ApplyReadyColor()`：库坏/故障/急停 → 红，空闲/停止 → 灰，运行中 → 绿（原来配色散在 3 处，已合并）。

**顺带改了状态行的字序**：`● 库 … ｜阈值 …｜方案 …｜工具 …`。原来"库"在**最后**，
而这一行比面板宽——默认窗口宽度下实测**只显示到"工具 未加"**，也就是说"库"这格根本看不见。
把库挪到最前，保证任何窗口宽度下都看得见；整行另挂了悬停提示（全文）。

### 18.3 实测（真 GUI，鼠标点，不是探针）

| 场景 | 实测结果 |
|---|---|
| 正路（MySQL 在跑） | 状态行 `● 库 已连｜阈值 0.70｜方案 未加载｜工具 未加载`（灰）；预览区「数据库已连接（8.0.44／vision_sort／结果表 vision_result 共 0 行）。等待开始：…」 |
| 反路（**临时**把 `DbSettings` 端口改成 3307 重编译） | 状态行 `● 库 连不上`（**红**）；预览区「数据库连不上：连不上数据库服务器：确认服务已启动、端口正确、防火墙放行。（`Server=…;Uid=vision;Pwd=***;…`）／生产不受影响：检测结果会先写进本机补写文件，数据库恢复后自动补写。」 |
| 还原 | 端口改回 3306（`grep Port=` 核查）、重编译 **0 error / 0 warning**、重启复验仍是 `库 已连` |

### 18.4 还没做的（先说清楚）

- **只在开机连一次**：运行中库挂了不会主动再探，只会因写库失败把这一格变成 `异常`。要"定时重探"或"点一下重探"再说。
- **面板宽度没动**：默认窗口宽度下状态行**尾巴仍会被裁**（库在最前面，所以不受影响）。要彻底解决得动 `pnlLights1` 布局（加宽或换两行），风险比收益大，先不碰。

---

## 十九、DPI：为什么最后把 `DpiAwareness` 去掉了（2026-09-17 实测）

### 19.1 你看到的现象

「PerMonitorV2 什么情况，很多按钮都挤到一起了」——真 GUI 里确实是：
`开始检测/连续运行` 文字顶满按钮、`报警复位` 96/96 顶死、`初始化建表` 直接被裁。

### 19.2 实测数据（本机屏幕实际是 **175%** 缩放，168 DPI，不是 150%）

用探针（`winexe` + 应用同一份配置，在屏幕内创建真实 `MainForm`）量的：

| | 真感知 DPI（有 DpiAwareness 段） | DPI-Unaware（去掉该段后） |
|---|---|---|
| 窗口 DPI | 168 | 96（Windows 位图放大） |
| 微软雅黑 9pt 字体高 | 28 | 16 |
| MainForm `AutoScaleDimensions / Current` | `(13,28) / (13,28)` → **Factor=1×** | `(7,17) / (7,17)` → Factor=1× |
| 5 个视图同上 | 全部 `Factor=1×` | 全部 `Factor=1×` |
| `btnStart`（宽 100）文字宽 | 96 → **0.96（顶满）** | 56 → 0.56 |
| `btnReset`（宽 96）文字宽 | 96 → **1.00（顶死）** | 56 → 0.58 |
| `btnTestConn`（宽 110） | 96 → 0.87 | 56 → 0.51 |
| `btnSaveParam`（宽 120） | 96 → 0.80 | 56 → 0.47 |
| 窗口尺寸 | 1304×838（**用设计像素**） | 1296×813 设计尺寸 → 物理 2268×1423 |

### 19.3 机理（一句话）

WinForms 的 `AutoScaleMode.Font` 在运行时会把设计器写的 `AutoScaleDimensions`（本工程里还自相矛盾：
`MainForm/MainRunView` 是 `(6,12)`、另 4 个视图是 `(13,28)`）**改写成"当前字体在当前 DPI 下的度量"**，
于是**缩放系数恒为 1×，布局永远不缩放**；而字体是按真实 DPI 渲染的。
- Unaware 时：字体按 96 DPI 画 + Windows 整窗位图放大 1.75 → 比例自洽（只是略糊）✔
- 真感知 DPI 时：窗口还是设计像素，字体却 1.75 倍 → **文字撑破按钮** ✘

所以这不是"PerMonitorV2 的锅"，是**"绝对像素布局 + AutoScaleMode.Font"在真感知 DPI 下的必然结果**。

### 19.4 处置

`VisionSort\App.config` **去掉** `DpiAwareness` 段（只留 `startup`；注释里写清"不要再加回来"）。
进程回到 `Unaware`，外观与修 P0 之前**完全一致**。**P0 的修复不受影响**：库照样连（实测库灯 `已连`、
设置页「测试连接」成功）。现场 100% 缩放的工控机上，加不加这一段**没有任何视觉区别**。

### 19.5 以后真要支持高 DPI 时怎么做（别只把那段加回来）

把 `MainForm` 与 5 个视图的
```csharp
this.AutoScaleMode = AutoScaleMode.Dpi;
this.AutoScaleDimensions = new SizeF(96F, 96F);
```
`Dpi` 模式按"当前 DPI ÷ 96"这个**固定比例**缩放，不会因字体改动而改写基准（`OnFontChanged` 的改写只发生在
`Font` 模式），所以 175% 下会得到 `Factor=1.75`，布局与字体一起放大 → 既不挤也清晰。
**但那是一次独立改动**：要逐页（5 个视图）复验，尤其注意 `tlpRb` 这类"按像素算好的"表格布局、
以及 `pnlLights1`（386px 面板装不下那行长文案，本来就靠裁剪）。本机是 175%、现场大概率 100%，
除非现场也要上高 DPI 屏，否则不值得动。

### 19.6 实测记录

| 项 | 结果 |
|---|---|
| 去掉段后重编译 | **0 error / 0 warning** |
| 真 GUI（主运行页） | `开始检测/停止检测/单次运行/连续运行`、`拍摄中/检测OK/检测NG`、`急停/报警复位` **全部完整显示**；状态行 `● 库 已连｜阈值 0.70｜方案 未加载｜工具 未加载` 完整可见 |
| 真 GUI（系统设置页） | 四组标签/输入框对齐，`测试连接/初始化建表/保存参数/加载参数` 完整显示，复选框标签不再压框 |
| 库 | 预览区提示「数据库已连接（8.0.44／vision_sort／结果表 vision_result 共 0 行）」→ P0 修复仍然有效 |
| 探针 | 临时探针（`ProbeDpi`）已删除，`bin` 与 `_tmp` 无残留 |

---

## 二十、Q13 生产互锁 + 调试页共用机械臂客户端（2026-09-20，你指定"先把 E + D 补掉"）

> 这两条是 §17.4 里"不补就不能安全开机"的两件。原来都记着"没做/必须补"，现在补完了。

### 20.1 E：调试页不再自己 `new` 一个客户端

**原来的问题**：`DeviceDebugView.Robot.cs` 里是
`private readonly DobotArmClient _arm = new DobotArmClient();`，
主运行页分拣走的是 `RobotArmService.Shared.Client` —— **两个客户端**。
而 v3.0 只有**一个** x86 桥接进程、**一条**回环 UDP，两边各发各的命令会互相插队，
现场表现是"机械臂偶尔乱走 / 报超时"，且**很难复现**。

**改法**（一行语义、零调用点改动）：

```csharp
private DobotArmClient _arm { get { return RobotArmService.Shared.Client; } }
```

`_arm.xxx` 的十几处调用一行没动。顺带把两个语义变化写进注释：
① 本页『断开机械臂』断的是共享连接 —— 但生产在跑时那两个按钮已被互锁置灰（§20.2），所以不会发生；
② `ShutdownRobot()` 里的 `Dispose()` 现在释放共享实例 —— 本页只在程序退出时 Dispose，语义与改动前一致。

### 20.2 D：生产运行时把整页置灰（新增 `Views\DeviceDebugView.Lock.cs`）

- 订阅 `ProductionRunner.PhaseChanged` → 状态一变**立刻**刷（不等 500ms 轮询）；
  事件在后台线程，回 UI 线程用本页自己的 `OnDeviceUi`（与 `MainRunView.OnUi` 同款）。
- 进页面时（`OnLoad`）主动刷一次 —— 可能进这一页时生产**已经在跑了**。
- 灰掉的：
  - 传送带：连接/断开/故障复位/正转/反转/停止/**调速滑杆**
  - 机械臂：连接/断开/回零/消警/六个点动/定点/执行器/吸盘放/添加队列/清空队列/启动队列
  - 标定区：读当前位姿/加行/删末行/清空/粘贴/**计算/保存/重新载入/核对 PMA 原点**/四个几何框
    （理由：计算/保存/载入会改**生产每轮都要读**的那份标定；几何框改的是分拣在用的 Z 与工作范围；
     核对会 `VisionScheme.Load()` 等于生产跑到一半换掉工具对象）
  - **留着可点的**：标定区的『操作指引』（纯只读 —— 生产时人还是可能想翻一眼）
- 连带停掉两个轮询：`_robotTimer`（位姿）与 `_stateTimer`（Modbus 状态寄存器）——
  生产要那两条链路，别再拿轮询去占。`txtRunState` 显示「生产中（由主运行页控制）」，**不装作知道**。
- **组标题直接写明**「（生产中，已锁定）」：光把按钮变灰，现场会以为"程序卡了"。
- 锁定瞬间还会 `StopJogIfActive()`：如果用户正**按住** X+ 时生产开始，按钮被置灰后 `MouseUp` 可能收不到，
  机械臂会一直走 —— 所以锁定路径主动补一次点动停止。

### 20.3 实测（探针 37 项断言全过，跑完即删）

| 组 | 验了什么 | 结果 |
|---|---|---|
| **E** | `_arm` 已是属性；**调试页的 `_arm` 与 `RobotArmService.Shared.Client` 是同一个实例**（哈希相同、`ReferenceEquals` 为真） | ✅ |
| **D 初始** | 生产没跑时：正转/X+/计算都可点，组标题没有"已锁定" | ✅ |
| **D 锁定** | 传送带 5 项 + 机械臂 5 项 + 标定 4 项全部灰；**『操作指引』仍然可点**；两个轮询定时器 `Enabled == false`；两个组标题都写了「生产中，已锁定」 | ✅ |
| **D 解锁** | `_productionLock` 复位；正转/X+/计算恢复可点；组标题复位 | ✅ |
| **不误伤** | 互锁不去改 `ProductionRunner.IsRunning`（生产自己的状态没被动过） | ✅ |

**编译**：Debug x64 **0 error / 0 warning**。

### 20.4 还没做的（同属 §17.4）

- ~~**C**：放料点 `PlaceX/Y/Z` 没有界面~~ → ✅ **2026-09-20 已补**（见 §二十一.1）：标定区「抓取几何」加成两行，
  第二行是 `待机 X/Y` + `放料 X/Y/Z`，与标定一起存 `calib.json`；顺手把 `PickX/PickY` 改名 `StandbyX/StandbyY`
  （它的真实语义是"待机位"，不是取料点 —— 取料点现在由标定换算）。
- **A/B/F**：~~PMA 模板未训练~~✅已训 / ~~整机探针 S3 没跑完~~✅全流程 29/29 过 / 三项探针遗留 —— 见 §二十一。

---

## 二十一、放料点界面 + 全流程整机测试（2026-09-20，"改完就全流程测试"）

### 21.1 甲：抓取几何补全（原来是"必须重编译才能改"）

**背景**：`StandbyX/Y`（原 `PickX/PickY`）与 `PlaceX/Y/Z` 这 5 个数只活在 `RobotArmSettings` 的字段初始值里，
没进 `sysparam.json` 也没进 `calib.json` —— 现场想改放料点只能**改源码重编译**。

**改法**（不动 Designer，标定区本来就是代码建的）：

| 文件 | 改动 |
|---|---|
| `RobotArmSettings.cs` | `PickX/PickY` → **`StandbyX/StandbyY`**（语义澄清）；注释写清"取料点由标定换算，这里不是" |
| `RobotPickPlace.cs` | 回待机位改读 `StandbyX/StandbyY`；注释点明与取料点的区别 |
| `CalibrationService.cs` | `calib.json` 增存/读 `standbyX, standbyY, placeX, placeY, placeZ`（缺字段保留当前值，老文件不炸） |
| `DeviceDebugView.Calib.cs` | 「抓取几何」第二行：`待机 X Y` + `放料 X Y Z`；**并加一条校验：待机位必须落在工作范围内**（否则"回待机位"那一趟会先撞出去） |

界面尺寸复核：右列两行几何 y=148 / y=176（最右边 x=766 < 786），状态标签由 92px 压到 68px，总高 272 < 286。

### 21.2 全流程整机测试：**29 项断言全过**

真：训练好的 VPP + 三张样例图 + **真 MySQL**；假：**Modbus 从站（COM2 虚拟对）+ 机械臂桥接进程（UDP 18899）**。

| 轮次 | 验了什么 | 结果 |
|---|---|---|
| 1 判 OK | 完整阶段链 `Preparing→WaitingTrigger→StoppingBelt→Inspecting(0.933→OK)→Recording→Resuming→Stopped`；停带写 `100=0`、恢复写 `100=1`；**OK 轮一次机械臂都不动**；真库写入成功（`Written 0→1, Pending=0`） | ✅ |
| 2 判 NG | **机械臂收到 3 条 moveJ：`60.36,48.61,-50`（＝视觉像素 (620.2,508.9) 经九点标定换算出来的取料点）/ `200,-150,-50`（放料点）/ `200,0,-50`（待机位）**；停/恢复带；写库 | ✅ |
| 3 连续+急停 | 连续模式能连跑；急停 → 停带（写 `100=0`）+ `IsRunning=false` + `Estopped`；报警复位 → `Stopped` 且不会自己跑起来 | ✅ |

### 21.3 ★ 全流程测试挖出一个 P0（**已修**）：判 NG 时拿不到像素 → **分拣从来没工作过**

**现象**：第 2 轮判 NG 时，视觉返回"没有找到匹配：Accept"、**`HasPixel=false`** → 机械臂一步没动。

**根因（实测，阈值扫描 + 底层直读）**：

```
2.bmp，同一张图、同一套方案：
  阈值 0.000 / 0.300 / 0.500 → 分数 0.9401，Results=1
  阈值 0.900 / 0.930 / 0.940 / 0.999 → 分数 0.0000，Results=0     ← 分数 0.94 的匹配也被滤掉了！
底层直读：AcceptThreshold=0.00 → Results=1；AcceptThreshold=0.99 → Results=0
```

**`CogPMAlignTool.RunParams.AcceptThreshold` 会把结果从 `Results` 集合里滤掉** ——
不是"结果在、Accepted=false"，是**根本没有结果**。
后果：判 NG 的工件 → 没有像素坐标 → 机械臂不知道该去哪儿抓 → **NG 分拣永远不动作**。
（这条从"判定用 PMA"那期就存在，只是以前 `RobotPickPlace` 用固定示教点，看不出问题。）

**修法**：判定改由我们自己算。
- `Inspect` 里固定 `AcceptThreshold = 0`（不让 VisionPro 滤结果）→ 取最佳候选
- `Accepted = 分数 ≥ MatchThreshold`（判定）
- `HasPixel = 分数 ≥ VisionSettings.PickScoreFloor`（"视野里到底有没有工件"）

`PickScoreFloor` 默认 **0.30**，是**量出来的**（同一套方案、`AcceptThreshold=0`）：

```
有齿轮（整幅）                    分数 0.9401
只有背景的 5 块 300×300 裁剪       分数 0.0000
全黑 / 全白 / 全灰 1280×1024      分数 0.0000
```

分得极开，0.30 足够安全；它的作用只是"别对着空带子空抓一次"。**界面暂时没给这个框**（要现场调就说一声）。

### 21.4 顺带发现的文档/代码不一致 → **按 ② 处理：改文档，不改代码**

方案 §4.6 与 §十二 写的是「恢复 = `SetSpeedAsync(速度)` + `ForwardAsync()`」，
但实测从站只收到 `写100=0 / 写100=1`，**没有 `写102`** —— 代码里只调了 `ForwardAsync()`。

**处理（2026-09-20 你定 ②）**：**改文档，不改代码。**

- 依据：`§17.6 与方案的偏差` 里**早就记过这一条**——
  "恢复传送带：方案原本 `SetSpeed + Forward`，实际只发 `ForwardAsync`，
  理由：停带不碰速度寄存器，速度值保持原样，**少一次写就少一个『把速度改回去』的风险**"。
  也就是说这是当初**有意的偏差**，只是 §4.6 / §十二 那两处忘了跟着改。
- 本次把 §4.6、§十一 状态表的 `Resuming`、§十二 三处统一成实际行为，并都在原地指向 §17.6。
- **代码里也补了注释**（`ProductionRunner.cs` 恢复带那一处），免得后人看到 §4.6 的旧说法去"补"一次速度写。
- 结论：速度只由**设备调试页的调速滑杆**写入（`SetSpeedAsync`），生产循环只负责**起停**。

### 21.5 编译与清理

Debug x64 与 Release x64 均 **0 error / 0 warning**；探针（4 个）与 `_tmp\flow_pics` 全部删除，
`bin\config` 只剩 `sysparam.json`，`bin\vpps` 只有训练好的 vpp + 未训练备份。

---

## 二十二、2026-09-20 联动缺口修正：切回本页要刷新状态行

### 22.1 缺口（界面联动复查时发现）

§九 立的原则是"把**当前生效值**显式写出来 —— 改了不生效是现场最常见的困惑"，状态行也确实把
阈值 / 标定 / 方案 / 工具 都写出来了。**但刷新时机漏了一处**：

| 触发 `UpdateReadback()` 的时机 | 覆盖 |
|---|---|
| `OnLoad`（进程起来） | ✅ |
| `PhaseChanged` / `Faulted`（跑生产时） | ✅ |
| 开始检测（`StartRun`） | ✅ |
| **在别的页改完值、切回本页** | ❌ **漏的就是这里** |

后果：在系统设置页把阈值从 0.70 改成 0.85、或在设备调试页重新做九点标定，**切回主运行页看到的还是旧值**；
可阈值其实**下一轮就已经生效**了（`ProductionRunner` 每轮现读 `VisionRunSettings.Shared.MatchThreshold`）。
也就是说这条状态行会**主动说一句假话** —— 正好是它自己最不该犯的错（与「库」那格同款的"界面不许撒谎"）。

根因有两层：① 全程序**只有 `ProductionRunner` 一家发事件**，各设置对象（`VisionRunSettings` /
`CalibrationService` / `VisionScheme`…）改值**不通知任何视图**；② `MainForm` 里**没有任何 `TabControl` 事件**，
所以切页这个动作本来就没人在听。

### 22.2 修法

`MainRunView.Run.cs`：

```csharp
// WireRunEvents() 里加一行
VisibleChanged += MainRunView_VisibleChanged;

private void MainRunView_VisibleChanged(object sender, EventArgs e)
{
    if (!Visible || IsDisposed || !IsHandleCreated) return;   // 只在"变可见"且控件可用时刷
    UpdateReadback();
}
```

两个设计选择：

- **用 `VisibleChanged`，而不是给 `TabControl` 挂 `SelectedIndexChanged`**：本页不需要、也不该知道
  "自己被谁装着"。切页时父级可见性变化会自然传到这一层 → **零耦合**，以后换容器也不受影响。
- **重复触发无害**：`UpdateReadback()` 只读 `Shared` 的现值，不写状态、不碰设备。

> **范围说明**：本次**只**修"读数刷新"。数据库那一格仍沿用开机自检的结果 +
> "之后真写成功过 → 已连"的新证据优先规则（`DbLampText()`），**没有**改成"每次切页重连一次库" ——
> 那会在每次切页都压一次 3 秒连接超时。

### 22.3 实测（探针，本缺口 8 项断言全过，测完即删）

一个**真 `MainRunView`** 挂在带两个 TabPage 的**真 `TabControl`** 上，`form.Show()` 触发 `OnLoad`：

| # | 断言 | 结果 |
|---|---|---|
| 1 | 探针能反射到 `lblSysReady` | ✅ |
| 2 | 初始读数含阈值 `0.70` | ✅ |
| 3 | 初始读数含方案名 `SchemeA.vpp` | ✅ |
| 4 | **停在页面上时读数不刷新**（对照组 —— 这就是原来的错法） | ✅ |
| 5 | 切走再切回后，阈值刷新为 `0.85` | ✅ |
| 6 | 切回后方案刷新为 `SchemeB.vpp` | ✅ |
| 7 | 旧值 `0.70` 不再出现 | ✅ |
| 8 | 标定那一格仍在读数里 | ✅ |

读数实测前后对比（探针原样输出）：

```
初始 : ● 库 连接中…｜标定 无｜阈值 0.70｜方案 SchemeA.vpp｜工具 未加载
切回 : ● 库 连接中…｜标定 无｜阈值 0.85｜方案 SchemeB.vpp｜工具 未加载
```

编译：Debug x64 / Release x64 均 **0 error / 0 warning**。
（「标定 无」是因为探针进程没加载 `calib.json`，与本次修正无关。）

> ⚠ **§22.3 只是针对这两处改动的定点探针，不是全流程测试。** 补跑见 §二十三。

---

## 二十三、2026-09-20 全流程整机测试复跑（39 项断言全过）

### 23.1 为什么要补跑

§二十一 那套全流程探针当时用完就删了；§二十二 / 视觉参数配置方案 §十一 只跑了**定点探针**。
"定点探针过"只能证明**新加的那两段代码本身对**，不能证明**没把整条生产流程弄坏** —— 尤其是 §二十二
新加的 `VisibleChanged → UpdateReadback()` 会在**生产运行中**被触发。所以补跑一次全流程。

### 23.2 环境（真一半、假一半）

| 真 | 假 |
|---|---|
| 训练好的 `QuickBuild1.vpp`（2.32 MB） | **Modbus RTU 从站**：探针自己实现 RTU 帧 + CRC16，跑在 **COM2**（虚拟对 COM1↔COM2） |
| 样例图 `20260908-素材\齿轮缺齿检测\2.bmp` | **机械臂桥接进程**：UDP `127.0.0.1:18899`，线协议与真品一致（`ping/connect/pose/moveJ/suction/…`） |
| **真 MySQL**（`vision_sort.vision_result`） | 位姿"即时到位"：假桥接把当前位姿设为 `moveJ` 目标，否则 `WaitArrive` 容差判定会白等 5 秒 |
| **真 `MainRunView`** 挂在**真 `TabControl`** 上（轮 3 要切页） | 九点标定：程序化加 9 对点（`mm = 0.192308×px + 10` / `-100`，行列式 > 0 不镜像） |

> 用改阈值（0.50 / 0.99）而不是换图来制造 OK / NG —— 同一张图（分数 0.9401）两轮都跑，
> 变量只有一个，OK 与 NG 的差别**只**来自判定，结论更干净。

### 23.3 结果

| 轮 | 验了什么 | 结果 |
|---|---|---|
| 前置 | VPP / 样例图 / 9 对点加入 / 拟合成功 / 未镜像 / 探针能拿到读数标签 | ✅ 6 |
| **1 判 OK**（阈值 0.50） | 跑完且终态 `Stopped`；统计 OK=1；**机械臂一步没动**（`moveJ` 计数 = 0）；从站只收到 `100=0`→`100=1`、**没写 102**；真库 `Written` 增长 | ✅ 9 |
| **2 判 NG**（阈值 0.99，同一张图） | 统计 NG=1；`moveJ` **正好 3 条**；吸盘 `on=1`→`on=0`；停带 `100=0` + 恢复 `100=1` | ✅ 11 |
| **3 连续 + 急停** | 连跑 ≥2 轮；**生产运行中切页**；急停 → `Estopped` + 写 `100=0`；报警复位 → `Stopped` 且不自己跑 | ✅ 13 |

**★ 轮 2 的核心证据 —— 视觉像素 → 九点标定 → mm → 机械臂，全链路对得上：**

```
视觉像素 (620.2, 508.9)  →  mm (129.27, -2.14)    分数 0.9401
桥接实收 moveJ：129.2738,-2.1390,-50.0000 | 200.0000,-150.0000,-50.0000 | 200.0000,0.0000,-50.0000
                 └── 取料点（= 标定换算值）  └── 放料点 PlaceX/Y/Z        └── 待机位 StandbyX/Y + PickZ
```

而且这个像素 **(620.2, 508.9) 与 §二十一 当时实测的完全一致** —— 换了一套探针重算，视觉结果可复现。

### 23.4 本次两处修正的回归结论

| 修正 | 全流程测试覆盖到了吗 | 结论 |
|---|---|---|
| 缺口 2（切页刷新读数） | ✅ **直接覆盖**（轮 3 在生产运行中切页） | 切页后阈值 `0.50 → 0.66` 跟上、方案名与标定格正确、**生产未中断（总数继续涨）**、无异常 |
| 缺口 1（保存方案回写 `VppPath`） | ❌ **覆盖不到** | 全流程不点「保存方案」，所以它**只能**靠 §十一.3 那支定点探针验 —— 这不是偷懒，是这条路径本身在生产流程之外 |

轮 3 切页前后读数（探针原样输出）：

```
切页前：● 库 已连｜标定 0.1923mm/px·RMSE 0.000｜阈值 0.50｜方案 QuickBuild1.vpp｜工具 CogPMAlignTool1
切页后：● 库 已连｜标定 0.1923mm/px·RMSE 0.000｜阈值 0.66｜方案 QuickBuild1.vpp｜工具 CogPMAlignTool1
```

### 23.5 收尾自查

- 探针 exe / config / 源码、`_tmp\` 全部删除；`bin\config` 只剩 `sysparam.json`，`bin\vpps` 只有 vpp + 未训练备份；COM1/COM2 已释放。
- **真库副作用（如实记账）**：本次往 `vision_sort.vision_result` 写了 **5 行**测试数据（`workpiece_no` 形如 `T00x`；全表 20 行）。这是"真库写入成功"那条断言的代价，需要的话一条 SQL 就能清掉。
- **真程序退出冒烟**：`VisionSort.exe` 启动 8 秒（125.5 MB）→ `CloseMainWindow()` → **15 秒内退出、无残留进程**。
  （补这条是因为探针进程跑完不自行退出 —— 那是探针没走 `Application.Run`/`Application.Exit` 所致，
  真程序实测干净退出，**不是产品问题**。）
- 编译：Debug x64 / Release x64 均 **0 error / 0 warning**。


