<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!--
    ⚠ 一、不要把 System.Windows.Forms.ApplicationConfigurationSection 写进 <configSections>！
    它是 .NET Framework 4.7+ 的**内置节**，声明它会让**整个配置系统**初始化失败：
      ConfigurationErrorsException：配置系统未能初始化
        └ “System.Windows.Forms.ApplicationConfigurationSection”节是内置节。它不能由用户声明。
    后果不是"DPI 设置失效"这么轻：
      System.Transactions.Diagnostics.DiagnosticTrace 的类型初始值设定项抛异常
        → MySqlConnector 每次 Open() 碰 Transaction.Current 都失败
        → 打包出去的程序连不上 MySQL（2026-09-17 GUI 实测抓到的 P0，见 数据库方案.md §二十）。

    ⚠ 二、也不要再加下面的 DpiAwareness 段（2026-09-17 GUI 实测后移除）：
      <System.Windows.Forms.ApplicationConfigurationSection>
        <add key="DpiAwareness" value="PerMonitorV2" />
      </System.Windows.Forms.ApplicationConfigurationSection>

    为什么移除（都是实测数据，不是推测）：
      · 本项目窗体是**绝对像素布局**，而 WinForms 的 AutoScaleMode.Font 在运行时会把握手值
        AutoScaleDimensions 改写成"当前字体在当前 DPI 下的度量"，于是缩放系数恒为 1×。
        175% 机器（168 DPI）实测：MainForm 与 5 个视图全部 Design=(13,28) Current=(13,28) Factor=1x1。
      · 进程一旦真感知 DPI：窗口仍用设计像素，字体却按 1.75 倍渲染 → 文字撑破按钮：
        btnStart 控件宽 100 / 文字宽 96、btnReset 96/96、『初始化建表』直接被裁 —— 就是"按钮挤到一起"。
      · 不加这一段时进程是 DPI-Unaware：Windows 把整窗位图放大（字略糊，但比例自洽、按钮不挤）。
      · 现场工控机大概率 100% 缩放，这两种做法在 100% 下**没有任何区别**。

    要真正支持高 DPI（字清晰 + 布局不挤），正确做法是把 MainForm 与 5 个视图的
    AutoScaleMode 改成 AutoScaleMode.Dpi 且 AutoScaleDimensions=(96,96)
    （Dpi 模式按 DPI 比例缩放，不会因字体改动而改写基准），然后逐页复验。
    那是一次独立改动，见 主运行页方案.md §十九。**不要只把这一段加回来。**
  -->
  <startup>
    <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.8" />
  </startup>
</configuration>
