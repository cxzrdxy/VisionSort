# 传送带 Modbus · 实施方案与实测记录

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms）
> 页面：`Views/SysConfigView.cs`（参数配置）+ `Views/DeviceDebugView.cs`（实时控制）
> 版本：v4.0（**已实现；自测 + 真实串口 + Modbus Slave 联调均通过**）
> 协议库：NModbus 3.0.83（`Libs/NModbus.dll` 本地引用，离线可用）

---

## 一、范围与决策（已定稿）

| 题 | 决定 |
|---|---|
| Q1 范围 | **B**：`SysConfigView` 参数配置 + `DeviceDebugView` 实时控制（连接/断开/复位/正转/反转/停止/调速/状态）都做 |
| Q2 方向 | **A（已按示例工程更正，2026-09-18）**：**100=启停**（写 1 运行 / 写 0 停止）、**101=方向**（写 1 正转 / 写 0 反转）；**停止只需 100←0**，不必动 101。~~原写"100=正转启停、101=反转启停、停止=两边都写 0、正反转互锁"~~ ← 那是错的，见 §二 |
| Q3 参数存哪 | **A**：`config\sysparam.json`（整页 4 组；极简 JSON 手写读写，无第三方依赖） |
| Q4 站号 | **2**（界面可改） |
| Q5 协议库 | **NModbus 3.0.83**（RTU 串口） |
| Q6 界面 | IP/端口 → 串口号(COM5)/波特率(9600)，加站号(2)+反转寄存器(101)，默认 100/101/102 |

**不在范围**：机械臂控制（调试页右侧 `grpRobot`）—— 单独方案见《机械臂TCP控制方案.md》v3.0：**官方 `DobotDll.dll` + x86 桥接进程 `DobotArmBridge.exe` + 回环 UDP IPC**（官方原生 DLL 只有 32 位，与 x64 的 VisionPro 不能同进程，必须靠独立 x86 进程隔离；"TCP 直连"是已废弃的 v1.4 路线，v2.1 是官方二进制协议，v3.0 才是现行）。

---

## 二、设备协议（**以现场给的示例工程为准**）

> **依据**：工作区里的原始示例工程
> `day30\03-code\MyModBus\MyModBus\BeltControl.cs`（按钮 → 调用一字不改地抄在下面）。
> 2026-09-18 更正：本方案早先写的"100=正转启停、101=反转启停、停止=两边都写 0、正反转互锁"**是错的**，
> 正确语义见下表。**不要再按"互锁/双停"那套写代码或写文档。**

```
传输    : Modbus RTU，串口 9600 / 8 数据位 / 无校验 / 1 停止位（示例代码值）
站号    : 2（界面可改）
100     : 启停        写 1 = 启动（运行），写 0 = 停止
101     : 方向        写 0 = 反转（写 0 即换向，**不需要先停正转**）；写 1 = 正转
102     : 速度        ushort 直写（界面滑杆 0–1000 对应 0–100%，即 /10）
超时    : 串口读写 3 秒
换向    : **没有"先停稳再反向"这套**——这台传送带没这么高级（2026-09-18 你确认，别再为它加互锁/死区延时）
故障复位: 示例代码里没有该寄存器 → 暂定"停止"（100←0），见 §六 待办
```

示例工程里五个按钮的实际调用（**权威对照**）：

| 示例按钮 | 调用 |
|---|---|
| 启动 | `WriteSingleRegister(2, 100, 1)` |
| 停止 | `WriteSingleRegister(2, 100, 0)` |
| **正转** | `WriteSingleRegister(2, 101, 1)` |
| **反转** | `WriteSingleRegister(2, 101, 0)` |
| 设置速度 | `WriteSingleRegister(2, 102, speed)` |

由此推出三条**必须记住**的结论：

1. **"反转"= 往 101 写 0**（不是写 1）；写 0 就是反转，**不需要先停正转**——这台传送带没有"先停稳再反向"的要求。
2. **"正转"= 往 101 写 1**；别按"1=启动"的直觉去套方向寄存器。
3. **"停止"= 只往 100 写 0**，不需要动 101（方向留着，下次启动沿用）。

> **写入侧已按此改完并抓过报文（见 §5.4）；只剩"状态回读"待现场定，见 §六 待办 7。**

> **现场必看**：校验位必须是 **None**。Modbus Slave 默认是 Even，实测不改就永远读超时。
> 若现场变频器只支持偶校验，改 `ConveyorSettings.CreateResource()` 里的 `Parity.None` 一处。

---

## 三、落盘清单

| 文件 | 说明 |
|---|---|
| `Services/SerialPortStreamResource.cs`（新增） | NModbus 3.x 删了串口直连（2.x 的 `ModbusSerialMaster.CreateRtu` 已不存在），30 行适配器包 `SerialPort.BaseStream` 实现 `IStreamResource` |
| `Services/ConveyorSettings.cs`（新增） | 唯一真相源（静态 `Shared`，MainForm 不接线，靠单例给三页共用）：配置 + RTU 连接 + 正转/反转/停止/复位/调速/回读 |
| `Views/SysConfigView.Modbus.cs`（新增） | 6 个输入框失焦校验 + 保存/加载整页参数 |
| `Views/DeviceDebugView.Conveyor.cs`（新增） | 7 个动作 + 500ms 状态轮询 + 滑杆调速（防抖） |
| `Views/SysConfigView.Designer.cs`（改） | RTU 化（4 行→6 行）+ 6 个 Leave + 2 个 Click 绑定 |
| `Views/DeviceDebugView.Designer.cs`（改） | 6 个 Click + 滑杆 `ValueChanged`；「断开Modbus」初始置灰 |
| `VisionSort.csproj` + `Libs/NModbus.dll` | NModbus 3.0.83（net46）本地引用 |

示例代码（2.x 写法）→ 本实现（3.x）对照：`using Modbus.Device` → `using NModbus` / `NModbus.IO`；
`ModbusSerialMaster.CreateRtu(port)` → `CreateResource()` → `new ModbusFactory().CreateRtuTransport(...)` → `CreateMaster(...)`；
`WriteSingleRegisterAsync(站号, 地址, 值)` 同名同参，直接照抄。

---

## 四、关键设计（都是踩过坑才这么写的）

1. **串口访问串行化**：NModbus 主站不是线程安全的，界面 500ms 状态轮询与按钮动作会并发 → 自测实测"刚点正转，回读还是停止"。`ConveyorSettings` 内加 `SemaphoreSlim(1,1)`，所有读写都在同一把锁内完成（一次动作里若发多条报文，也保证中间不被插队）。
2. **界面丢弃过期回读**：轮询结果晚到时若动作正在进行（`_busy`），丢弃这次回读，避免旧值覆盖新状态。
3. **全 Async**：串口超时按秒计，同步会卡 UI（示例也是 async + await，照抄）。
4. **调速防抖**：滑杆拖动即时更新显示，停手 250ms 后才真正写设备，避免拖动过程刷几十条串口指令。
5. **按钮可点规则**：连接/断开互斥置灰（连接状态的唯一指示，不新增控件）；正反转/停止/复位永远可点，未连接时弹提示。
6. **参数落盘**：`config\sysparam.json` 存整页 4 组（Modbus/机械臂/视觉/数据库）；界面改动即时写进 `Shared`，不必先点保存。
7. **连接验活**：连接后空读一次速度寄存器，通不过就整体失败并清理资源，不留半连接状态。

---

## 五、实测记录

> ⚠ **本节 5.1 / 5.2 的结论写在 2026-09-18 更正语义之前**，里面凡是"101=反转启停""互锁""双停"的说法
> 都按**旧（错误）理解**记的；**以 §二 的语义表为准**，报文级证据与偏差见 §5.4。

### 5.1 自测（探针驱动真实 `VisionSort.exe`；RTU over socket 回环 + 界面自动化；探针跑完即删）

| # | 场景 | 结果 |
|---|---|---|
| S1–S7 | 连接/正转/反转/调速450/停止/复位/断开 | ✅ 全部通过（当时验证的是"能发出去、能读回来"，不是"方向字写对了"） |
| U1–U5 | 系统设置默认值 / 非法寄存器回退 / 合法改动即时生效 / 保存 / 加载 | ✅ COM5-9600-2-100-101-102；非法值弹提示并回退；`sysparam.json` 落盘与回读一致 |
| V1–V6 | 调试页初始态 / 未连接点正转提示 / 连接 / 正反转停止复位 / 滑杆500→50.0且设备回读500 / 断开 | ✅ 全部通过 |
| — | 布局渲染（截图人眼确认） | ✅ Modbus 六行齐全、两个按钮都可见、调试页置灰/绿灯正确 |
| — | 真机冒烟 | ✅ 启动无异常退出 |

### 5.2 真实串口联调（ELTIMA 虚拟对 COM1↔COM2 + Modbus Slave）

环境：Modbus Slave 设 `ID=2 / F=03 / Address=100 / Qty=3 / COM2 / 9600-8-N-1 / RTU`；VisionSort.exe 在『系统设置』把串口号改成 `COM1`。走的是生产代码路径（`SerialPortStreamResource` → NModbus RTU，无替身注入）。

| # | 场景 | 结果 |
|---|---|---|
| E1 | 探针直连 COM1 | ✅ 连接 → 正转 → 调速450 → 反转 → 停止 → 断开，全通 |
| E2 | 从站界面独立核对 | ✅ Modbus Slave 显示 `100=1 / 101=0 / 102=500` |
| E3 | 真机 App 连接 | ✅ 通讯状态**已连接**；运行状态**正转**、当前速度**50.0**（从站里残留值，证明读的是设备） |
| E4 | App 点『反转』 | ✅（**旧语义**）从站 `100=0 / 101=1`；按更正后的语义，`101=1` 是**正转**方向，这条只证明"发了报文"，见 §5.4 |
| E5 | App 拖滑杆 | ✅ 从站 `102` 由 500 变 **400** |
| E6 | 断开 + 关闭 | ✅ 无残留配置文件 |

### 5.4 报文级抓取：正转/反转/停止到底往串口写了什么（2026-09-18 **改完"先停再转"后重抓**）

> 本节重写过两次：第一版按**错误语义**解读（以为是"101 写 1 = 启动反转"）；第二版按更正后的语义重抓，
> 但那时"先停另一边"的两条写还在；**这一版是删掉"先停"之后的最终报文**（每个动作只剩它自己那一条）。

方法：COM2 当假从站（站号 2；FC06 原样回显，FC03 回速度 300），COM1 跑应用自己的
`ConveyorSettings.Shared`（走生产代码路径，无替身注入）。探针跑完即删。

```
--- 正转 ForwardAsync() ---
  02 06 00 64 00 01 09 E6   | FC=0x06 地址=100（启停）值=1

--- 反转 ReverseAsync() ---
  02 06 00 65 00 00 99 E6   | FC=0x06 地址=101（方向）值=0

--- 停止 StopAsync() / 故障复位 ResetAsync()（两条完全相同）---
  02 06 00 64 00 00 C8 26   | FC=0x06 地址=100（启停）值=0

--- 设速度 SetSpeedAsync(250) ---
  02 06 00 66 00 FA E9 A5   | FC=0x06 地址=102（速度）值=250

--- 连接验活（ConnectAsync 内部）---
  02 03 00 66 00 01 64 26   | FC=0x03 读 102 起 1 个寄存器
```

**与 §二 语义逐条对照**：

| 动作 | 代码发出 | 含义 | 结论 |
|---|---|---|---|
| 正转 | `100←1` | 启动 | ✅ |
| 反转 | `101←0` | 方向 = 反转 | ✅ 与你的说法一字不差 |
| 停止 | `100←0` | 停止 | ✅ 只需这一条 |
| 故障复位 | `100←0` | 停止 | ✅ |
| 调速 | `102←值` | 设速度 | ✅ |

**每个动作只发一条报文**——"先停另一边"那两条（`ForwardAsync` 里的 `101←0`、`ReverseAsync` 里的 `100←0`）
已按你的说明删掉（2026-09-18）。改前/改后：

| | 改前 | 改后 |
|---|---|---|
| 正转 | `101←0` → `100←1` | **`100←1`** |
| 反转 | `100←0` → `101←0` | **`101←0`** |
| 停止 | `100←0` | `100←0`（没变） |

其余结论：

1. **功能码固定 `0x06`**（写单个保持寄存器），不是 `0x10` 写多个；8 字节帧 = 站号 + 功能码 + 地址(2) + 值(2) + CRC(2)。
2. **速度寄存器 102 不参与正反转**：换向/停止/复位都不写它，只有滑杆写。
3. 一次动作若将来又要发多条报文，仍会被 `_gate` 排在一起、中间不被别的帧插队（现在用不上了，锁保留）。

### 5.3 实测中发现并修掉的问题

1. **并发串包（真 bug）**：见 §四.1，`SemaphoreSlim` 修掉。
2. **`Timer` 二义性 CS0104**：项目开了 ImplicitUsings，`System.Threading.Timer` 撞车 → 字段写全名。
3. **「加载参数」按钮被挤出可视区**（既有布局问题）：底部 `flpBtns` 只有 136 宽且 AutoSize，第二个按钮换行后掉出面板 → 改 `AutoSize=false` + 宽 280 + 两按钮并排。

---

## 六、现场待办

1. **接真变频器**：电机真的起停、转速目视跟随（虚拟从站只验证了协议与流程）。
   **顺序**：先空载 → 先只按「反转」（`101←0`）看带子是否真的反转 → 再按「停止」（`100←0`）→ 最后再测正转与调速。
2. **故障复位语义**：当前=只写 `100←0`（停止）；若变频器有专用复位寄存器，只改 `ConveyorSettings.ResetAsync` 一处。
3. **调速量程**：滑杆 0–1000 → 0–100%（`/10`）是据设计稿 `txtCurSpeed=30.0`+滑杆300 推的，现场确认。
4. **串口被占用**：提示文案已写好，但"被别的软件占用"这条现象还没实测。
5. **方向字已按你的说明改完（2026-09-18）**：`ForwardAsync` = `100←1`、`ReverseAsync` = `101←0`、`StopAsync` = `100←0`，
   "先停另一边"那两条已删；报文级验证见 §5.4。**不需要**为换向加"先停/死区延时"（这台传送带没这要求）。
   剩下的只有第 7 条那个**状态回读**要你定。
6. **断开连接 / 关闭页面不会写停止（安全项）**：`Disconnect()` 只关串口，**不写 0**。
   如果当时正在转，关掉软件后带子会**继续转**。要不要在断开/退出前先 `StopAsync()`（写不进去也继续关）？
7. **只剩「状态回读」要你定**：`ReadRunStateAsync` 现在还是旧判断
   `100≠0 → "正转"；101≠0 → "反转"；否则 "停止"`（一次读 100/101 两个寄存器）。按新的写入语义，两种可选判定：
   - **甲**（100=启停、101=方向）：`100=0 → "停止"；否则 101≠0 → "正转"、101=0 → "反转"`
   - **乙**（100=正转启停、101=反转启停且 0=运行）：`100≠0 → "正转"；否则 101=0 → "反转"；否则 "停止"`

   现场接上变频器后**分别只按一次「反转」和「停止」**，看从站/设备里 100、101 各变成什么值，就能定下来；定完我改这两行。

---

## 七、历史（一句话）

v1 曾按 Modbus **TCP**（IP/端口）设计，后来你给出传送带示例代码（串口 RTU），方案整体改为 RTU 并作废手写 TCP 客户端；界面随之 RTU 化。自测用的"反射注入 socket 传输层"这一测试缝已在收尾时删除（如需重新做无硬件回环测试，加回 3 行即可）。
