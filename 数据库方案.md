# 数据库（MySQL）· 实施方案（v1.1：**已落盘 + 自测通过**）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms + VisionPro 9.0 CR2）
> 页面：`Views/SysConfigView.cs` → `grpDb`「◆ 数据库配置」 + `Views/DataLogView.cs`「数据日志」 + `Views/MainRunView.cs`（只定写入契约）
> 决策：**数据库用 MySQL**（你拍板）；随后 **Q1–Q12 全部按推荐 A**（"全用默认挺好的，改方案"）。
> 状态：**三步已一次做完并自测通过**（2026-09-17，编译 0 error / 0 warning）。
> **实测记录、抓到并修掉的问题、与方案的偏差统一写在 §十七**；下面是当初的设计稿，未逐条回改，保真留档。
> 现场要看的部分：§十三 现场验证清单 + §十七.9（多了一条 TLS 相关的坑）。

---

## 一、现状基线（已核对，含本机实测）

| 事实 | 证据 |
|---|---|
| 日志页**只有布局，没有逻辑** | `DataLogView.cs` 17 行（仅 `InitializeComponent()`）；`DataLogView.Designer.cs` 447 行 |
| 日志页控件已按草稿定死，**列名可直接映射** | `dgvLog` 七列 `DataPropertyName`：`Time` / `WorkpieceNo` / `RecipeID` / `Result` / `Score` / `ImagePath` / `Note`；查询条 `dtpFrom`/`dtpTo`/`cmbResult`；按钮 `btnQuery`/`btnClear`/`btnExport`；四张统计卡 `lblSumTotal`/`lblSumOK`/`lblSumNG`/`lblSumRate`；提示文案「万级记录后台导出，不锁界面」 |
| 设置页 DB 面板**只有控件，没有处理函数** | `cmbDbType`（下拉项＝**SQLite / SQL Server / PostgreSQL**，**没有 MySQL**）、`txtConnStr`（默认 `Data Source=D:\VisionData\db\vision.db`）、`btnTestConn`（测试连接）、`btnInitDb`（初始化建表）——后两个**零处理函数**；`txtConnStr_TextChanged` 是空方法 |
| 但 DB 组**已经在落盘** | `SysConfigView.Modbus.cs:160-163` 存 `db.cmbDbType` + `db.txtConnStr`；`:198-207` 读回。所以本期只改"存什么"，不新增落盘机制 |
| 全项目 **0 行数据库代码、0 个数据库包** | `Services\` 下无任何 Db 文件；csproj 无 PackageReference；仅已引用框架程序集 `System.Data` |
| 包源是**故意清空的** | `NuGet.Config`：`<packageSources><clear /></packageSources>` —— 第三方库一律走 `Libs\` + `HintPath`（NModbus 3.0.83 就是这么进来的：`VisionSort\Libs\NModbus.dll`） |
| 主运行页有**工件编号与四个统计位** | `MainRunView.Designer.cs`：`cmbRecipe`（标签写「工件编号」）、`lblTotal`/`lblOKCount`/`lblNGCount`/`lblRate`、状态链文案「等待工件 → 光电触发 → 停止传送带 → 拍照检测 → 机械臂分拣 → 恢复传送带」 |
| **本机装了 MySQL 8.0.44 且服务在跑** | 服务 `MySQL80` Running；`127.0.0.1:3306` 通；`C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqld.exe`（54 MB）与 `mysql.exe` 都在。**但 `mysql -u root` 免密登录被拒**（`ERROR 1045`），我不知道口令 |
| 可**免口令**起一个一次性实例做自测 | `mysqld --initialize-insecure` + 独立 datadir + **端口 3307（本机空闲）**；不碰现有 `MySQL80` 服务与 `C:\ProgramData\MySQL` 数据 |
| 本机**装了 Excel** | `C:\Program Files\Microsoft Office\root\Office16\EXCEL.EXE` → 导出的 xlsx 可以用 Excel COM **真打开**验证，不必靠"应该没问题" |
| 本会话 shell **不能直接下 NuGet**，Node 可以 | `Invoke-WebRequest`/`curl` 一律 `SSL connection could not be established` / schannel `SEC_E_NO_CREDENTIALS`（本沙箱）；`node fetch` 走环境代理 `127.0.0.1:7892` **成功**。已用 Node 把下面的依赖真实下载并解包核对过（§附录 A） |

---

## 二、范围

**本期做**

1. MySQL 驱动入库（离线可部署）+ 数据层（连接 / 建表 / 插入 / 分页查询 / 统计 / 流式游标）
2. 设置页 DB 面板真正可用：测试连接、初始化建表、字段校验、随「保存参数 / 加载参数」落盘
3. 日志页：按时间+结果查询、分页、四张统计卡、后台导出 Excel
4. 给主运行页定"写一行结果"的**契约**（`ResultRepository.Enqueue(...)`），主运行页本身**不在本期**

**本期不做**（明确留口，避免以后扯皮）

- 配方管理页 / 配方表（`RecipeID` 本期恒 0）
- 用户权限、审计、多机并发写入的协调
- 历史数据定时清理 / 归档 / 分区（万级~百万级对 MySQL 不算事，先不做）
- 报表设计器、图表、趋势分析
- 双击日志行打开对应图像（留升级口，列里已有 `ImagePath`）
- 主运行页的运行编排（另立方案）

---

## 三、驱动选型（Q1）

| 方案 | 对 net48 的依赖（实测，见附录 A） | 评价 |
|---|---|---|
| **甲 MySqlConnector 2.4.0**（MIT，社区维护，`mysqlconnector.net`） | **10 个 DLL ≈ 1.47 MB**（清单见 §四） | **推荐**。真异步（不卡 UI）、支持 MySQL 8 的 `caching_sha2_password`、支持 MariaDB、无原生 DLL（不需要 `SQLite.Interop` 那种位数坑）、版本与现场 MySQL 8.0.44 同代 |
| 乙 官方 `MySql.Data` 8.0.33 | 更重：`Google.Protobuf` + `BouncyCastle.Cryptography` + `K4os.Compression.LZ4.Streams` + `System.Configuration.ConfigurationManager` + `System.Text.Encoding.CodePages` + … 约 12+ 个 DLL，其中还有原生/加密栈 | 不推荐。离线入库体积与出问题的面都大得多，且官方驱动的 net48 支持一向滞后 |
| 丙 `MySql.Data` 6.9.12（老版） | **单 DLL 零依赖** | 不推荐。**不支持 `caching_sha2_password`**——MySQL 8.0 默认就是它，必须让现场把账号改成 `mysql_native_password`（8.0 已标记废弃、8.4 已移除）。为了省 9 个 DLL 把风险推给现场不值 |
| 丁 Cognex `Cognex.VisionPro.Database` 工具（csproj 已引用） | 0（框架内） | 不推荐。它走 ODBC/OLE DB，现场要多装 MySQL ODBC 驱动，且与我们的表结构/查询解耦不了，只适合"QuickBuild 里配一下"的场景 |

**结论：甲 MySqlConnector 2.4.0。**

---

## 四、依赖落盘（Q2）

沿用 NModbus 的老套路：**文件引用**，不恢复包源。

```
VisionSort\Libs\mysql\
    MySqlConnector.dll                                      (net48)  818.9 KB
    Microsoft.Extensions.Logging.Abstractions.dll            (net462)  66.3 KB
    Microsoft.Extensions.DependencyInjection.Abstractions.dll(net462)  62.3 KB
    Microsoft.Bcl.AsyncInterfaces.dll                        (net462)  26.3 KB
    System.Diagnostics.DiagnosticSource.dll                  (net462) 184.7 KB
    System.Threading.Tasks.Extensions.dll                    (net461)  25.4 KB
    System.Memory.dll                                        (net461) 138.9 KB
    System.Buffers.dll                                       (net461)  20.4 KB
    System.Numerics.Vectors.dll                              (net46)  113.1 KB
    System.Runtime.CompilerServices.Unsafe.dll               (net461)  17.6 KB
                                                            合计 ≈ 1.47 MB
```

csproj 侧：每个 DLL 一条 `<Reference>` + `HintPath`，并 `<Private>true</Private>` 保证拷进 `bin`；另加框架程序集 `<Reference Include="System.Transactions" />`（MySqlConnector 的框架依赖）。

**必须一起做的两件事（否则运行时才炸）**

1. **绑定重定向**：这 9 个 `System.*` / `Microsoft.Extensions.*` 都是强命名且版本敏感。VisionPro / NModbus 若引用了别的版本，.NET Framework 会抛 `FileLoadException`。落盘时用 `AssemblyName.GetAssemblyName()` 读出每个 DLL 的真实版本号，往 `VisionSort.exe.config`（现 611 字节）写 `oldVersion="0.0.0.0-<新版本>"` 的 redirect。**这一步我会实测后再写进实测记录，不靠猜。**
2. **拷贝规则**：`Libs\mysql\*.dll` 要能进输出目录（`<None Include="Libs\mysql\*.dll"><CopyToOutputDirectory>PreserveNewest</...>` 或 Reference 的 `Private`），并确认现场部署脚本会带上。

> 为什么不用 `<PackageReference>`：包源被 `NuGet.Config` 清空是为了**现场离线**不卡 restore；一旦引入 PackageReference，restore 就依赖 `%USERPROFILE%\.nuget\packages` 缓存或联网，和现有约定冲突。文件引用是唯一和 NModbus 一致的形态。

---

## 五、连接与配置（Q4 / Q5 / Q6）

### 5.1 默认连接串

```
Server=127.0.0.1;Port=3306;Database=vision_sort;Uid=vision;Pwd=CHANGE_ME;CharSet=utf8mb4;SslMode=None;AllowPublicKeyRetrieval=True;ConnectionTimeout=3;DefaultCommandTimeout=5;Pooling=True;MaximumPoolSize=4
```

> ⚠ **这里相对原稿改过一处（v1.1 实测后改的，理由见 §十七.7 问题①）**：原稿是 `SslMode=Preferred`，实测在 .NET Framework 上连不通（schannel 报"指定的值在 SslProtocolType 枚举中无效"；限死 `TlsVersion=Tls12` 后又报"安全包中没有可用的凭证"），改成 **`SslMode=None`** 一次通过。`SslMode=None` 下 caching_sha2_password 仍会取服务端 RSA 公钥加密口令交换，**不是明文发口令**。现场若强制 TLS：改回 `SslMode=Preferred;TlsVersion=Tls12`，并在该机器上实测能否协商。

- `SslMode=None` + `AllowPublicKeyRetrieval=True`：**MySQL 8 用 `caching_sha2_password` 时必须有后者**，否则报 "Authentication method 'caching_sha2_password' failed"。这两个是实测唯一跑通的组合。
- `CharSet=utf8mb4`：中文/生僻字/emoji 全要（工件编号可能有中文）。
- `ConnectionTimeout=3` / `DefaultCommandTimeout=5`：**不许把生产节拍挂在数据库上**。

### 5.2 口令怎么存（Q4）

| 方案 | 说明 |
|---|---|
| **甲 明文存 `config\sysparam.json`**（推荐） | 与现有 `modbus` / `robot` / `vision` 三组完全一致，最简；文档里明确写"该文件含数据库口令，注意工控机权限" |
| 乙 DPAPI 加密（`ProtectedData`，net48 内置） | 多约 20 行，机器+用户绑定，**换机器/换用户要重填**；现场克隆系统会失效 |
| 丙 不存口令 | 每次启动手填，生产上不现实 |

### 5.3 控件形态（Q5）

| 方案 | 说明 |
|---|---|
| **甲 保持"连接字符串"单框**（推荐） | 现有 `txtConnStr` 不动，只改默认值 + 标签补一句示例；改动最小，且连接串本来就该让人能手改 |
| 乙 拆成 主机 / 端口 / 库名 / 账号 / 口令 5 个控件 | 更好填、口令框可 `PasswordChar` 掩码；但要改 Designer（`tlpDb` 行数 3→6），且要写"拼/拆连接串"的双向逻辑 |

### 5.4 类型下拉（Q6）

现有三项 `SQLite / SQL Server / PostgreSQL` 里**没有 MySQL**。三选一：

| 方案 | 说明 |
|---|---|
| **甲 只留 "MySQL" 一项**（推荐） | 既然定了 MySQL，就不留假选项；以后要扩展再加 |
| 乙 保留四项、非 MySQL 项置灰 | 视觉上保留"将来可扩展"的暗示，但置灰项容易让人以为坏了 |
| 丙 直接删掉下拉，标签与连接串框占满一行 | 最干净，但以后加库种要动 Designer |

> 注意：`db.cmbDbType` 最初是**按下标**存的。改下拉项会让老 `sysparam.json` 的"0"从 SQLite 变成 MySQL —— 本期正好是从 SQLite 换 MySQL，**语义上是对的**；落地时已改为**存类型名**（`"MySQL"`），并保留"读不动就按下标当老格式"的兼容分支（见 `SysConfigView.Modbus.cs` 的 `btnLoadParam_Click`）。

---

## 六、表结构（Q7）

### 6.1 库与表（软件"初始化建表"执行的 SQL）

```sql
-- 库建议手工建（见 Q9），软件只建表：
-- CREATE DATABASE IF NOT EXISTS vision_sort DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

CREATE TABLE IF NOT EXISTS vision_result (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  uuid         CHAR(36)        NOT NULL                COMMENT '客户端幂等键：断网补写不重复',
  ts           DATETIME(3)     NOT NULL                COMMENT '检测时刻（客户端本地时间）',
  workpiece_no VARCHAR(64)     NOT NULL DEFAULT ''     COMMENT '工件编号 / 批次（主运行页「工件编号」输入框）',
  recipe_id    INT             NOT NULL DEFAULT 0      COMMENT '配方号，本期恒 0',
  result       TINYINT         NOT NULL                COMMENT '1=OK，0=NG',
  score        DECIMAL(6,5)        NULL                COMMENT 'PMA 匹配分数 0~1，未检出为 NULL',
  image_path   VARCHAR(260)    NOT NULL DEFAULT ''     COMMENT '图像文件，相对图像根目录',
  note         VARCHAR(255)    NOT NULL DEFAULT ''     COMMENT '备注：机械臂异常 / 检测超时 / 存图失败 / 补写标记',
  PRIMARY KEY (id),
  UNIQUE KEY uk_uuid (uuid),
  KEY ix_ts (ts),
  KEY ix_result_ts (result, ts)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='检测结果';
```

### 6.2 与日志页七列/主运行页的映射（逐字段交代来源）

| 表字段 | 日志页列 | 来源 | 备注 |
|---|---|---|---|
| `ts` | 时间 | 主运行页判定完成时刻 `DateTime.Now` | `DATETIME(3)` 毫秒足够；不存时区 |
| `workpiece_no` | 工件编号 | `MainRunView.cmbRecipe.Text` | 下拉可取可手输，空串允许 |
| `recipe_id` | 配方 | 本期恒 `0`（无配方管理） | 留列，将来加配方表时不用改表 |
| `result` | 检测结果 | `PMA 分数 >= VisionRunSettings.Shared.MatchThreshold` → 1，否则 0 | **阈值取"系统设置页"那一份**（Q2=A 的职责分离：系统设置页＝生产判定阈值，视觉配置页＝训练阈值），主运行页每轮 `AcceptThreshold = v.MatchThreshold` 后再 Run |
| `score` | 匹配分数 | PMA `MatchScore` | `DECIMAL(6,5)` 而不是 `FLOAT`：避免 0.6999999 这种显示/比较抖动 |
| `image_path` | 图像路径 | 存图时算出的路径，**存相对图像根目录的相对路径** | 相对路径换机/换盘可迁移；绝对路径一旦改盘符就全断 |
| `note` | 备注 | 机械臂异常、检测超时、补写标记等 | 正常轮次空串 |

### 6.3 为什么单表（Q7 的推荐理由）

日志页要的全部信息（时间/编号/结果/分数/图/备注）**都挂在"一次检测"这一件事上**，一对一；分表只会在查询时多一次 JOIN 而没有任何收益。两种备选：

- **乙 单表 + 日汇总表**：查询更快，但要维护汇总（补写、跨零点、修正都要同步），本期数据量（万级/年）完全不需要。
- **丙 主表 + NG 瑕疵明细表**：只有"一次检测出多个缺陷、每个缺陷要单独记录位置"时才值；PMA 只给一个匹配分数，**本期没有数据源**，做了就是空表。若以后加了 Blob/缺陷工具，再加从表，主表不用动。

---

## 七、数据层设计（新文件）

| 文件 | 职责 | 预估 |
|---|---|---|
| `Services/DbSettings.cs` | 共享单例 `DbSettings.Shared`（与 `ConveyorSettings` / `VisionRunSettings` / `RobotArmSettings` 同构）：`DbType`、`ConnectionString`、`ConnectTimeoutSec`、`CommandTimeoutSec`、`AutoRetrySec`、`SchemaChecked` | ~70 行 |
| `Services/ResultRecord.cs` | 行模型：`Uuid`/`Time`/`WorkpieceNo`/`RecipeId`/`IsOk`/`Score`/`ImagePath`/`Note`；`Result` 属性返回 `"OK"`/`"NG"` 供 `dgvLog` 绑定 | ~50 行 |
| `Services/ResultRepository.cs` | 共享单例 `ResultRepository.Shared`。`TestConnectionAsync()`（版本+库+表+行数）、`EnsureSchemaAsync()`、`Enqueue(record)`、`InsertAsync(record)`、`QueryAsync(filter,page,size)`、`SummaryAsync(filter)`、`StreamAsync(filter)`（导出用流式游标）、`FlushPendingAsync()`（补写） | ~330 行 |
| `Services/PendingStore.cs` | 断网补写的本地 JSONL（`config\pending_results.jsonl`）：追加、逐行读、成功后重写 | ~90 行 |

**线程与连接**（这是上次 Modbus 那课的直接延续）：`MySqlConnection` **不是线程安全的**。策略：

- 每次操作 `using (var conn = new MySqlConnection(cs))`，靠驱动自带的**连接池**兜底（`Pooling=True;MaximumPoolSize=4`）；
- 补写线程与 UI 线程可能同时插 → 补写用独立连接，**不与 UI 共用连接对象**；
- 需要串行的地方（补写与实时插入争同一张表）不需要锁：MySQL 自己保证，唯一键保证幂等。

**异常与提示**：所有异常在仓储层转成 `ResultRepository` 的中文消息（"连不上数据库：…（3 秒超时）" / "认证失败：检查账号或口令" / "库不存在：vision_sort"），**弹栈不弹给操作工**，与现有相机/机械臂的提示风格一致。

---

## 八、写库策略（Q8）

| 方案 | 行为 | 代价 |
|---|---|---|
| **甲 后台队列 + 本地补写**（推荐） | 主运行页调用 `Enqueue(record)` **立即返回**；后台线程批量 `INSERT`（每 20 条或每 1 秒 flush 一次，多值 INSERT）；失败则写 `pending_results.jsonl`，之后每 5 秒重试，成功即删；断网→复网自动补齐，`uuid` 唯一键保证不重复 | 多约 150 行（`PendingStore` + flush 循环） |
| 乙 同步直写、失败跳过并提示 | 每轮检测同步 `INSERT`（超时 5 秒），失败弹一次提示、本轮结果丢弃 | 最简（约 +30 行），但**数据库一抖就真丢数据**，而且每轮节拍被 DB 拽住 |

推荐甲的三个具体理由：① 生产节拍由相机+机械臂决定，不该由数据库决定；② 工控机与 MySQL 之间断网/重启在现场太常见；③ 有了 `uuid` 幂等，"重试"是安全的，而"丢弃"是不可逆的。

**队列写入时机**：**一轮完整动作结束后**（分拣完成/失败都已知道）再 `Enqueue`，这样 `note` 能带上机械臂结果；队列是异步的，不影响节拍。若该轮"检测超时/未检出"，也写一行（`result=0`, `score=NULL`, `note=超时原因`），**不丢事件**。

---

## 九、设置页 DB 面板（改动清单）

| 控件 | 现在 | 改成 |
|---|---|---|
| `cmbDbType` | 三项，无 MySQL | 按 Q6 定 |
| `txtConnStr` | SQLite 文件路径 | MySQL 默认连接串（§5.1） |
| `btnTestConn` | **无处理函数** | `TestConnectionAsync()`：连接 → `SELECT VERSION()` → 库在不在 → 表在不在 → 行数；结果用中文弹窗（成功/失败都给可执行的下一步） |
| `btnInitDb` | **无处理函数** | `EnsureSchemaAsync()`：执行 §6.1 的 `CREATE TABLE IF NOT EXISTS`；库不存在时给出建库 SQL 让操作工去执行（或按 Q9 自动建库） |
| `txtConnStr_TextChanged` | 空方法 | 轻校验：非空、含 `Server=`/`Database=`，把"格式可疑"显示在提示上（不弹窗）；或按你意见删掉这个空方法 |
| 保存/加载参数 | 已存 `db` 组 | 只改默认值来源：`DbSettings.Shared` 进 `PushUiToShared()` / `PushXxxToUi()` 那条链（与 modbus/robot/vision 完全同构） |

**安全细节**：任何把连接串打进日志/提示的地方都要**打码口令**（`Pwd=***`），避免弹窗截图外泄。

---

## 十、日志页设计（Q10 / Q11）

### 10.1 查询与统计

- 查询条件：`dtpFrom` / `dtpTo`（默认今天 00:00 ~ 现在）+ `cmbResult`（全部 / OK / NG）
- 取数：`WHERE ts BETWEEN @from AND @to [AND result=@r] ORDER BY ts DESC LIMIT @size OFFSET @off`
- **统计四卡与列表用同一个 WHERE**（一条 SQL 出四个数：`COUNT(*)`、`SUM(result=1)`、`SUM(result=0)`、`SUM(result=1)/COUNT(*)`），保证"卡片和表格永远对得上"
- 分页（Q11）：甲 每页 200 行 + 上一页/下一页/跳到第 N 页（推荐）／乙 时间范围限死、一次最多装 5000 行／丙 `VirtualMode` 全量虚拟滚动（最顺滑、代码最多）
- 主运行页那四个统计位是**本次运行的会话统计**（来自 `MainRunView` 自己的计数器），与日志页的**库内统计**是两套口径，文档里写明，避免以后被当成 bug

### 10.2 导出（Q10）

| 方案 | 依赖 | 说明 |
|---|---|---|
| **甲 手写 xlsx**（`System.IO.Compression.ZipArchive` + 内联字符串，net48 内置） | **0** | 推荐。xlsx 本质是 zip：`[Content_Types].xml` + `_rels/.rels` + `xl/workbook.xml` + `xl/_rels/workbook.xml.rels` + `xl/worksheets/sheet1.xml`，扁平表用 `t="inlineStr"` 就能写，Excel 正常打开。约 90 行；**本机有 Excel，自测用 COM 真打开校验**（不是"我觉得能开"）。时间列默认写成文本，若要"Excel 里能按时间排序/筛选"，加 `styles.xml` 多约 20 行 |
| 乙 `DocumentFormat.OpenXml` 3.0.2（官方，MIT） | +3 个 DLL ≈ **6.7 MB**（`DocumentFormat.OpenXml.dll` 6.18 MB + `Framework.dll` 462 KB + `System.IO.Packaging.dll` 47 KB） | 最"正统"，自测可用 `OpenXmlValidator` 校验；代价是主程序凭空大 6.7 MB（比整个 MySQL 驱动还大 4 倍） |
| 丙 CSV（UTF-8 **带 BOM**） | **0** | 十行代码，Excel 双击能开；但按钮叫"导出Excel日志"，给出 `.csv` 与界面文案不符 |

- **NPOI 已否决**：2.7.1（net472 组）实测拖 **10 个**依赖（含 `SixLabors.ImageSharp`、`MathNet.Numerics`、`BouncyCastle`、`System.Security.Cryptography.Pkcs`…），其中 ImageSharp 2.1.8 的许可证不是纯 OSS；2.5.6（net45）虽只拖 `SharpZipLib` + `Portable.BouncyCastle`，但版本太老且 BouncyCastle 对写 xlsx 毫无用处。
- 导出在**后台线程**跑：流式游标边读边写 zip 条目，不把万级记录全读进内存；带进度与取消，不锁界面（对齐 Designer 里那句"万级记录后台导出，不锁界面"）。
- 导出范围：**当前查询条件命中的全部记录**（不受分页限制），文件名 `检测日志_20260917_1620.xlsx`，默认落在图像根目录同级或让用户选（按现有"选择路径"习惯）。
- 表头用中文（时间/工件编号/配方/检测结果/匹配分数/图像路径/备注），列宽写死在 `sheet1.xml` 的 `<cols>`。

---

## 十一、与主运行页的契约（本期只定接口，不实现）

```csharp
// 主运行页每轮结束（含失败）调用一次，异步、不阻塞节拍
ResultRepository.Shared.Enqueue(new ResultRecord {
    Time        = DateTime.Now,
    WorkpieceNo = cmbRecipe.Text.Trim(),
    RecipeId    = 0,
    IsOk        = score >= VisionRunSettings.Shared.MatchThreshold,
    Score       = score,                 // 未检出 = null
    ImagePath   = relativeImagePath,     // 图像保存配置的根目录 + 文件名
    Note        = note                   // 机械臂超时 / 检测超时 / "" 
});
```

- 阈值**只读** `VisionRunSettings.Shared.MatchThreshold`（系统设置页），与视觉配置页的训练阈值互不写穿（沿用 Q2=A）。
- 图像路径由"图像保存配置"那期补的存图方法产出（该方法已按你的决定删掉，等主运行页开工时再加回来，接口就是这条 `ImagePath`）。
- 主运行页另外要在每轮 Run 之前设 `_pmaTool.RunParams.AcceptThreshold = v.MatchThreshold`。

---

## 十二、自测与验收（本机可做，含真实 MySQL 与真实 Excel）

| # | 项 | 手段 | 通过标准 |
|---|---|---|---|
| T1 | 编译 | VS MSBuild `x64 Debug` | 0 error / 0 warning |
| T2 | 驱动能加载 | 探针 `Assembly.Load` + `new MySqlConnection(cs)` | 10 个 DLL 全部解析成功（验证绑定重定向正确） |
| T3 | 一次性实例 | `mysqld --initialize-insecure --datadir=_tmp\mysql8test\data --port=3307 --console` | 起来、能连、`SELECT VERSION()` = 8.0.44 |
| T4 | 建表 | `EnsureSchemaAsync()` | 表结构、索引、字符集与 §6.1 一致（`SHOW CREATE TABLE` 对拍） |
| T5 | 插入/查询/统计/分页 | 造 1234 行（含中文工件编号、emoji、NULL 分数、超长备注截断） | 分页边界（第 1/末/越界页）、四卡数字与 SQL 手算一致 |
| T6 | 幂等 | 同 `uuid` 插两次 | 第二次不产生新行、不抛异常 |
| T7 | 断网补写 | 中途 `Disconnect`（关掉实例或改错端口）→ 继续 Enqueue → 恢复 | JSONL 有记录、恢复后补写成功、`SELECT COUNT(*)` 对上、无重复 |
| T8 | 超时不当机 | 指向 10.255.255.1:3306 | 3 秒内返回中文提示，UI 不卡（探针计时） |
| T9 | 认证失败提示 | 错口令 | 提示"认证失败"，不是英文异常 |
| T10 | 导出 xlsx | 导 1234 行 | `Excel COM` 打开、读 `A1`/`A1235`、列数=7、（可选）`OpenXmlValidator` 通过 |
| T11 | 界面探针 | 探针驱动真实 `VisionSort.exe`：设置页 测试连接/初始化建表 + 日志页 查询/分页/统计/导出 | 控件状态与提示文字符合预期；对话框有人应答（沿用上次的对话框看门狗） |
| T12 | 收尾 | 停 3307 实例、删 datadir、删探针与 `bin\config` 产物 | `_tmp` 与 `bin` 干净 |

> 一次性实例的生命周期：**起来之前先记下"这不是你的 MySQL80"**，测试结束 `mysqladmin -P 3307 shutdown` 后删除 `_tmp\mysql8test`；全程不碰 3306 与 `C:\ProgramData\MySQL`。

---

## 十三、现场验证清单（本机测不到的部分）

1. **MySQL 版本与账号插件**：`SELECT VERSION(); SELECT user,host,plugin FROM mysql.user;` —— 若是 `caching_sha2_password`（8.0 默认），确认连接串带 `AllowPublicKeyRetrieval=True`；若是 8.4+，`mysql_native_password` 已不存在，只能用 MySqlConnector/MySql.Data 8.x 这条路（本方案已满足）。
2. **远程连接**：MySQL 在另一台机器时，`bind-address`、账号的 `host` 是否允许工控机 IP、防火墙 3306。**现场常见坑**：账号是 `'vision'@'localhost'`，从工控机连必然失败。
3. **字符集**：`SHOW VARIABLES LIKE 'character_set_%';` 确认服务端默认不是 `latin1`；我们的表显式 `utf8mb4`，但连接串也带了 `CharSet=utf8mb4`。
4. **时区**：我们存**客户端本地时间**。要确认 MySQL 服务器的 `time_zone`（若以后有人用 `NOW()` 就会与我们的 `ts` 差几小时）。
5. **断网演练**：拔网线/停服务，跑 20 个工件 → 恢复 → 日志页能看到这 20 条（补写生效）。
6. **导出在目标机打开**：现场 Office/WPS 版本打开 xlsx 无警告。
7. **权限**：账号至少有 `SELECT/INSERT`；"初始化建表"需要 `CREATE`；建库需要 `CREATE` on `*.*`（按 Q9 定）。
8. **磁盘与备份**：`D:\VisionData` 与 MySQL datadir 分盘/同盘的决定；万级记录 + 图像的实际增长速度。

---

## 十四、落盘顺序（分三步，每步独立验收）

1. **数据层**（不碰 UI）：`Libs\mysql\` 10 个 DLL + csproj 引用 + 绑定重定向 + `DbSettings` / `ResultRecord` / `ResultRepository` / `PendingStore` → 用一次性实例跑 **T1–T10**
2. **设置页**：`Views\SysConfigView.Db.cs`（新）+ Designer 按 Q5/Q6 微调 + `db` 组接入 `Shared` → **T11 的设置页部分**
3. **日志页**：`Views\DataLogView.cs`（拆 `DataLogView.Query.cs` / `DataLogView.Export.cs`）→ **T11 的日志页部分 + T10 的 COM 校验**

> ⚠️ **错峰**：第 1、2 步要改 `VisionSort.csproj` 与 `SysConfigView.Designer.cs`，而**机械臂 v3.0 那一轮正在改同一批文件**（16:20 还在写）。建议等那轮收尾（编译通过 + 文档更新）我这边再开始第 1 步，避免两边互相覆盖。

---

## 十五、需要你拍板（Q1–Q12，回 A/B/C 即可）

| # | 问题 | 选项（**＝推荐**） |
|---|---|---|
| **Q1** | 驱动 | **A MySqlConnector 2.4.0** ／ B 官方 MySql.Data 8.0.33（12+ DLL）／ C MySql.Data 6.9.12（单 DLL，要求账号改 mysql_native_password） |
| **Q2** | 依赖怎么进项目 | **A `Libs\mysql\` 10 个 DLL + HintPath（与 NModbus 一致）** ／ B 恢复 nuget.org 包源改用 PackageReference |
| **Q3** | 我怎么自测真库 | **A 我起一次性 mysqld 实例（端口 3307，测完删，不碰你的 MySQL80）** ／ B 你给我一个 3306 上的测试账号口令 ／ C 只测编译与失败路径，真库留现场 |
| **Q4** | 口令存储 | **A 明文进 `sysparam.json`（与现有三组一致）** ／ B DPAPI 加密（换机失效）／ C 不存，每次手填 |
| **Q5** | 连接串控件 | **A 保持单框，只改默认值** ／ B 拆 5 个控件（口令可掩码，要改 Designer） |
| **Q6** | 类型下拉 | **A 只留 "MySQL"** ／ B 保留四项、非 MySQL 置灰 ／ C 删掉下拉 |
| **Q7** | 表结构 | **A 单表 `vision_result`** ／ B 单表 + 日汇总表 ／ C 主表 + NG 瑕疵明细表 |
| **Q8** | 写库策略 | **A 后台队列 + 本地 JSONL 补写（断网不丢）** ／ B 同步直写、失败跳过并提示 |
| **Q9** | 建表方式 | **A 软件建表（库先手工建；另给一份 .sql 备份脚本）** ／ B 只给 .sql，软件不建表 ／ C 软件连库一起建（账号需要更高权限） |
| **Q10** | 导出 Excel | **A 手写 xlsx（0 依赖，本机 Excel COM 验证）** ／ B 官方 OpenXml SDK（+6.7 MB）／ C CSV（UTF-8 BOM） |
| **Q11** | 日志页取数 | **A 分页 200 行/页** ／ B 时间范围内一次最多 5000 行 ／ C VirtualMode 全量虚拟滚动 |
| **Q12** | 现场 MySQL 在哪 | **A 工控机本机装 MySQL** ／ B 局域网服务器（要给我 IP/库名/账号）／ C 还没定，先按 A 出默认值 |

---

## 十六、我代为决定的项（不否决就照此执行）

- 表/连接统一 `utf8mb4` + `utf8mb4_general_ci`；时间 `DATETIME(3)`、存客户端本地时间；分数 `DECIMAL(6,5)`
- 幂等键 `uuid CHAR(36)` + `UNIQUE`；主键仍用自增 `BIGINT`（聚簇索引局部性）
- 图像路径**存相对图像根目录的相对路径**
- 统计四卡**跟随当前查询条件**；导出**当前条件命中的全部记录**（不受分页限制）
- 主运行页统计（会话）与日志页统计（库内）**两套口径**，互不覆盖
- 连接池开启、`MaximumPoolSize=4`、连接超时 3 秒、命令超时 5 秒
- 补写文件位置 `config\pending_results.jsonl`，UTF-8（无 BOM），每行一条 JSON
- 提示文案一律中文、不弹英文栈；连接串在日志/提示中**口令打码**
- 不做：清理/归档/分区、权限与审计、配方管理、报表设计器、双击开图

---

## 十七、实测记录（v1.1，已落盘 + 自测通过）

> 2026-09-17。**Q1–Q12 全部按推荐 A** 定稿（你："全用默认挺好的，改方案"）。
> 三步（数据层 → 设置页 → 日志页）一次做完；编译 **0 error / 0 warning**；
> 数据层 **24/24**、界面 **21/21**、导出文件用 **真实 Excel COM 打开**校验通过；探针与一次性 MySQL 已清理干净。

### 17.1 落盘清单（实际）

| 文件 | 内容 | 行数 |
|---|---|---|
| `VisionSort\Libs\mysql\*.dll` | MySqlConnector 2.4.0 + 9 个依赖，10 个 / 1.44 MB（§17.2） | — |
| `Services\DbSettings.cs` | 共享单例、默认连接串、口令打码、补写路径（自测可指走） | 94 |
| `Services\ResultRecord.cs` | 行模型；属性名与 `dgvLog` 的 `DataPropertyName` 逐字一致 | 47 |
| `Services\PendingStore.cs` | 断网补写 JSONL（手写读写，零依赖；含转义/坏行跳过/重写） | 297 |
| `Services\ResultRepository.cs` | 连接、自检、建表、入队+后台泵、批量插、补写、分页查询、统计、流式读、中文错误映射 | 681 |
| `Services\XlsxWriter.cs` | 手写 xlsx（`ZipArchive` + `inlineStr`，冻结表头、列宽、XML 转义） | 249 |
| `Views\SysConfigView.Db.cs` | 连接串校验、测试连接、初始化建表、双向同步（与 modbus/robot/vision 同构） | 116 |
| `Views\DataLogView.Query.cs` | 查询、分页、统计卡、OK/NG 着色、运行时分页条 | 214 |
| `Views\DataLogView.Export.cs` | 后台流式导出 + 进度回显 | 130 |
| 改 `VisionSort.csproj` | 10 条驱动引用 + `System.Transactions` + `System.IO.Compression` | +81 |
| 改 `SysConfigView.Designer.cs` | 下拉只留 MySQL、连接串默认值、`Leave` 绑定、**两个按钮补 `Click` 绑定**、删空 `TextChanged` | ±9 |
| 改 `SysConfigView.Modbus.cs` | `OnLoad`/`PushUiToShared` 各挂一次；db 组改存**类型名**（兼容老下标） | +14 |
| 改 `DataLogView.cs` | 构造函数里 `WireLogEvents()`（Designer 里按钮本就没绑事件） | +9 |
| 改 `SysConfigView.cs` | 删掉空的 `txtConnStr_TextChanged` | −5 |

新增逻辑代码合计约 **1 828 行**（不含 Designer）。

### 17.2 依赖闭包（实测后入库，可离线部署）

| 程序集（`Libs\mysql\`） | 取自 | 程序集版本 | 大小 |
|---|---|---|---|
| MySqlConnector.dll | net48 | 2.0.0.0 | 818.9 KB |
| Microsoft.Extensions.Logging.Abstractions.dll | net462 | 8.0.0.2 | 66.3 KB |
| Microsoft.Extensions.DependencyInjection.Abstractions.dll | net462 | 8.0.0.2 | 62.3 KB |
| Microsoft.Bcl.AsyncInterfaces.dll | net462 | 8.0.0.0 | 26.3 KB |
| System.Diagnostics.DiagnosticSource.dll | net462 | 8.0.0.1 | 184.7 KB |
| System.Threading.Tasks.Extensions.dll | net461 | 4.2.0.1 | 25.4 KB |
| System.Memory.dll | net461 | 4.0.1.2 | 138.9 KB |
| System.Buffers.dll | net461 | 4.0.3.0 | 20.4 KB |
| System.Numerics.Vectors.dll | net46 | 4.1.4.0 | 113.1 KB |
| System.Runtime.CompilerServices.Unsafe.dll | net461 | 6.0.0.0 | 17.6 KB |

**绑定重定向是必需的，但不用手写**：`<AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>`（工程里本来就有）
在构建时自动生成了 `System.Runtime.CompilerServices.Unsafe` 的 `0.0.0.0-6.0.0.0 → 6.0.0.0` 重定向（`System.Memory` 要 4.0.4.1、我们带的是 6.0.0.0）。
这一点被实测**验证过**：探针第一次跑挂在 `FileLoadException: System.Runtime.CompilerServices.Unsafe, Version=4.0.4.1`，把 `VisionSort.exe.config` 的重定向块给了探针自己的 config 之后立刻通过。

### 17.3 自测环境（真实，不是 mock）

- **一次性 MySQL 8.0.44**：`mysqld --no-defaults --initialize-insecure --datadir=_tmp\mysql8test\data --port=3307 --mysqlx=0 --console`。
  测完 `mysqladmin shutdown`（日志里是 `Normal shutdown`）+ 删数据目录。**全程没碰你的 `MySQL80`（3306）**，测后它的状态仍是 Running。
- 账号 `vision`@`%` 用 **`caching_sha2_password`**（MySQL 8 默认插件）——验的是真实认证路径，不是 native_password 的偏门。
- 探针：`probe_db.exe`（数据层 24 项）、`probe_ui.exe`（真实 `SysConfigView`/`DataLogView` + 对话框看门狗，21 项），
  编译进真实 `bin`、引用真实 `VisionSort.exe`；跑完**探针、探针 config、`bin\config`、一次性实例全部删除**。
- Excel：`C:\Program Files\Microsoft Office\root\Office16\EXCEL.EXE` 用 COM **真打开**导出的 xlsx。

### 17.4 数据层自测（24 项，全过）

| 项 | 结果 |
|---|---|
| T2 驱动加载 | MySqlConnector 2.0.0.0 解析成功（`d33d3e53aa5f8c92`） |
| T2b 口令打码 | `SafeConnectionString` 里 `Pwd=***`，原文口令不出现 |
| T3 连接自检（表不存在） | 版本 8.0.44 / 库 vision_sort / 表不存在 → 中文提示"请点初始化建表" |
| T4 初始化建表 | `SHOW CREATE TABLE` 与方案 §6.1 **逐字段一致**：`uk_uuid` / `ix_ts` / `ix_result_ts`、`utf8mb4_general_ci`、InnoDB |
| T5a 批量插入 1234 行 | 写入 1234，**283–332 ms** |
| T5b/T5c 计数与统计卡 | 总 1234 / OK 925 / NG 309 / 率 74.96%，OK、NG 过滤各自对上 |
| T5d 分页 | 第1页 200、第7页 34、越界 0，倒序正确 |
| T5e 中文与 emoji | `🔧齿轮-0007` 原样往返 |
| T5f 超长备注 | 300 字 → 入库截断 255 字 |
| T5g NULL 分数 | `Score=NULL`、`Result=NG` 正确 |
| T5h DECIMAL 精度 | 0.52 原样读回（没有浮点抖） |
| T6 幂等 | 同 uuid 重插受影响行 0，库内仍是 1234 |
| T7a 库不可达 → 本地补写 | 写库 0、`pending=5`、提示"连不上数据库服务器…（Pwd=***）" |
| T7b 恢复后补写 | 补写 5、pending 归零、库内 1239 |
| T7b2 重试间隔后自动补写 | 不手工调用，靠后台泵自己补上（pending 3 → 0，库内 1242） |
| T7c 补写后不重复 | 再跑一轮写入 0 |
| T7d 后台泵自动落库 | 入队 2 条后 2.6 秒内自动落库（库内 1244） |
| T8 不可达地址 | `10.255.255.1` → **3.0 秒**返回中文提示，界面不卡 |
| T9a 认证失败 | "认证失败：账号或口令不对。（…Pwd=***）" |
| T9b 库不存在（低权限→1044） | "打不开数据库「no_such_db」：库不存在…或账号没有该库的权限" |
| T9b2 库不存在（root→1049） | 同上（两种错误码同一句话，见 §17.7 问题③） |
| T9c TLS 失败 | 给出"改 `SslMode=None` / 加 `TlsVersion=Tls12`"的可执行建议（见 §17.7 问题①） |
| T9d 非法表名 | `vision_result; DROP TABLE x` 被拒，不会拼进 DDL |

### 17.5 界面自测（21 项，全过）

设置页：U1 控件找齐 / U2 下拉只有 MySQL 且 OnLoad 已推值 / U3 默认值是 MySQL 串 / U4 失焦写入 Shared /
U5 非法串警告并回退 / **U6 测试连接弹"连接成功。服务器版本：8.0.44 / 数据库：vision_sort / 结果表：vision_result（1244 行）"** /
U7 按钮名与可用性恢复 / U8 初始化建表（幂等，表存在=True）/ U9 保存参数写出 `db` 组（`"cmbDbType": "MySQL"` + 连接串）/
U10 加载参数把连接串还原。

日志页：U11 打开即静默查询（`第 1/7 页 · 共 1244 条 · 每页 200 行`）/ U12 表格 200 行 / U13 统计卡与库内同口径（1244/932/312）/
U14 倒序 / U15 时间列毫秒格式 / U16 **分页条与表格不重叠**（分页条 top=609、表格 bottom=609）/ U17 下一页（第 2/7，上一页变可用）/
U18 NG 过滤 + 统计同 WHERE（共 312 条，全部为 NG）/ U19 清空查询回到全部 / U20 导出落盘 + 完成弹窗（44 775 B，共 1 244 条）/
U21 导出后按钮恢复。

> U20 顺带验证了 `SaveFileDialog` 那条真实路径：看门狗把路径填进对话框并按保存，导出真的写到了指定文件。

### 17.6 导出的 xlsx 用 Excel 真打开（最硬的一条）

| 检查 | 结果 |
|---|---|
| 工作表名 | `检测日志` |
| 使用区域 | **1245 行 × 7 列**（表头 + 1244 条） |
| 表头 | `时间 / 工件编号 / 配方 / 检测结果 / 匹配分数 / 图像路径 / 备注` |
| 第 2 行 | `2026-09-17 10:00:00.000` / `工件-0000` / `NG` / 分数**空白**（NULL 正确落空） |
| 第 3 行分数 | `0.51`，且 `Value2` 类型是 **Double**（是数字不是文本，能在 Excel 里排序求和） |
| 末行时间 | `2026-09-17 10:20:33.000`（与库里最新一条一致） |
| emoji | `🔧齿轮-0007` 在 B9，Excel 里正常显示 |

### 17.7 实测抓到并修掉的 6 个真问题

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| ① | `SslMode=Preferred` 连不上：先是"指定的值在 SslProtocolType 枚举中无效"，限死 `Tls12` 后又是"安全包中没有可用的凭证"（`SEC_E_NO_CREDENTIALS`，和本机 `curl`/`Invoke-WebRequest` 同一个 schannel 故障） | MySqlConnector 在 .NET Framework 上默认会带 TLS 1.3；本机 schannel 不认 | 默认改 **`SslMode=None`**（配 `AllowPublicKeyRetrieval=True`，口令走 RSA 公钥加密交换，不是明文）；并把这类失败单独映射成"改连接串"的中文建议 |
| ② | 查询分页抛 `InvalidCastException: 无法将类型为 System.Guid 的对象强制转换为 System.String` | `uuid` 列是 `CHAR(36)`，MySqlConnector 默认把它当 `Guid`（`GuidFormat=Char36`），`GetString` 不行 | `ReadRecord` 改按 `object` 取再转字符串（Guid→`ToString("D")`） |
| ③ | 库名写错时提示"账号没有访问该库的权限"——把操作工引向错误方向（现场最多的是库名拼错） | 低权限账号连不存在的库时，MySQL 8 返回 **1044**（不是 1049），避免泄露库是否存在 | 1044/1049 合并成一句话，并把**库名打进提示**；自测同时覆盖 1044（vision 账号）与 1049（root） |
| ④ | 库不可达/连接超时时，提示落到兜底的英文 `Unable to connect to any of the specified MySQL hosts.` / `Connect Timeout expired.` | MySqlConnector 这两处的 `ErrorCode` 不稳定（有时 0、有时 1042），按类型分支漏了 | 加"按报文文本"判定，统一成"连不上数据库服务器：确认服务已启动、端口正确、防火墙放行" |
| ⑤ | 点「测试连接」「初始化建表」毫无反应 | **Designer 里这两个按钮根本没绑 `Click`**（草稿生成的设计器只建控件不绑事件——日志页的查询/导出按钮也是同样问题） | 补 `Click` 绑定；日志页按钮在构造函数里 `WireLogEvents()` 接上。**这是 U6 抓出来的**，纯靠读代码看不出"没反应" |
| ⑥ | `sysparam.json` 里 `"cmbDbType": "0"`，人看不懂，而且下拉项一变含义就变（SQLite 那三项换 MySQL 这一项时正好踩上） | 存的是下拉下标 | 改存**类型名**（`"MySQL"`）；加载时先按名字找，找不到再按老格式下标解析（兼容已有文件） |

### 17.8 与方案的偏差（4 处，都有理由）

| 偏差 | 方案原本 | 实际 | 理由 |
|---|---|---|---|
| 分页条 | 没写在哪 | **运行时代码构建**（`BuildPager()`），没进 Designer | `SysConfigView.Designer.cs` 那轮正被机械臂 v3.0 同时改，少动一处少一处冲突；分页条本身没有设计期价值。已用 U16 断言布局不重叠 |
| 导出默认目录 | "图像根目录同级或让用户选" | **桌面**（`SaveFileDialog` 仍由用户选） | 图像保存根目录没对外暴露"当前值"（该配置按你的决定不落盘、只活在视觉配置页控件里）。等主运行页开工把它提成共享设置再改 |
| 导出取消 | §10.2 写了"带进度与取消" | **只有进度，没有取消** | 草稿布局里没有取消按钮，加一个要动 Designer；导出 1244 行实测瞬间完成，先不做 |
| 流式读 API | `StreamAsync(filter)` | `WriteRowsAsync(from,to,filter,onRow)` | 实现时发现"回调式"更贴合导出（写一行丢一行），名字也直说"会写出去"；连接超时/命令超时没做成独立配置字段——连接超时在连接串 `ConnectionTimeout` 里，避免两个真相源 |

### 17.9 现场验证清单（在 §十三 之上补三条）

1. **TLS**：现场 MySQL 若开了强制 TLS（`require_secure_transport=ON`），我们的 `SslMode=None` 会被服务器拒绝，报 `SSL connection is required`。
   此时把连接串改成 `SslMode=Preferred;TlsVersion=Tls12`，**并在那台机器上实测能否协商**（本机 schannel 协商不了，见 §17.7 ①）。
2. **账号权限**：`vision` 账号至少要 `SELECT/INSERT`；点「初始化建表」需要 `CREATE`。低权限账号看到的报错统一是
   "打不开数据库「…」：库不存在，或账号没有该库的权限"——先核对库名，再核对授权。
3. **时区**：`ts` 存的是**客户端本地时间**，与 MySQL 服务器的 `time_zone` 无关。现场若有人用 `NOW()` 直查，会差几小时。
4. 其余照旧：`bind-address`/防火墙、字符集 `utf8mb4`、断网演练（拔网线跑 20 个工件 → 恢复后日志页能看到这 20 条）、导出在目标机 Office/WPS 打开、磁盘与增长。

### 17.10 交给主运行页的接口（最终形态）

```csharp
// 每轮结束（含失败）调用一次：立即返回，不阻塞节拍；首次调用自动起后台泵
ResultRepository.Shared.Enqueue(new ResultRecord
{
    Time        = DateTime.Now,
    WorkpieceNo = cmbRecipe.Text.Trim(),
    RecipeID    = 0,                                     // 无配方管理，本期恒 0
    IsOk        = score >= VisionRunSettings.Shared.MatchThreshold,   // 阈值只认系统设置页那一份
    Score       = score,                                 // 未检出/超时 = null
    ImagePath   = relativeImagePath,                     // 相对图像根目录
    Note        = note                                   // 机械臂超时 / 检测超时 / ""
});

// 可选：想知道写没写成功 / 断网时看提示
ResultRepository.Shared.Written        // 累计落库行数
ResultRepository.Shared.QueuedCount    // 还在排队
ResultRepository.Shared.Pending.Count  // 断网暂存待补写
ResultRepository.Shared.LastError      // 最近一次失败的中文原因（成功后清空）
```

主运行页**不要**直接 `new MySqlConnection`，也不要自己重试——重试与幂等已经在数据层里（`INSERT IGNORE` + `uuid`）。

---

## 十八、生产库落定与踩坑记录（2026-09-17）

### 18.1 现场/开发机上的库已按下面这套建好并验通

| 项 | 值 |
|---|---|
| 库 | `vision_sort`（`utf8mb4` / `utf8mb4_general_ci`） |
| 表 | `vision_result`（9 列 + `uk_uuid` / `ix_ts` / `ix_result_ts`，InnoDB） |
| 账号 | `vision` / `***（现场自行设置，已脱敏）`（`caching_sha2_password`） |
| 位置 | 本机 MySQL 8.0.44（`MySQL80` 服务，`127.0.0.1:3306`） |
| 项目默认值 | **已写入** `Services\DbSettings.cs` 与 `SysConfigView.Designer.cs` 的连接串默认值（按 Q4=A，明文；也会进 `sysparam.json`） |

**验证记录（用应用账号 `vision` 真跑，不是 root）**：连接 → `vision@%` / 8.0.44 ✓；
`SHOW CREATE TABLE` 与 §6.1 设计逐字段一致 ✓；索引 PRIMARY/`uk_uuid`/`ix_ts`/`ix_result_ts` ✓；
插入中文工件号 + `score=0.87654` + 中文相对路径 + 中文备注 → 原样读回（utf8mb4 正常）→ 删除后 0 行 ✓。

### 18.2 重新初始化用的两个文件（都在 `04-WinForms\`）

| 文件 | 用途 |
|---|---|
| `数据库初始化.sql` | 常规版：在 MySQL Workbench 里用**管理员账号**执行（清 `vision_sort` + 建库 + 建 `vision` 账号 + 建表 + 自检） |
| `数据库初始化_跳过鉴权一键.sql` | 跳过鉴权模式专用版（开头多一句 `FLUSH PRIVILEGES`，因为跳过鉴权时 `CREATE USER` 必须先 FLUSH，且必须与建账号在**同一连接**里） |

### 18.3 本轮踩到的两个坑（都有代价，记下来）

| # | 坑 | 结论 |
|---|---|---|
| ① | **`mysqld --skip-grant-tables` 在 MySQL 8 会连带关掉 TCP**：日志 `port: 0` → `[ERROR] TCP/IP, --shared-memory, or --named-pipe should be configured on NT OS` → `Aborting`，而这个 abort 又踩到 InnoDB 断言（`trx0sys.cc:644`）崩了一次 | 重置/初始化**优先用 `--init-file`**：服务器正常启动（TCP 照样在），启动阶段以 root 身份执行 SQL 文件——不需要绕过鉴权、不需要 root 口令、不会触发那个 abort。脚本见 `_tmp\fix_db.ps1` |
| ② | 中文写进 `.ps1` 被 Windows PowerShell 按 GBK 读 → 变 `瀹屾垚銆?` 并把结尾引号吃掉，解析直接失败 | 交给用户执行的脚本**一律纯 ASCII**（含 SQL 里的 `COMMENT`），编码风险归零；中文说明留在 `.md` 里 |

> 另外记一条操作性的：`mysqld --skip-grant-tables` 起不来时**别慌**——它多半只是"没有可用传输方式"而 Aborting，数据目录没有损坏（正常启动会做一次崩溃恢复）。本轮实测 `net start MySQL80` 一次就恢复。

### 18.4 表里的 `COMMENT` 已恢复成中文

一开始为把编码风险清到零，`_tmp\init_vision.sql` 里的列注释写成了 ASCII 英文。
后来用**应用账号**重做格式化时验证了：`cmd /c "mysql … --default-character-set=utf8mb4 < xxx.sql"`
是**字节原样**传递的，中文注释完好无损 —— 所以 `_tmp\format_vision.sql` 用的是中文注释（与 §6.1 设计一致），
实测 `SHOW CREATE TABLE` 里九个 `COMMENT` 全部正常显示。列注释只是文档性质，改不改都不影响数据。

### 18.5 格式化 + 端到端链路验证（2026-09-17 追加）

**格式化**（应用账号 `vision` 就够，**不需要 root**）：

```
mysql -u vision --default-character-set=utf8mb4 -e "DROP DATABASE IF EXISTS vision_sort;"
mysql -u vision --default-character-set=utf8mb4 -e "CREATE DATABASE vision_sort DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"
cmd /c "mysql -u vision --default-character-set=utf8mb4 < _tmp\format_vision.sql"     # 建表（中文注释）
```

结果：**库级 `DROP`/`CREATE` 都成功**（说明 `GRANT ALL ON vision_sort.*` 覆盖库级操作）；
`SHOW CREATE TABLE` 与设计逐字段一致、九个中文注释完好、`utf8mb4_general_ci`、
四个索引（PRIMARY / `uk_uuid` / `ix_ts` / `ix_result_ts(result,ts)`）齐全；
最后 `TRUNCATE` 把自增归 1 —— **库干净、开箱可用**。

**端到端链路（之前欠的那条，现已补上）**：探针只调 `ResultRepository`，不碰相机/机械臂/传送带，
跑的就是应用自己的代码路径，**7/7 通过**：

| 项 | 结果 |
|---|---|
| E1 应用默认连接串连真库 | 连接成功。服务器版本 8.0.44，库 vision_sort，表 vision_result（0 行） |
| E2 `Enqueue` 不抛异常 | 排队=2、补写=0 |
| E3 后台泵写入**真库**（没落进补写） | 排队=0、补写=0、累计写入=2、无错误 |
| E4/E5 查回并核对内容 | `端到端-NG｜NG｜NULL｜…`、`端到端-OK-中文｜OK｜0.91234｜…` 全对（中文与 NULL 分数往返正确） |
| E6 行数 | 正好 +2 |
| E7 同 `uuid` 重复提交 | 行数不变（幂等生效） |

**结论：`Enqueue → 后台泵 → 真 MySQL` 这条链路通了**（此前只验过两端各自，从未在一次运行里串起来）。
验证用的两行随后已 `TRUNCATE` 清掉，库恢复为空（`rows_now=0`、`AUTO_INCREMENT=1`）。

---

## 十九、库 ↔ 代码 ↔ 文档 对齐复核（2026-09-17 19:50）

你的问题："`SysConfigView.Db.cs` 对齐了没？" 逐处对过一遍，**抓到 4 处不对齐，已全部收掉**。

### 19.1 对齐的（不用改）

| 检查点 | 结论 |
|---|---|
| `SysConfigView.Db.cs` 里的硬编码 | 没有。读写全走 `DbSettings.Shared`；唯一字面量是第 34 行报错示例里的 `Pwd=口令`（**故意不打真口令**） |
| 口令只出现在该出现的地方 | 提示/日志一律走 `ResultRepository.Describe` → `DbSettings.MaskPassword`（`ResultRepository.cs:604`） |
| `Db.cs` ↔ `Designer.cs:623` ↔ `DbSettings.cs:37` 三处连接串 | 同一个串、同一个口令，一致 |
| `cmbDbType` 只留 `MySQL` ↔ `DbType="MySQL"` ↔ `PushDbToUi` 回填 | 自洽（Q6=A） |
| 表名 `vision_result` | 代码 / 两个 `.sql` / 现网表 / 本文档，四处一致 |

### 19.2 抓到并修掉的 4 处不对齐

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| ① | `VisionSort\bin\x64\Debug\net48\config\sysparam.json` | `Pwd=` **是空的**。看时间戳就明白：该文件写于 **18:49:16**，而口令进 `DbSettings.cs` 是 **19:20:17**、进 `Designer.cs` 是 **19:20:40** —— 早 31 分钟，是早期 UI 探针（查 `btnTestConn` 没绑事件那一轮）点「保存参数」留下的陈旧件 | **已删除**。全盘只有这一份，且 build 不会从源码目录拷它；启动时也不读它（只有「加载参数」读），删掉是安全的 |
| ② | `ResultRepository.cs:158`、`数据库方案.md:148` | `workpiece_no` 注释是短的 `'工件编号 / 批次'`，两个 `.sql` 脚本是长的 `'工件编号 / 批次（主运行页「工件编号」输入框）'` | 统一成**长版**（以现网表为准） |
| ③ | `ResultRepository.cs:163`、`数据库方案.md:153` | `note` 注释代码里少一项：`'…检测超时 / 补写标记'` vs 脚本 `'…检测超时 / 存图失败 / 补写标记'` | 统一成**脚本版**（以现网表为准） |
| ④ | `数据库方案.md:98`（§5.1 默认连接串）、`:132`（§5.4） | §5.1 的"默认连接串"里 `Pwd=` 也是空的（与 ① 同源的老稿）；§5.4 末尾还写着"`db.cmbDbType` 是**按下标**存的"，而落地时已改成存**类型名**（代码里保留了下标兼容分支） | 两处都改成与代码一致 |

> ①④ 是同一次"口令还没定下来"留下的两处化石：**文件里的空口令**。①会真咬人（点「加载参数」就把无口令串灌进界面，再点「保存参数」就固化），④只是文档误导。

### 19.3 ②③ 的硬证据（不是"我看着一样"）

方法：把 `EnsureSchemaAsync()` 里那段 `ddl` **从 C# 源码原样抽出来**（正则还原字符串拼接），用它在真库里建一张临时表 `__align_check`，
然后 `SHOW CREATE TABLE` 把**临时表**和**现网 `vision_result`** 两段 DDL 都导出来，归一表名后做**逐字比较**：

```
=== 逐字比较：代码 DDL 建出的表 vs 现网 vision_result ===
MATCH  两段 SHOW CREATE TABLE 完全一致（表名归一后）

=== 收尾 ===
0                 ← vision_result 行数
vision_result     ← 库里表清单（临时表 __align_check 已 DROP 干净）
```

即：**"初始化建表"在新工控机上建出来的表，与现网那张表逐字相同**（字段、9 条中文注释、`utf8mb4_general_ci`、`PRIMARY(id)`/`uk_uuid`/`ix_ts`/`ix_result_ts(result,ts)`、表注释「检测结果」）。
抽 DDL 的临时文件已清理；`vision_sort` 库仍是干净的（0 行、`AUTO_INCREMENT=1`）。

### 19.4 复核后的构建

`MSBuild … /p:Configuration=Debug /p:Platform=x64` → **0 error / 0 warning**（改的只是两处注释文本）。

---

## 二十、P0：打包出去的程序连不上 MySQL —— 真因在 `App.config`（2026-09-17 GUI 实测抓到）

你问了一句"你测试了吗"。上一轮我只测了数据层（探针 + 真库），**没跑 GUI**。于是这次把 `VisionSort.exe` 真启起来、用鼠标真点。

### 20.1 症状

系统设置页点「测试连接」→ **失败**：

```
数据库 · 测试连接失败
“System.Transactions.Diagnostics.DiagnosticTrace”的类型初始值设定项引发异常。
```

而主运行页右上角当时写着「**库 正常**」。那句话来自 `MainRunView.Run.cs:344`：
`Pending.Count == 0 && string.IsNullOrEmpty(LastError) ? "正常" : "异常"` —— 它只是"**本进程内最近一次写库有没有报错**"，
**不是连通性检查**：新进程、一次库操作都没做过时，它**恒显示"正常"**。所以它在骗人。

### 20.2 三轮对照实验（把配置当唯一变量）

探针：`winexe`、x64、放在 `bin` 里跑（与应用同目录、同 DLL、同 `System.Transactions`），只改配置文件。

| 变体 | 配置文件 | `Transaction.Current` | `MySqlConnection.Open()` | `TestConnectionAsync` |
|---|---|---|---|---|
| A | `VisionSort.exe.config` **原样** | ❌ TypeInitializationException | ❌ 同一个异常 | `Ok=False` |
| B | 手写最小配置（只有 `startup`+`Unsafe` 重定向） | `null` | ✅ **OK 8.0.44** | `Ok=True` |
| D | 应用配置**只删掉那 4 行 `<configSections>`** | `null` | ✅ **OK 8.0.44** | `Ok=True` |

A 的内层异常就是真因（`ResultRepository.Describe` 只回 `ex.Message`，**内层被吞了**，所以界面看不到）：

```
System.TypeInitializationException: “System.Transactions.Diagnostics.DiagnosticTrace”的类型初始值设定项引发异常。
  inner: System.Configuration.ConfigurationErrorsException: 配置系统未能初始化
    inner: System.Configuration.ConfigurationErrorsException:
           “System.Windows.Forms.ApplicationConfigurationSection”节是内置节。它不能由用户声明。(VisionSort.exe.Config line 4)
```

因果链：
`<configSections>` 里声明了 .NET Framework 4.7+ 的**内置节** → **整个配置系统**初始化失败 →
`System.Transactions` 的静态构造抛异常 → MySqlConnector 每次 `Open()` 都要碰 `Transaction.Current` →
**连库必失败**。而我的探针一律用最小配置，所以"链路 7/7 通过"是**探针环境**的通过，不代表应用通过。

### 20.3 修法

`VisionSort\App.config` 删掉 `<configSections>…</configSections>`（4 行），**只留节本身**，并把原因写成注释钉在那里，防止以后有人"补回去"。
生成出来的 `bin\...\VisionSort.exe.config` 已确认不含该声明，`System.Runtime.CompilerServices.Unsafe` 重定向仍自动生成。

### 20.4 修完在真 GUI 里逐条验的（鼠标真点，不是探针）

| 步骤 | 实测结果 |
|---|---|
| 点「测试连接」 | **连接成功。服务器版本：8.0.44／数据库：vision_sort／结果表：vision_result（0 行）** |
| 点「保存参数」 | 参数已保存：`…\bin\x64\Debug\net48\config\sysparam.json` |
| 看那个 json 的内容 | `"txtConnStr": "…Uid=vision;Pwd=***;…"` ← **口令已落盘，不是空的**（§19.2 ① 那个坑反向验通） |
| 点「加载参数」 | 参数已加载。 |
| 加载完**再点「测试连接」** | 连接成功（8.0.44／vision_sort／0 行）← **保存→加载→再连** 全链路通 |
| 编译 | 0 error / 0 warning |

> §19.2 ① 里被我删掉的那个 `sysparam.json`，这次是**应用自己重新生成**的，内容正确。

### 20.5 同一个 bug 的第二个后果：DPI 设置也从来没生效过 —— 后来干脆把它去掉了

顺手量了进程 DPI 感知度（`GetProcessDpiAwareness` / 窗口级 `GetWindowDpiAwarenessContext`）：

| | 配置系统坏着时 | 修好 configSections 后 | **最终（去掉 DpiAwareness 段后）** |
|---|---|---|---|
| DpiAwareness | `Unaware`(0) | `SystemAware`(1) | `Unaware`(0) |

- `PerMonitorV2` 这个设置在配置系统坏掉时是**死设置**，一次都没生效；
- 但把它"修活"之后**反而更糟**：进程真感知 DPI 了，而本项目的窗体是绝对像素布局、WinForms 的 `AutoScaleMode.Font` 又把握手值改写成了当前字体度量（175% 机器实测 `Factor=1x1`）→ 窗口仍用设计像素、字体按 1.75 倍渲染 → **按钮文字撑破、全挤到一起**（你 2026-09-17 看到的现象）。
- 所以最终决定：**从 `App.config` 里去掉 DpiAwareness 段**，让进程回到 `Unaware`（Windows 位图放大：字略糊、比例自洽）。
  **与这次的 P0 无关**——P0 的修复是删掉非法的 `<configSections>`，去掉 DpiAwareness 后库照样连得上（实测库灯仍 `已连`）。
- 现场工控机大概率 100% 缩放，**两种做法在 100% 下没有任何区别**。
- 要真正支持高 DPI（清晰 + 不挤）的正确做法与实测数据见 `主运行页方案.md` §十九。

### 20.6 顺带记两条

- `vsp\App.config`（不在 `VisionSort.sln` 里的老工程）有**一模一样**的非法声明。它哪天当 WinForms 跑且要连库，会犯同一个错。
- 主运行页那句「库 正常/异常」的语义（§20.1）：**已定（2026-09-17）：改成"开机真连一次"**，取值、配色、正路/反路实测见 `主运行页方案.md` §十八。另外那句状态行原来把"库"放在**最后**、被面板裁掉（默认窗口下只显示到"工具 未加"），已挪到最前。

### 20.7 教训（写给下一个我）

上一轮我明明在文档里写过一句「copying `VisionSort.exe.config` verbatim fails: console host rejects the built-in `ApplicationConfigurationSection`」——
**那条线索指向的就是这个 P0**，我却把它归因成"探针宿主机的问题"，然后继续用最小配置的探针自证"链路通"。
**探针环境 ≠ 应用环境时，"探针通过"不能替代"应用通过"。有 GUI 的功能，必须在 GUI 里点一遍。**

---

## 附录 A：依赖闭包实测证据（2026-09-17 本机）

方法：用 Node `fetch` 从 `api.nuget.org` 拉 `.nuspec` 递归解析 **net48** 依赖组，并下载 `.nupkg` 解包核对 `lib/` 目录与文件大小。
（当时的下载与解析脚本放在 `_tmp\nugetcheck\`，属临时产物，**落盘后已删除**；成品 DLL 在 `VisionSort\Libs\mysql\`，见 §17.2。）

```
## mysqlconnector 2.4.0                    [组 .NETFramework4.8] -> lib/net48   818.9 KB
   框架程序集: System.Transactions
   依赖: Microsoft.Extensions.Logging.Abstractions 8.0.2 | System.Diagnostics.DiagnosticSource 8.0.1
         | System.Threading.Tasks.Extensions 4.5.4 | Microsoft.Extensions.DependencyInjection.Abstractions 8.0.2
## Microsoft.Extensions.Logging.Abstractions 8.0.2  -> lib/net462   66.3 KB
## Microsoft.Extensions.DependencyInjection.Abstractions 8.0.2 -> lib/net462  62.3 KB
## Microsoft.Bcl.AsyncInterfaces 8.0.0      -> lib/net462   26.3 KB
## System.Diagnostics.DiagnosticSource 8.0.1 -> lib/net462  184.7 KB
## System.Threading.Tasks.Extensions 4.5.4   -> lib/net461   25.4 KB
## System.Memory 4.5.5                       -> lib/net461  138.9 KB
## System.Buffers 4.5.1                      -> lib/net461   20.4 KB
## System.Numerics.Vectors 4.5.0             -> lib/net46   113.1 KB
## System.Runtime.CompilerServices.Unsafe 6.0.0 -> lib/net461  17.6 KB
```

对照（被否决项）：`NPOI 2.7.1` net472 组 **10 个依赖**；`NPOI 2.5.6` net45 组 2 个依赖；`DocumentFormat.OpenXml 3.0.2` net46 组 → `DocumentFormat.OpenXml.Framework 3.0.2` → `System.IO.Packaging 8.0.0`，三个 DLL 合计约 6.7 MB。

## 附录 B：被否决的方案（留档）

| 方案 | 否决理由 |
|---|---|
| 原草稿的 SQLite（`Data Source=D:\VisionData\db\vision.db`） | 你已改为 MySQL |
| `System.Data.SQLite` / `Microsoft.Data.Sqlite` | 同上；且它们带原生 `SQLite.Interop.dll`，x86/x64 位数坑与 VisionPro 的 x64 约束叠加 |
| MySql.Data 6.9.12 单 DLL | 不支持 MySQL 8 默认的 `caching_sha2_password` |
| 官方 MySql.Data 8.0.33 | 12+ 个依赖（含 Protobuf/BouncyCastle/LZ4 加密栈），离线部署面太大 |
| NPOI 导出 | 依赖过多（详见附录 A），且 ImageSharp 2.1.8 许可证非纯 OSS |
| `Cognex.VisionPro.Database` 工具 | 走 ODBC/OLE DB，现场要多装 MySQL ODBC 驱动，且表结构被工具绑死 |
| 空 `txtConnStr_TextChanged` 保留不动 | 留着就是个"看起来该有校验却什么也没做"的坑；按 Q5/Q6 一起收掉 |

