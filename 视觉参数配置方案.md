# 视觉参数配置 · 实施方案（v2.3，待审核）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms）
> 页面：`Views/SysConfigView.cs` → `grpVisionP`「◆ 视觉参数配置」
> 版本：v2.6（**两块都已实现并自测通过**）
> 决定：Q1=**A** 只做配置项；Q2=**A** 阈值职责分离（不写穿）；Q3=**A** 0–60000ms；Q4=相机硬触发（Auto，作为生产目标记录）；**Q5=做（按 A 方案：4 选项下拉，默认「自动探测」保持相机原配置）**；Q6=**C** 视觉配置页阈值下拉保留原样；两处阈值标签不加限定词

---

## 一、Q2 改回 A 带来的连锁改动（先说这个）

Q2=A（分离）与"Q6=删掉视觉配置页的阈值下拉"**互斥**：下拉一删，训练/自检就没有阈值入口了。所以：

| 项 | 上一版（Q2=B） | 本版（Q2=A） |
|---|---|---|
| `VisionRunSettings` 的阈值 | 属性 + 变更事件 | **普通字段**（不需要事件） |
| 改阈值时的写穿 | 立即写进方案里所有 PMA | **不写**，什么都不做（只更新共享值） |
| 加载 VPP 时 | 把共享值写进方案 | **不写**（方案保持自己的值） |
| `VisionConfigView` 的改动 | 订阅/退订事件 + 写穿方法（~26 行） | **0 行**（不需要动） |
| 视觉配置页阈值下拉（Q6） | 删掉（A） | **保留原样（C）** |

**职责边界（本版核心）**：

| 阈值 | 归属 | 用在哪 | 谁改 |
|---|---|---|---|
| 系统设置页 `txtThreshold`（0.70） | `VisionRunSettings.Shared.MatchThreshold` | **生产判定**（主运行页将来使用） | 系统设置页 |
| 视觉配置页 `cmbThreshold`（0.50–0.80） | `PmaTrainSettings.AcceptThreshold` | **训练 / 自检**（`Pma.cs` 写进 PMA 运行参数、自检打分） | 视觉配置页 |

两者**互不写对方**：训练页改阈值不会碰生产阈值，系统设置页改阈值也不会碰方案里的 PMA。两处标签文字**都保持原样**（你已决定不加限定词），区分靠本文档 §一 这张职责表和主运行页方案里的复述。

---

## 二、现状基线（已核对）

| 控件 | 默认值 | 标签 | 现有逻辑 |
|---|---|---|---|
| `txtThreshold` | `0.70` | 匹配分数阈值 | 仅被「保存/加载参数」写进 `sysparam.json` 的 `vision` 组 |
| `txtStopDelay` | `300` | 光电触发后停止延时(ms) | 同上 |
| `txtPhotoWait` | `100` | 拍照等待时间(ms) | 同上 |
| `txtTimeout` | `2000` | 检测超时(ms) | 同上 |

全仓 grep 确认：**没有校验、没有共享对象、没有消费者**；落盘已完成（`SysConfigView.Modbus.cs` 151–154 行），本次不重做。

两个前提：
1. **`MainRunView` 是空壳**（只有 Designer）→ 本期只做"配置 + 共享对象 + 契约"。
2. **相机现在是"自由运行"**（`VisionConfigView.Camera.cs` 连上后 `TriggerEnabled = false`），与 Q4 的生产触发方式不一致。

---

## 三、Q4 落实：触发时序与参数语义

自动触发（截图原文）：**不需要 `startAcquire`，也不软件触发；光电传感器每触发一次，相机采一张图**。软件侧"触发发生了"= **相机有新帧到达**。

```csharp
Acq.OwnedTriggerParams.TriggerEnabled = true;
Acq.OwnedTriggerParams.TriggerModel   = CogAcqTriggerModelConstants.Auto;
```
（与我相机那轮反射探针查到的可写成员完全一致。）

```
等待工件 →[光电硬触发相机]→ 相机出图 → 软件读到新帧
        →(停止延时)→ 发停带 →(拍照等待)→ 检测这张图 → 机械臂分拣 → 恢复传送带 → 等下一件
```

| 参数 | 语义（Q4 之后） | 计时起点 |
|---|---|---|
| 匹配分数阈值 | 生产 OK/NG 判据（主运行页用 `Shared` 的值） | — |
| 光电触发后停止延时 | 新帧到达到发"停带"指令的等待 | 新帧到达 |
| 拍照等待时间 | 停带后再等这么久（停稳/消抖）开始检测 | 发停带后 |
| 检测超时 | 开始检测到出结果的看门狗，超时判异常并停线 | 开始检测 |

> 诚实标注：`PhotoWait` 有两种读法——① 图在触发瞬间已采好，它只是"停稳后再检测"；② 停带停稳后才采图（那就不该用 Auto 硬触发）。按截图**按①实现**；现场若发现触发时刻工件未进视野，属机构/触发调整，回来改本文档。

---

## 四、设计（Q1=A：只做配置项）

### 4.1 新增 `Services/VisionRunSettings.cs`（约 40 行）

静态 `Shared` 单例（与 `ConveyorSettings.Shared` 同风格），**纯数据、无事件**：

```csharp
public double MatchThreshold = 0.70;   // 0 < x ≤ 1（生产判定）
public int StopDelayMs       = 300;    // 0–60000
public int PhotoWaitMs       = 100;    // 0–60000
public int DetectTimeoutMs   = 2000;   // 0–60000
```

### 4.2 新增 `Views/SysConfigView.Vision.cs`（约 95 行）

4 个框失焦校验（`0 < 阈值 ≤ 1`、时间 `0–60000` 整数；非法值弹提示 + 回退上次有效值，与 Modbus 那 6 个框同套路），合法值**即时写入 `Shared`**。仅此而已——不碰任何 PMA、不碰方案。

**「加载参数」时视觉组怎么进 `Shared`**（沿用 Modbus 组现有机制，`SysConfigView.Modbus.cs` 205–245 行）：
文件里的值先无条件写进输入框（`RestoreGroup` 不校验）→ `PushUiToShared()` 逐项校验，合法值收进 `Shared`、非法值**跳过**（`Shared` 保留旧值）→ 末尾再 `PushModbusToUi()` 把 `Shared` 的生效值推回界面（视觉组同理补一段 `PushVisionToUi()`）。
即：加载路径上是"界面 → Shared → 界面"一趟，作用是**校验后的回显纠偏**（否则界面会显示 `abc` 这种非法值、而 `Shared` 里是旧值，两边不一致）。
已知特性：非法值被**静默**忽略（用户不会被告知"文件里有个值没用上"）。若你希望改成"明确告知忽略了哪些字段"，约 +15 行，说一声即可（本期默认沿用现状，与 Modbus 组保持一致）。

### 4.3 消费契约（写给将来的主运行页，本期不实现）

```csharp
VisionRunSettings v = VisionRunSettings.Shared;

// 取图：按相机实际触发方式（现状=Manual 软件触发 → 调 Run()；将来 Auto 硬触发 → 等新帧、不调 Run()）
ICogImage frame = CaptureByTriggerMode();
Thread.Sleep(v.StopDelayMs);
await _conveyor.StopAsync();                    // ConveyorSettings.Shared
Thread.Sleep(v.PhotoWaitMs);

var watch = Stopwatch.StartNew();
_pmaTool.RunParams.AcceptThreshold = 0;                  // 固定 0：不让 PMA 过滤结果（2026-09-20 P0 修正，见 §十）
... PMA.Run() ...
if (watch.ElapsedMilliseconds > v.DetectTimeoutMs) { /* 超时：判异常 + 停线 + 报警复位灯 */ }
// 判定改由我们自己比：Accepted = 分数 ≥ VisionRunSettings.Shared.MatchThreshold（分工没变，方式变了，见 §十）

// 机械臂分拣：像素→mm 用 9 点标定 → 工作范围校验 → MoveJ 取料 / 吸 / MoveJ 放料
// 底层＝「独立项目 DobotArm → x86 桥接进程 DobotArmBridge.exe（宿主官方 DobotDll.dll）→ USB 串口」。
//   ⚠ 官方原生 DLL **只有 32 位**，与 x64 的 VisionPro 不能同进程，必须独立 x86 进程隔离；
//     "TCP 直连 + 不用原生 DLL"是 v1.4 的旧路线，v3.0 已改为**回环 UDP + 桥接进程**（《机械臂TCP控制方案.md》§一/§三）。
// 系统设置页配：串口号 / 桥接端口 / 抓取延时 / 复位延时 / 自动启动桥接进程。
// 设备调试页「九点标定」区配：待机位 / 放料点 / 抓取高度 Z / 工作范围。实现见 Services/RobotPickPlace.cs。
await _robot.MoveToAsync(pickXmm, pickYmm, pickZmm);
await _robot.SuctionAsync(true);  Thread.Sleep(RobotArmSettings.Shared.PickDelayMs);
... 放料 ...
await _conveyor.ForwardAsync();
```

> 关键点：生产阈值在**每次 Run 之前**从 `Shared` 取（不依赖加载时写穿）——这样系统设置页改了阈值，下一次检测就生效，与"分离"策略完全一致，也不需要任何事件。

### 4.4 视觉配置页阈值下拉保持原样（Q6=C）

`pnlThr`（`lblThr` "匹配阈值" + `cmbThreshold` 0.50–0.80）与 `Pma.cs` 407–422 全部不动；标签文字也不改（不加限定词）。

### 4.5 相机触发方式下拉（Q5=A，已实现）

先说清为什么不是"两选一"，而是**四选一**：复核 `VisionConfigView.Camera.cs` 发现它已被现场实测重写——连接时按 **保持相机原配置 → FreeRun+关触发 → Manual+开触发** 依次试取（现场那台 Hikrobot 是 **Manual 才出图**，注释原话："盲目改成 FreeRun 反而一帧不来"）。若把下拉做成"自动触发/自由运行"两选一并默认 Auto，就会**顶掉现场已可用的配方**。所以实现成：

| 下拉选项 | 行为 |
|---|---|
| **自动探测（保持相机原配置）** ← 默认 | **不写相机**；连接时按 原配置 → FreeRun → Manual 依次试取，哪种出图用哪种（=`ProbeOneFrame`，原逻辑不变） |
| 强制：自动触发（外部硬触发） | 写 `TriggerModel=Auto; TriggerEnabled=true`，仍试取一帧验证 |
| 强制：自由运行 | 写 `TriggerModel=FreeRun; TriggerEnabled=false`，同上 |
| 强制：手动（软件触发） | 写 `TriggerModel=Manual; TriggerEnabled=true`，同上 |

配套行为：

- **未连接时只记界面值**（与相机页其它参数同套路），连接时按它应用；
- **已连接时切换立即生效**：强制模式直接写相机；从强制切回「自动探测」→ 把相机**还原成连接时记下的原始触发配置**（`RememberOriginalTrigger`/`RestoreOriginalTrigger`）；
- **强制模式写失败不静默退回**：写相机抛错、或试取不到图 → 还原相机原配置 + 明确报错并提示"切回自动探测"（`ProbeWithForcedTrigger`），不猜用户意图；
- **Auto 模式下『单次拍照』先提示**：Auto 是外部硬触发，软件 `Run()` 取不到图（还会干等超时），所以按钮里先弹一句说明、直接返回；
- **状态标签 `lblTrigMode`** 保持"读回真实值"的定位，前缀改成 `实际：`，后缀 `· 原配置`/`· 已切换`（下拉占掉了原来"触发模式："的位置）；
- **布局**：`pnlTrig` 从 30px 加高到 38px（ComboBox 实际约 36px 高，30px 会被裁），与 `pnlPkt`/`pnlExp` 一致。

---

## 五、改动清单

| # | 文件 | 改动 | 行数 |
|---|---|---|---|
| 1 | `Services/VisionRunSettings.cs` | **新增**：4 参数单例（纯数据） | ~40 |
| 2 | `Views/SysConfigView.Vision.cs` | **新增**：4 个 Leave 校验 + 写 Shared | ~95 |
| 3 | `Views/SysConfigView.Designer.cs` | 4 个 `Leave` 绑定 | 4 |
| 4 | `Views/SysConfigView.Modbus.cs` | `PushUiToShared()` 补视觉组 | ~5 |
| 5 | `Views/VisionConfigView.Camera.cs` + `VisionConfigView.Designer.cs` | 触发方式 4 选项下拉（默认自动探测）+ `TriggerChoice` 枚举/写入 + 强制失败还原 + Auto 下拍照提示 + `pnlTrig` 加高 + 状态标签前缀改字 | ~110 / 15 |
| — | `Views/VisionConfigView.cs` / `.Pma.cs` / `VisionConfigView.Designer.cs` 的阈值下拉 / `csproj` | **不动** | 0 |

---

## 六、验收 / 自测

| # | 场景 | 期望 | 本机可测 |
|---|---|---|---|
| T1 | 编译（VS MSBuild x64/net48） | 0 error / 0 warning | ✅ |
| T2 | 默认值 | 0.70 / 300 / 100 / 2000，已进 `Shared` | ✅ |
| T3 | 阈值 `abc`/`-1`/`0`/`1.5` 失焦 | 弹提示 + 回退 | ✅ |
| T4 | 时间 `abc`/`-1`/`70000` 失焦 | 弹提示 + 回退 | ✅ |
| T5 | 合法改动（阈值 0.85 / 延时 500） | 无提示，`Shared` 即时同步 | ✅ |
| T6 | 保存 → 改乱 → 加载 | 界面与 `Shared` 一致 | ✅ |
| T7 | **分离验证①**：加载 VPP 后改系统设置页阈值 | 方案里 PMA 的 `AcceptThreshold` **不变**、视觉配置页下拉**不变** | ✅ |
| T8 | **分离验证②**：视觉配置页改下拉并训练自检 | 系统设置页阈值**不变**、`Shared.MatchThreshold` **不变** | ✅ |
| T9 | 相机触发下拉存在、默认"自动触发"、切"自由运行"不崩；无相机时连不上也不崩 | 下拉/默认/异常路径 | ⚠️ 仅 UI 与异常路径（**本机无相机**，写属性那步测不到） |
| T10 | 真机冒烟 + 布局渲染 | 启动无异常；页面与标签文字保持原样 | ✅ |

---

## 七、风险

| 风险 | 影响 | 规避 |
|---|---|---|
| 两处阈值被误当成同一个 | 调错地方、以为没生效 | 本文档 §一 职责表写死 + 主运行页方案里复述一遍（标签按你的决定不改） |
| 主运行页忘了用 `Shared` 的生产阈值 | 生产判定用了别的值 | §4.3 契约里写明"每次 Run 前从 Shared 取"；主运行页方案里再强调 |
| 相机触发方式（Auto/Manual）未定 | 主运行页取图方式写错 | **见 §九：现场实测那台相机是 Manual 才出图，与 Q4 截图假设的 Auto 冲突，等定** |
| `PhotoWait` 语义二义 | 现场检测不稳 | §三 已标注；属机构/触发调整 |

---

## 八、落盘顺序

1. ✅ `Services/VisionRunSettings.cs`；
2. ✅ `Views/SysConfigView.Vision.cs` + Designer 4 个绑定 + `SysConfigView.Modbus.cs` 补视觉组；
3. ✅ 相机触发方式下拉（4 选项，默认"自动探测"=不破坏现场配方）；
4. ✅ 编译 0 error/0 warning → T1–T10 自测 → 本文档更新为 v2.6（实测记录见 §九）。

---

## 九、实测记录（v2.4）

### 9.1 落盘内容（视觉参数部分）

| 文件 | 动作 | 说明 |
|---|---|---|
| `Services/VisionRunSettings.cs` | 新增（约 30 行） | 4 参数静态单例（纯数据、无事件）；注释里写明与 PMA 阈值的分工 |
| `Views/SysConfigView.Vision.cs` | 新增（约 110 行） | 4 个框失焦校验 + 即时写 Shared + `PushVisionToUi/PushVisionUiToShared`；复用 Modbus partial 的 `WarnAndRevert` |
| `Views/SysConfigView.Designer.cs` | 改 | 4 个 `Leave` 绑定 |
| `Views/SysConfigView.Modbus.cs` | 改 3 行 | `OnLoad` 加 `PushVisionToUi()`；`PushUiToShared()` 末尾加 `PushVisionUiToShared()` |

### 9.2 自测结果（探针驱动真实 `VisionSort.exe`；跑完即删）

| # | 场景 | 结果 |
|---|---|---|
| T1 | 编译（VS MSBuild x64/net48） | ✅ 0 error / 0 warning |
| T2 | 默认值 | ✅ 界面与 Shared 均为 0.70 / 300 / 100 / 2000 |
| T3 | 阈值 `abc` / `-1` / `0` / `1.5` 失焦 | ✅ 各弹 1 次提示，回退 0.70，Shared 不变 |
| T4 | 停止延时 `abc` / `-1` / `70000` 失焦 | ✅ 各弹 1 次提示，回退 300，Shared 不变 |
| T5 | 合法改动 0.85 / 500 / 250 / 3000 | ✅ 无弹框，Shared 即时同步 |
| T6 | 保存 → 改乱 → 加载 | ✅ 文件落盘，界面与 Shared 都恢复 0.85/500/250/3000 |
| T6b | **手改文件塞非法值（0.85→9.9）再加载** | ✅ 非法值被跳过、界面回显 0.85（"校验后回显纠偏"生效） |
| T7 | 分离验证①：系统设置页改阈值 | ✅ 本页无任何 PMA 引用，只写 Shared |
| T8 | 分离验证②：视觉页下拉改 0.60 | ✅ `Shared.MatchThreshold` 不受影响（仍 0.85） |
| T9 | 相机触发下拉 | ✅ 存在、4 选项、默认"自动探测"；未连接时切 4 个选项都不崩、不弹框（只记界面值）；未连接点『单次拍照』提示"相机未连接"；无相机点连接提示"未发现 GigE 相机"；布局渲染确认不裁切（见 §9.3） |
| T9b | 触发写入相机本身 | ⚠️ **本机无相机，写属性那步走不到**：`ApplyTriggerChoice`/强制试取/还原逻辑只能代码审查 + 现场验证 |
| T10 | 真机冒烟 | ✅ 启动 4 秒存活、无异常退出 |

自测中踩的坑（探针自身）：模拟失焦时用 `UserControl.Focus()` 当失焦目标 —— UserControl 拿不到焦点，`Leave` 根本不触发，导致首轮 T3/T4/T5 假失败。改成焦点移到同页另一个 TextBox 后全部通过（产品代码无问题）。

### 9.3 相机触发方式：实施中发现的冲突 → 按 A 方案落地

动手前复核 `VisionConfigView.Camera.cs`，发现它已被**现场实测**重写过（1059 行，含 `ProbeOneFrame` / `_triggerKeptAsIs` / `LiveDisplayOwnsFifo` / `TriggerModelName`，头部还记着现场机型 Hikrobot MV-CS060-10GC、曝光 5ms、包大小 1508、延迟 3 等实测值）：

- 注释原话：**"现场那台相机在 VisionPro 里是『手动』触发才出图，盲目改成 FreeRun 反而一帧不来"**；
- 连接流程：① 保持相机原配置 → ② FreeRun+关触发 → ③ Manual+开触发，哪种出图用哪种；
- 也就是说现状是 **Manual（软件触发 `Run()`）**，与 Q4 截图要求的 Auto（外部硬触发）不同。

**结论**：把下拉做成 **4 选项、默认"自动探测"**（§4.5）——不写相机、完全保留现场可用行为；只有你明确选"强制"时才写相机，且写失败会还原原配置并报错（不静默退回）。

**现场验证清单（本机无相机，必须现场做）**：
1. 默认"自动探测"连接 → 应与改动前完全一致（能出图、标签显示 `实际：手动（软件触发）· 触发开 · 原配置`）；
2. 选「强制：手动」连接 → 出图，标签显示 `· 已切换`；
3. 选「强制：自动触发」并**接上光电传感器** → 硬触发出图（这是生产的最终形态）；未接触发器时应报"取不到图"并还原原配置（不把相机留在半路状态）；
4. 连接状态下切回「自动探测」→ 相机被还原成原始触发配置（可用 VisionPro 的取像工具或标签值核对）。

---

## 十、2026-09-20 修正：`MatchThreshold` 的用法变了（全流程测试挖出的 P0）

**原来（Q2=A 的写法）**：`VisionRunSettings.MatchThreshold` 每轮写进 PMA 的 `RunParams.AcceptThreshold`，
判定读 VisionPro 给的 `CogPMAlignResult.Accepted`。

**问题（实测）**：`AcceptThreshold` 会把结果**从 `Results` 集合里滤掉** —— 同一张分数 0.9401 的图：

```
阈值 0.000 / 0.300 / 0.500 → Results=1，分数 0.9401
阈值 0.900 / 0.930 / 0.999 → Results=0，分数 0.0000     ← 0.94 的匹配也被滤掉
```

后果：**判 NG 时没有结果 → 没有像素坐标 → 机械臂不知道该去哪儿抓 → 分拣永远不动作**。
（判定 OK/NG 本身还是对的，坏的只是"NG 件要被抓走"这条主功能。）

**现在**：`Inspect` 固定用 `AcceptThreshold = 0` 跑，判定改由我们自己比：

| 输出 | 谁决定 |
|---|---|
| `Accepted`（OK/NG） | `分数 ≥ VisionRunSettings.MatchThreshold` ← **还认系统设置页那一份，分工没变** |
| `HasPixel`（要不要让机械臂去抓） | `分数 ≥ VisionRunSettings.PickScoreFloor`（新增，默认 **0.30**，实测"有工件 0.9401 / 没有任何工件 0.0000"） |

**这条修正不影响 Q2=A 的职责划分**：判定阈值仍然只有系统设置页那一份说了算，
视觉配置页那份依旧只管训练/自检。变的只是"用我们自己的比较代替 VisionPro 的过滤"。

> 详细实测与修法见 `主运行页方案.md` §二十一.3。

---

## 十一、2026-09-20 修正：「保存方案」另存为之后，生产该加载哪个方案

### 11.1 缺口（界面联动复查时发现）

`VisionScheme.VppPath` 是主运行页每次「开始检测」重新加载方案的依据
（`ProductionRunner.Start` 拿它做启动校验，`LoopAsync` 里 `VisionScheme.Load()` 用它），
但它在**全程序只有一处写入** —— 就是本页「加载VPP」那里。

于是「保存方案」（`btnSaveVpp_Click`）**只写文件、不动 `VppPath`**：

```
训练 →「保存方案」→ 另存为 Sort_B002.vpp
  ├─ 本页标签「当前VPP路径：…\Sort_B002.vpp」    ← 显示的是新路径
  └─ VisionScheme.VppPath 仍是 …\Sort_A001.vpp   ← 生产照样加载**旧那个文件**
```

这是"界面和事实不一致"里最坏的一种：**改也改了、存也存了，就是没生效**。
（覆盖保存原路径时看不出问题 —— 所以这个坑只在"另存为"时露头，平时很难撞上。）

### 11.2 修法

`VisionConfigView.cs`：把"存盘之后的簿记"抽成独立方法，由保存路径调用：

```csharp
CogSerializer.SaveObjectToFile(toSave, dlg.FileName);
ApplySavedVppPath(dlg.FileName);

private void ApplySavedVppPath(string path)
{
    if (string.IsNullOrEmpty(path)) return;
    Services.VisionScheme.Shared.VppPath = path;   // 生产下次加载它
    _vppPath = path;                               // 下次「保存方案」默认就是这个新名字
}
```

- 顺带更新 `_vppPath`：否则存成新名字后，再点一次「保存方案」对话框里默认还是旧名字。
- **空路径直接忽略**，不允许把正在生效的方案指成空 —— 那会让主运行页「开始检测」直接拒绝启动。
- 抽成独立方法不只是为了好看：`btnSaveVpp_Click` 中间有**模态** `SaveFileDialog`，探针点不到它；
  拆出来之后这段簿记可以被直接调用并断言。

### 11.3 实测（探针，本缺口 5 项断言全过，测完即删）

真 `VisionConfigView` 实例 + 反射调用：

| # | 断言 | 结果 |
|---|---|---|
| 1 | 探针能反射到 `ApplySavedVppPath` | ✅ |
| 2 | 探针能反射到 `_vppPath` | ✅ |
| 3 | 另存为后 `VisionScheme.VppPath` 指向新文件 | ✅ |
| 4 | `_vppPath` 同步更新（下次「保存方案」默认名正确） | ✅ |
| 5 | 传空路径被忽略，已有的生效指向不被破坏 | ✅ |

编译：Debug x64 / Release x64 均 **0 error / 0 warning**。

> 联动缺口 2（切回主运行页刷新状态行）见 `主运行页方案.md` §二十二；
> 两个缺口是**同一支定点探针**验的，合计 13 项断言全过。
>
> ⚠ **本节这条修正，全流程整机测试覆盖不到**：全流程测试（`主运行页方案.md` §二十三，39 项全过）
> 用的是一开始就加载好的 vpp，**不会去点「保存方案」**。所以缺口 1 只能由上面这支定点探针保证 ——
> 原因是这条路径本身就在生产流程之外，不是漏测。
