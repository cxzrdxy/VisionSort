# 九点标定操作指引

**VisionSort · 零件分拣机**

> 这份文件是**唯一真相源**：程序里设备调试页「操作指引」按钮弹出的就是这一份
> （以嵌入资源编译进 `VisionSort.exe`，改了本文重新编译程序里的字就跟着变）。
> 讲"为什么这么做"的是《九点标定联机分拣方案.md》，本文只讲"怎么做"。

---

## 这个东西是干什么的

相机看东西是用"像素"（图像上的第几行第几列），机械臂走路是用"毫米"（相对它自己的底座）。
九点标定就是**求出这两套坐标之间的换算关系**。

做完一次，以后每一件工件，程序自己把相机看到的位置换算成机械臂能走的位置，**人不用管**。
所以这个活是"做一次、用很久"，但**做错的代价很大**：程序不会报错，只会安静地抓偏。

## 什么时候要重做

- 第一次装机
- **相机、镜头、支架、焦距、光圈动过任何一个**
- 换产品、或者相机的高度/工作距离变了
- 机械臂的底座被挪过
- 平时：**每天开机做一次"反查验证"**（第五节），过了就不用重标

## 开始之前，确认三件事

1. 机械臂上电、指示灯**绿色**、USB 线插好；在 VisionSort 的**设备调试页**能点动它（X±/Y± 按钮按一下能动）。
2. VisionPro（QuickBuild）能打开生产方案，相机能出图；或者手头有一张清楚的标定图。
3. **记住一句话：标定的时候工件（或标定图上的面）的高度，必须和生产抓取时一样高。**
   九点标定是平面的，管不了高低；高度一变，换算出来的位置就不准。

---

## 一、在 VisionPro 里做九点标定（9 步）

**1.** 打开 QuickBuild，加载生产方案（一般是 `vpps\QuickBuild1.vpp`）。

**2.** 加标定工具：在 `CogToolBlock1` 里面加一把 **`CogCalibNPointToNPointTool`**
（工具箱里找"标定 / Calibration"，名字是 N 点标定 / N-Point）。

**3.** 给它一张图：把工具的 `InputImage` 设成相机的实时图（或一张清楚的图）。
图上要能看清你打算点的那九个位置。

**4.** 点第一个点：在工具的「点偶」表第 0 行，在图上点一下你的第一个位置。
**先放大再点** —— 手点一下的误差大约是 2～3 个像素，换算成毫米就是 0.4～0.6mm，放大能明显改善。

**5.** 填机械臂坐标：在 VisionSort 的**设备调试页**用 `X±` `Y±` 把机械臂**点动到这个点的正上方**，
回到 VisionSort 的标定表格，点「**读当前位姿 → 填到第 N 行**」。

> **不要手抄机械臂的数字** —— 抄错一个就整张标定歪掉。这半边的数字一律让程序读。

**6.** 重复第 4、5 步，直到九个点都填完。三条注意事项：

- **九个点要铺开**，覆盖工件可能出现的位置。九点标定的有效范围就是这九个点围出来的框，
  **框外面是猜的，误差涨得很快**。别九个点挤在画面正中间一小块。
- **九个点不要在一条直线上**（那算不出东西）。
- **顺序要一致**：第 i 个像素点对应第 i 个机械臂点，两边不能错位。
  最稳的做法是：先在图上把九个点按"从左到右、从上到下"点完，机械臂也按同样的路走一遍。

**7.** 点工具上的「**校正**」。表里会出现算好的结果。

**8.** 把数据抄回 VisionSort：九个「未校正 X/Y」（像素）和九个「原始的已校正 X/Y」（机械臂）
填进**设备调试页 → 九点标定**表格。

> 像素那两列：VisionPro 的表格**能复制就粘贴，不能复制就手填**。
> 机械臂那两列：一律用「读当前位姿」，别抄。

**9.** 点「**计算**」→ 看第二节的四个数 → 没问题就点「**保存**」。

---

## 二、算完必须看的四个数

| 数字 | 好的样子 | 不对怎么办 |
|---|---|---|
| **RMSE** | 越小越好，**小于 0.5mm** | 大了说明有点填错、高度不一致、或者手点太粗 |
| **mm/像素** | 和镜头视野对得上（比如 0.19 mm/px） | 差得离谱 → 九个点的顺序错位了 |
| **旋转** | 和相机装的方向差不了多少（几度以内） | 几十度 → 顺序配错、或者轴对错了 |
| **手性** | **✓（打勾）** | **✗ = 镜像**，程序会**拒绝保存**。检查 X/Y 有没有对错轴 |

四个数里有任何一个不对，**先别保存**，回第一节重来。

---

## 三、★ 最容易踩的坑：模板原点默认在 (0,0)

**现象**：标定数据看着全对，RMSE 也很小，但机械臂抓的位置**整个偏掉**，而且**程序不报任何错**。

**原因**：VisionPro 的定位工具（PMA）输出的那个"位置"，指的是**模板原点**在图像里的位置。
而模板原点的**默认值是 (0,0)** —— 也就是**图像左上角**，**不是工件**。
所以它报出来的数根本不是你想要的工件的像素位置。

**怎么查**：VisionSort 标定区有「**核对 PMA 原点**」按钮。
它会拿一张图跑一次定位，把它报的位置和表格里的像素点比一比：
如果报出来在 (0,0) 附近，就是原点没设。

**怎么修**：在 VisionPro 里把 PMA 的**模板原点**设到**工件上要被吸住的那个点**
（训练完之后，把原点标记拖到你想让机械臂去抓的那个位置即可）。
**改原点不需要重新训练**，改完直接保存方案。

**还有一件事（实测碰到的，不注意会差几个毫米）**：
原点设对了，PMA 报出来的位置**也不一定正好是区域中心**。
拿齿轮那张样例图试过：原点设成区域中心 (630,530)，实际跑出来报的是 (620.2, 508.9) ——
差了约 20 像素，按 0.19mm/px 算就是 **≈4mm**。

所以：**标定时点的那九个像素点，要以「PMA 报出来的位置」为准，不要以你眼睛看着的工件中心为准。**
最稳的做法是先在图上跑一次 PMA，看它报在哪里，再对着那个位置点标定点。

**程序里的防呆（不用你记）**：

- 视觉配置页训练完模板，会检查原点；还在 (0,0) 就出黄字警告。
- 标定区点「计算」之前会强制核对，**不通过不让保存**。

---

## 四、三个"必须一致"（不一致，标定就是错的）

1. **高度**：标定时的 Z 高度 = 生产抓取时的 Z 高度；标定图上那个面 = 工件上表面。
2. **相机**：相机、镜头、支架、焦距、光圈，动过任何一个 → 重新标定。
3. **成像设置**：分辨率、视频格式、ROI、彩转灰参数，要和生产时一样。

---

## 五、做完必须验证（反查）

找一个**没有参与标定**的位置：

1. 用同一套办法读出它的像素坐标，填进标定表格算出毫米坐标；
2. 让机械臂走到那个坐标；
3. **量一下实际差多少**。

**目标：小于 0.5mm。** 超过就回头看：高度对不对 / 模板原点设没设 / 九个点顺序 / 手点精度。

**每天开机做一次**这个动作 —— 相机被谁碰一下，标定就废了，而它不会自己告诉你。

---

## 六、抓不准的时候，先查什么

| 现象 | 先查这一条 |
|---|---|
| 整体平移一个固定的量 | **模板原点**（第三节） |
| 离画面中心越远越偏 | 高度不一致 / 九个点没铺开 |
| 左右（或上下）反了 | X/Y 对错轴了（看"手性"） |
| 只有某个角落不准 | 那个角落附近的标定点手点太粗 |
| 昨天准今天不准 | 相机 / 支架被动过 → 重新标定 |
| 定位工具报的位置在 (0,0) 附近 | 模板原点没设（第三节） |
| RMSE 突然变大 | 有一个点填错了（顺序错位 / 数字抄错） |
