# 相机设置 · 实施方案（最小版，待审核）

> 项目：`D:\visionPor\20260914-资料\04-WinForms\VisionSort`（net48 / x64 / WinForms）
> 页面：`Views/VisionConfigView.cs` → `grpCamera`「相机设置」
> 版本：v1.6（**触发状态在界面上可见 + `Auto` 时预览 `own=true`**；§十四 是 v1.5 的试触发与 RunStatus 诊断）
> 状态：`VisionConfigView.Camera.cs` 已落码，编译 0 error/warning
>
> 变更索引：§八 Q3「包 9000」**已被现场推翻**（相机里本来就是 1508，见 §13.6）；§12.6 的 Q1–Q4 **暂缓**。

---

## 一、目标与范围

**就四件事：连上相机、曝光增益生效、能拍到图、没相机时不崩。**

| 项 | 内容 |
|---|---|
| 在范围内 | ① 连接/断开 GigE 相机；② 曝光、增益、包大小、延迟级别应用到相机；③ 单次拍照（软件触发取一帧）；④ 无相机时所有按钮行为友好 |
| 不在范围内 | 触发模式真切硬触发/IO（`lblTrigMode` 保持静态文字）、参数持久化（与图像保存一致：重启回 Designer 默认）、配额/日志/数据库、主运行页的连续检测取流 |
| 不改动 | `Services/PmaTemplateTrainer.cs`、`Views/VisionConfigView.Pma.cs`、`Views/VisionConfigView.Save.cs`（只复用它们预留的钩子，不改它们） |

---

## 二、现状基线（已核对）

`grpCamera` 里 9 个可操作控件**全是裸控件**：5 个按钮零 Click 绑定，3 个下拉既无默认选中也无事件，`txtExposure` 默认 `8.0`，`lblTrigMode` 是纯静态文字。全仓相机逻辑为零——`VisionConfigView.cs` 里一行相机代码都没有。

| 控件 | 现状 | 本期用途 |
|---|---|---|
| `btnCamConn` / `btnCamDis` | 无事件 | 连接 / 断开相机 |
| `btnLive` / `btnStopLive` | 无事件 | 开始 / 停止实时预览（**独立小窗**显示，关窗即停，见 §3.5） |
| `btnSnap` | 无事件 | 单次拍照（软件触发取一帧，**独立小窗**显示，见 §3.5） |
| `txtExposure` | 默认 `8.0`，无事件 | 曝光 ms（数字校验，失焦生效） |
| `cmbGain` | `0/3/6/12`，**未选中** | 增益 dB |
| `cmbPacket` | `1500/4096/8192`，**未选中**（本期加一项 `9000` 并默认选中，见 §八 Q3） | GigE 包大小（默认 9000，现场网卡同步开巨帧 9000） |
| `cmbLatency` | `1/2/3/4`，**未选中** | 包发送延迟级别 |
| `lblTrigMode` | 「触发模式：Semi 半自动（停带拍照）」 | **只显示，不实现**（与 `lblQuota` 同例） |

可用家底（不用新引任何东西）：

- csproj 已引用 `Cognex.VisionPro.FGGigE` + `ImagingDevice` + `Display.Controls`，无第三方相机 SDK → 路线锁定**纯 VisionPro `CogAcqFifoTool`**，不碰 Basler/Hik 私有 SDK；
- `Pma.cs` 已预留 `TrainingImageProvider`（`Func<ICogImage>`，全仓从未被赋值）+ 注释写明“GigE 同一时刻基本只允许一个 FIFO 占用，训练页借图不抢相机”→ 相机页独占一个 FIFO，拍到的帧通过这个钩子共享出去，一行赋值即联动，PMA 训练页零改动；
- 方案自带的 `Image Source`（CogAcqFifoTool）**不动**：相机页自己建 FIFO，不污染已加载方案。

---

## 三、设计（最小）

### 3.1 只新增 1 个文件，不加控件

| 文件 | 职责 |
|---|---|
| **`Views/VisionConfigView.Camera.cs`**（新增，partial，约 230 行） | 5 个按钮 + 1 个失焦 + 3 个下拉事件；一个 `CogAcqFifoTool` 的建/连/断/释放；参数应用；预览小窗（代码动态建 Form，不加 Designer 控件） |
| `Views/VisionConfigView.Designer.cs` | 只补事件绑定 + 3 个下拉的默认选中（增益 0 / **包 9000（新增下拉项）** / 延迟 1） |

不加 `Services` 新类、不改 csproj、不加显示控件（除非 Q1 选了 A）。

### 3.2 状态与可点规则（按 Q4：按钮常可用，前置不满足弹提示）

```
[连接相机]/[断开相机]：互斥置灰——这是连接状态的唯一指示，不新增任何控件
[实时预览][单次拍照][停止预览]：永远可点；前置条件不满足时弹提示，不执行：
  未连接点拍照/预览 → “相机未连接，请先点『连接相机』”
  未预览点停止      → “当前没有预览”
```

> 说明（已确认）：连接/断开互斥置灰保留，作为连接状态的唯一指示，不新增控件。

### 3.3 参数与生效时机

| 参数 | 校验（Q5：曝光只卡 >0） | 生效时机 |
|---|---|---|
| 曝光 `txtExposure` | 数字且 >0，否则警告并恢复上次有效值（同 `txtSavePath.Leave` 套路）；上限不卡，超限由驱动夹断并读回显示 | 已连接则立即写驱动；未连接只记界面值，连接时应用 |
| 增益（默认 0）/ 包大小（默认 **9000**）/ 延迟级别（默认 1） | 下拉单选 | 连接时一次性应用；已连接时改动→尝试热应用，驱动拒绝则提示“重连生效” |
| 包 9000 前提 | — | 现场网卡必须同步开巨帧 9000，否则丢包/花屏/连不上；联调若网卡不支持再改回 1500（见 §七） |
| 触发模式 | — | 静态文字不动；单次拍照内部走软件触发 |

驱动特性名（曝光/增益/包大小/延迟到底调哪个属性）**不硬编码猜**：先写 10 行反射探针枚举 `Operator` 真实特性，联调一次确认后再落码——沿用 PMA 那轮“反射先行”的做法。

### 3.4 与 PMA 训练页的联动（零改动复用）

相机页连上后加一行：`TrainingImageProvider = () => 最新帧;`

- 『采集训练图』现有退化链（注入 → 方案取像 → 本地文件）自动升为第一优先级，PMA 文件**一字不改**；
- “借图不抢相机”：训练页只读帧，不碰 FIFO，GigE 独占约束天然满足。

### 3.5 预览小窗（按 Q1-A：不动现有布局）

- 『实时预览』/『单次拍照』弹一个独立小窗（代码里动态 `new Form + CogDisplay`，不往 Designer 加控件，不挤 `grpCamera` 布局）；
- 小窗关闭 = 停止预览（`FormClosed` 里停采集）；重复点击不叠窗（单例：已打开则 `BringToFront`）；
- 小窗标题带相机名 + 分辨率，方便现场确认接的是哪台相机。

---

## 四、目录与命名（无）

本期不产生任何文件输出（拍照的帧只在内存里流转：预览显示 / 送训练页）。图像落盘是图像保存分组的事，已有方案。

---

## 五、改动清单（预计）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `Views/VisionConfigView.Camera.cs` | **新增**：5 按钮 + 失焦 + 3 下拉事件 + FIFO 管理 + `TrainingImageProvider` 赋值 |
| 2 | `Views/VisionConfigView.Designer.cs` | 事件绑定；`cmbGain/cmbPacket/cmbLatency` 各补默认选中 |
| 3 | `VisionSort.csproj` | 无需改动 |

> 同样提醒：VS 设计器保存窗体会重写 `Designer.cs`，改完复查绑定。

---

## 六、验收 / 自测清单

本机（离线工作站）大概率无实体相机，所以自测分两档，**第一档无相机可全过**：

| # | 场景 | 期望 |
|---|---|---|
| T1 | 编译（x64 / net48，VS MSBuild） | 0 error / 0 warning |
| T2 | 无相机点『连接相机』 | 友好提示“未发现 GigE 相机…”，不抛异常 |
| T3 | 无相机点『单次拍照』/『实时预览』/『停止预览』 | 按钮可点，弹提示（未连接 / 当前没有预览），不执行不崩溃；『断开相机』初始置灰点不了 |
| T4 | 曝光框输 `abc` / `-5` 后失焦 | 警告并恢复上次有效值（只卡 >0，不卡上限） |
| T5 | （本页无此类约束，删除） | — |
| T6 | 切到视觉配置页 | 默认值正确（曝光 8.0 / 增益 0 / **包 9000** / 延迟 1），无配置文件生成 |
| T-live1 | 有相机：连接 → 单次拍照 | 出图（尺寸/像素格式正常），断开后资源释放、无残留 FIFO |
| T-live2 | 有相机：改曝光/增益再拍 | 亮度 visibly 变化（截图对比即可） |
| T-live3 | 有相机：实时预览 → 停止 | 画面动 → 停；连续开关 10 次不卡死不泄漏 |
| T-live4 | 有相机：相机页连着时点 PMA『采集训练图』 | 拿到的是相机帧（验证“借图不抢”不断流） |

T-live 必须到现场跑，我这边跑完第一档 + 代码审查即交付。

---

## 七、风险与规避

| 风险 | 影响 | 规避 |
|---|---|---|
| 驱动特性名随相机品牌而异，硬编码调错属性 | 连接成功但参数没生效 | 反射探针先枚举、联调确认后再落码；应用后读回校验一次 |
| LiveDisplay 起停签名拿不准 | 编译不过 | P4 探针点：先确认签名再落码，本机无相机则按 FGGigE 通用签名写、现场联调一次 |
| 预览跨线程刷显示 | 跨线程异常 | 走 LiveDisplay 内部机制，本侧不自己起线程刷图 |
| 包大小与网卡巨帧不匹配 | 丢包/花屏/连不上 | 默认 9000 的前提是现场网卡开巨帧 9000（Q3 已定）；联调连不上/花屏第一件事就是把包改回 1500 再试，网卡巨帧同步检查 |
| FIFO 独占：方案里的 Image Source 与本页 FIFO 抢相机 | 一边断流 | 本页独占自建 FIFO；训练页只读帧不碰 FIFO（钩子已预留） |
| 预览跨线程刷显示 | 跨线程异常 | 显示走 `CogDisplay` 自带机制，不自己起线程刷图 |
| 断开时资源未释放 | 下次连不上，需重启软件 | `Disconnect` 里按固定顺序停预览 → 停采集 → Dispose，探针复测验证 |

---

## 八、我代为决定的项 + 需要你拍板的开放项

代为决定（如需否决直接说）：

| 项 | 决定 | 备选 |
|---|---|---|
| 相机路线 | 纯 VisionPro `CogAcqFifoTool`（FGGigE 已引用） | 引 Basler/Hik 私有 SDK（加重依赖，不推荐） |
| FIFO 归属 | 相机页自建独占，不动方案里的 Image Source | 复用方案里的取像工具（会污染方案，不推荐） |
| 参数持久化 | 不记，重启回 Designer 默认（与图像保存一致） | 加 ini（v2.1 删过一次，不推荐走回头路） |
| 状态显示 | 按钮 Enable 即状态，不加灯/字 | 加状态 label |
| 触发模式文字 | 保持静态 | 真做硬触发（那是主运行页+IO的事，本期不碰） |

**你的决定（已落入方案，不再是开放项）：**

| 题 | 你的选择 | 落点 |
|---|---|---|
| Q1 显示在哪 | **A：独立预览小窗** | §3.5：代码动态建窗，不动布局，关窗即停 |
| Q2 相机 | **HikRobot** | 仍走 VisionPro FGGigE（HikRobot 是标准 GigE Vision，不引 MVS 私有 SDK）；特性名按 GenICam 通用名 + 反射探针现场确认 |
| Q3 默认值 | **包 9000**（网卡巨帧同步 9000） | §二/§3.1/§3.3/§六T6/§七已同步；增益 0、延迟 1 不变 |
| Q4 未连接可点性 | **可点提示** | §3.2：功能按钮永远可点，前置不满足弹提示；仅连接/断开互斥置灰以显示连接状态 |
| Q5 曝光上限 | **默认（只卡 >0）** | §3.3：超限由驱动夹断并读回显示 |

---

## 九、落盘顺序（你点头后）

1. 反射探针确认驱动特性名（有相机则一次写对；无相机则按通用写法 + 联调确认点交付）；
2. 新增 `Views/VisionConfigView.Camera.cs`（按 §十 逐方法落码）；
3. `Designer.cs` 按 §十的 diff 补绑定 + 默认选中；
4. 编译 → 第一档自测（T1–T6，无相机已全过，见 §十一）→ 文档更新为 v1.2「已实现 + 实测记录」；
5. T-live 留待现场，你按 §六表格打勾即可。

---

## 十、代码实现详案（v1.1 待你批准，批准后照此开工）

> 约束先行（都是之前踩过的坑，直接写死）：
> ① 不新增 `OnLoad` 重写——`Save.cs` 已占用，同 partial 再写会 CS0111，下拉默认选中全部放 Designer；
> ② 不新增控件（含状态灯/文字），状态只靠连接/断开互斥置灰 + 提示框；
> ③ 成功静默、失败弹框（按钮状态即反馈，少打扰；与 VPP 保存页“成功也弹”不同，这里有意更安静）；
> ④ VisionPro 专有调用中拿不准的 4 处标为【探针锁定】，第一步先跑反射探针再落码，不猜 API。

### 10.1 文件骨架（`Views/VisionConfigView.Camera.cs`，约 230 行）

```csharp
using Cognex.VisionPro;
using Cognex.VisionPro.Display;      // 小窗里的 CogDisplay
using System;
using System.IO;                     // 不需要可删
using System.Windows.Forms;

public partial class VisionConfigView
{
    private CogAcqFifoTool _camFifo;      // 自建独占 FIFO，null = 未连接
    private ICogImage _lastFrame;         // 最新帧：小窗显示 + TrainingImageProvider 共享
    private Form _previewBox;             // 预览小窗单例（null = 没打开）
    private CogDisplay _previewDisplay;   // 小窗里的显示控件（LiveDisplay 直接绑它）
    private bool _previewLive;            // 连续预览进行中（自己记一位，不猜显示控件有没有现成属性）
    private bool _camBusy;                // 防重入（同步取图时）
    private bool _cleanupHooked;          // Disposed 钩子只挂一次
    private double _lastValidExposure = 8.0;  // 与 Designer 的 txtExposure.Text="8.0" 对齐

    // 事件：btnCamConn_Click / btnCamDis_Click / btnSnap_Click / btnLive_Click /
    //       btnStopLive_Click / txtExposure_Leave /
    //       cmbGain_SelectedIndexChanged / cmbPacket_SelectedIndexChanged / cmbLatency_SelectedIndexChanged
    // 内部：ApplyCameraParams / ShowPreview / ClosePreview / RefreshCamButtons / EnsureCleanupHook
}
```

### 10.2 连接 / 断开

```csharp
btnCamConn_Click:
  ① 已连接(_camFifo != null) → 提示“相机已连接，如需重连请先断开”，返回        // Q4：可点提示
  ② 枚举 GigE 相机【探针锁定：候选写法见 10.6】
     为空 → 提示“未发现 GigE 相机，请检查网线/供电/网卡巨帧/防火墙”，返回
     多台 → 取第一台（Q2 现实：现场一般 1 台；数量记下来报给用户）
  ③ new CogAcqFifoTool → 绑定 FrameGrabber【探针锁定】
  ④ ApplyCameraParams()（曝光/增益/包9000/延迟；任一步失败→Dispose→提示原因+“检查网卡巨帧/改1500”，连接整体失败）
  ⑤ _lastFrame = null；TrainingImageProvider = () => _lastFrame;   // PMA 联动，一行
  ⑥ EnsureCleanupHook()；RefreshCamButtons()；成功静默（相机名放到小窗标题上，不弹框）

btnCamDis_Click:
  ① 未连接 → 提示“当前未连接相机”，返回                              // Q4：可点提示
  ② 关小窗（预览中则先 StopLiveDisplay）→ TrainingImageProvider = null
     → _camFifo.Dispose() → _camFifo = null → RefreshCamButtons()；成功静默，异常弹框

RefreshCamButtons:  btnCamConn.Enabled = (_camFifo == null);
                     btnCamDis.Enabled  = (_camFifo != null);
                     // 拍照/预览/停止永远可点（Q4），不碰它们的 Enabled

EnsureCleanupHook:   if (!_cleanupHooked) { this.Disposed += (s,e) => { 关小窗；_camFifo?.Dispose(); }; _cleanupHooked = true; }
                     // 不碰构造函数/VisionConfigView.cs，首次连接时懒挂钩
```

### 10.3 拍照 / 预览（按你的决定：连续预览用 LiveDisplay API）

```csharp
btnSnap_Click:
  ① 未连接 → 提示“相机未连接，请先点『连接相机』”，返回
  ② _camBusy 防重入（try/finally，同 PMA 三个按钮套路）
  ③ 软件触发取一帧：_camFifo.Run() 取 OutputImage【标准用法，探针复核签名】
  ④ _lastFrame = 帧；ShowPreview(帧)；成功静默，异常弹框
     （预览进行中点单次拍照：允许，刷新 _lastFrame 并把小窗切到该静帧？不——
       决定：预览中点拍照只刷新 _lastFrame，不碰 live 画面，避免抢流。二选一已定。）

btnLive_Click:
  ① 未连接 → 同上提示，返回
  ② 小窗已开且 _previewLive → _previewBox.BringToFront()，返回（不叠窗）
  ③ 开小窗 → _previewDisplay.StartLiveDisplay(…, …)【签名探针锁定，见 10.6 P4】
     → _previewLive = true；启动失败弹框，小窗不留空窗（失败即关）
  说明：跨线程刷图由 LiveDisplay 内部处理，本侧不自己起线程；
  流中断（如拔线）：暂不做专门监听，下次点按钮/拍照时自然报错，联调若不够再加

btnStopLive_Click:
  ① !_previewLive → 提示“当前没有预览”，返回
  ② _previewDisplay.StopLiveDisplay()【签名探针锁定】→ _previewLive = false → 关小窗
     （关窗本身也调一次 Stop：FormClosed 里判 _previewLive 再停，双保险，不重复停）

ShowPreview(frame):（单次拍照用；live 不走这里）
  小窗 null → new Form{宽800高600} + new CogDisplay{Dock=Fill}
  _previewDisplay.Image = frame；Fit(true)；Show()（非模态）

TrainingImageProvider 取帧规则（live 下 _lastFrame 不再逐帧刷新，特定一下）：
  TrainingImageProvider = () =>  _previewLive && _previewDisplay.Image != null
                                 ? _previewDisplay.Image   // 预览中：拿显示控件当前帧
                                 : _lastFrame;             // 否则：拿最近一次单次拍照帧
```

> 与 Timer 轮询版的差异：live 帧率全速（不再是 ~5fps），代价是多 1 个探针锁定点（P4 起停签名）；`_lastFrame` 只在单次拍照时刷新，PMA 在预览中取图走显示控件当前帧，语义不变（“借图不抢相机”不断流）。

### 10.4 参数：校验与生效

```csharp
txtExposure_Leave:   // 照抄 txtSavePath_Leave 套路（已验证过）
  程序改框(_saveUiUpdating 复用？不，各管各：用 _camBusy？不，Leave 与取图不冲突。
  决定：不加新 guard，Leave 里只做纯校验，无重入链）→ 你审时重点看这句
  ① 取 Text.Trim()，与 _lastValidExposure.ToString() 一致 → 返回（没改不打扰）
  ② double.TryParse 且 >0（Q5：不卡上限）→ _lastValidExposure = 值；
     已连接 → 写驱动曝光特性【探针锁定】→ 读回校验，失败弹框并恢复旧值
  ③ 非法 → 警告 + 文本框恢复 _lastValidExposure

cmbGain/Packet/Latency_SelectedIndexChanged（三个同构）：
  ① 未连接 → 静默返回（只记界面值，连接时 ApplyCameraParams 统一应用）
  ② 已连接 → 尝试热应用该项；驱动拒绝/异常 → 提示“该参数需重连生效”，保持用户选择
     （不回滚选中项：重连/下次连接时 ApplyCameraParams 会应用它）

ApplyCameraParams()（连接时调用，fail-fast）：
  曝光(double) → 增益(按下拉文本转数) → 包大小(int，默认9000) → 延迟级别(int)
  每写一项读回校验；任一失败抛异常（带特性名+当前值），由调用方 Dispose 并提示
```

### 10.5 Designer diff（逐行，共 11 行）

```diff
 // flpCam 5 个按钮：各加 1 行 Click（btnCamConn/btnCamDis/btnLive/btnStopLive/btnSnap）
 this.btnCamConn.Click += new System.EventHandler(this.btnCamConn_Click);      // +1
 ...（余 4 个同构，共 +5）
 // btnCamDis 再加初始置灰 1 行（§3.2：连接/断开互斥置灰）
 this.btnCamDis.Enabled = false;                                              // +1

 // txtExposure：加 Leave（+1）
 this.txtExposure.Leave += new System.EventHandler(this.txtExposure_Leave);

 // 3 个下拉：各加 SelectedIndexChanged（+3）+ 默认选中（+2生效行，包另+1项）
 this.cmbGain.SelectedIndexChanged += ...this.cmbGain_SelectedIndexChanged;    // +1
 this.cmbGain.SelectedIndex = 0;                                               // +1（=0dB）
 this.cmbPacket.Items.AddRange(new object[] {"1500","4096","8192","9000"});    // 改：加"9000"
 this.cmbPacket.SelectedIndexChanged += ...this.cmbPacket_SelectedIndexChanged;// +1
 this.cmbPacket.SelectedIndex = 3;                                             // +1（=9000）
 this.cmbLatency.SelectedIndexChanged += ...this.cmbLatency_SelectedIndexChanged; // +1
 this.cmbLatency.SelectedIndex = 0;                                            // +1（=级别1）
```

合计：新增绑定 9 行 + 默认选中 3 行 + Items 加 1 项 + 断开初始置灰 1 行。`lblTrigMode`、`cmbImgFmt` 式的前车之鉴：改完复查绑定仍在（设计器重写风险）。

### 10.6 【探针锁定】的 4 处调用（已全部锁死，59.2.0.0 实测）

| # | 作用 | 锁死结论（反射探针 7 轮，本机无头验证） |
|---|---|---|
| P1 | 枚举 GigE 相机 | `new CogFrameGrabberGigEs()` → `Count` / `Item[int]` → `ICogFrameGrabber`（`Name`/`SerialNumber`/`UniqueID`）；本机 Count=0 不抛异常 |
| P2 | FIFO 绑定 | `grabber.CreateAcqFifo(videoFormat, Format8Grey=1, 0, true)` → `fifoTool.Operator = …`（`Operator` 可写；`FrameGrabber` 只读，此路不通已避开）；videoFormat 取 `GetAvailableVideoFormatOptions(0, null×5)` 第一个（options 是 bitmask 无 All 成员，0=不过滤） |
| P3 | 曝光/增益/包/延迟 | 曝光 `OwnedExposureParams.Exposure`（**毫秒**，官方 XML 原话，无需换算）；增益无模拟 dB 标准接口 → GenICam `"Gain"` 节点（`CogAcqCustomProperty(name, value, TypeFloat=2)`，值是 dB 字符串）；包 `PacketSize`（可写，`PacketSizeMax` 只读钳位）/ `LatencyLevel`（0–3，下拉文本 1–4 即 SelectedIndex）；触发 `TriggerEnabled=false` 自由运行 |
| P4 | 连续预览起停 | `_previewDisplay.StartLiveDisplay(object acqFifo, bool own)`（传 `_camFifo.Operator`，own=false）/ `StopLiveDisplay()` / 只读 `LiveDisplayRunning`；`Image` 可读写（live 取帧就读它） |

探针源码与 exe 跑完即删，结论已落码（`VisionConfigView.Camera.cs` 头部注释同步记录）。

### 10.7 与验收条款的对应（防漏）

T2→btnCamConn_Click②空表分支；T3→三个按钮的前置提示分支；T4→txtExposure_Leave③；
T6→Designer 默认选中；T-live1→10.2 全链 + Disposed 钩子；T-live2→10.4 热应用；
T-live3→10.3 LiveDisplay 启停 + 重复 BringToFront；T-live4→10.2⑤ + 10.3 取帧规则。

---

## 十一、实测记录（v1.2，第一档，无相机）

落盘内容：`Views/VisionConfigView.Camera.cs` 新增（约 330 行）；`Designer.cs` 改 13 行（9 绑定 + 3 默认选中 + 包加 `9000` 项 + 断开初始置灰）；`VisionSort.csproj` 未动。

自测方法：临时探针引用真实 `VisionSort.exe` 驱动 UserControl（看门狗自动关闭提示框并记录文本），跑完探针 exe/源码全删，`bin` 下无残留。

| # | 场景 | 结果 |
|---|---|---|
| T1 | 编译（VS MSBuild amd64，x64/net48） | ✅ 0 error / 0 warning（全量 grep 确认） |
| T6 | 默认值 | ✅ 曝光 8.0 / 增益 0(idx0) / 包 9000(idx3) / 延迟 1(idx0) / 连接可用 / 断开置灰 |
| T2 | 无相机点『连接相机』 | ✅ 弹“未发现 GigE 相机，请检查网线/供电/网卡巨帧/防火墙。”；仍未连接；`TrainingImageProvider` 保持 null |
| T3 | 未连接点拍照/预览/停止 | ✅ 各弹对应提示（未连接×2 / 当前没有预览）；断开按钮置灰点不了 |
| T4a/b | 曝光输 `abc` / `-5` 失焦 | ✅ 警告“曝光请输入大于 0 的数字（毫秒）”并回退 8.0 |
| T4c | 曝光输 `20` 失焦 | ✅ 无提示，内存值与框值均为 20 |
| T4d | 未连接切三个下拉 | ✅ 无提示（只记界面值） |
| T5 | 全程窗体数 | ✅ 恒为 1，无预览小窗误弹 |

自测中发现并修掉的 2 个问题：

1. **断开按钮初始可用**——`RefreshCamButtons` 只在连接/断开后调，首次加载没人调。修法：Designer 里 `btnCamDis.Enabled = false`（默认值放 Designer，与既有风格一致）。
2. **探针T4无触发**——探针只改 `Text` 没先进框，`Leave` 不触发（与实现无关，探针自身问题）。修法：先 `Focus()` 进框再改值，复测通过。反证了“没改不打扰”逻辑正确。

未覆盖（必须现场）：T-live1–4（连接/出图/参数目视/预览启停/PMA借帧），另增益 `"Gain"` 节点以 HikRobot 标准节点为准、现场 T-live2 目视确认亮度变化。

---

## 十二、现场相机兼容性复核（Hikrobot MV-CS060-10GC / Bayer Color）· v1.3 待拍板

**现场目标配置**（VisionPro `CogAcqFifoTool1` 对话框实测截图）：

| 项 | 现场值 |
|---|---|
| 图片采集设备 | `GigE Vision: Hikrobot: MV-CS060-10GC : DB1711521` |
| 视频格式 | `Generic GigEVision (Bayer Color)` |
| 曝光 | `5` ms |
| 亮度 | `0.524` |
| 对比度 | `0` |

**复核方式**：本机无相机（`CogFrameGrabberGigEs().Count = 0`，探针实测），因此是**对着 `Cognex.VisionPro*.dll` 反射 + 现有代码逐项比对**，不猜 API；探针源码/exe 已删，`bin` 无残留。

### 12.1 逐项比对结论

| 现场项 | 现有代码怎么做的 | 结论 |
|---|---|---|
| 枚举到这台相机 | `new CogFrameGrabberGigEs()` → `giges[0]` | ✅ 兼容。截图里那条设备名就是 FGGigE 驱动的命名格式，必在 `Count/Item` 列表里 |
| 视频格式 Bayer Color | `formats[0]` 盲取第一项 | ⚠️ 能过但靠运气，见 12.3 |
| 请求 `Format8Grey` | `CreateAcqFifo(formats[0], Format8Grey, 0, true)` | ⚠️ 需设备确认，见 12.3（有现成的无副作用预检接口） |
| 曝光 5 ms | `OwnedExposureParams.Exposure`（毫秒） | ✅ 接口兼容；⚠️ 默认 8.0 且**连接即强推**，见缺口2 |
| **亮度 0.524** | 界面无此格；代码写的是 GenICam `"Gain"` 自定义节点 | ❌ **功能上做不到**，见缺口1 |
| **对比度 0** | 界面无此格，代码不碰 | ❌ 同上 |
| 增益(dB) | GenICam `"Gain"` 自定义属性 | ✅ 路子没错（`OwnedDigitalCameraGainParams` 只有 `DigitalHighGainSelected` 一个 bool，没有 dB 增益标准接口），但**现场配方没用它** |
| 包大小 / 延迟 | `PacketSize` + `LatencyLevel` | ⚠️ 包 9000 超 `PacketSizeMax` 时**连接直接失败**，见缺口3 |
| 触发 | `TriggerEnabled = false`（自由运行） | ✅ 与「视觉参数配置」那期规划的硬触发不冲突，见 12.4 |

### 12.2 三个真实缺口

**缺口1（唯一"做不到"的项）· 亮度/对比度没接**
反射证据（59.2.0.0 实测）：`ICogAcqFifo.OwnedBrightnessParams.Brightness`（Double，get/set）、`ICogAcqFifo.OwnedContrastParams.Contrast`（Double，get/set）**都存在**——就是 VisionPro 设置页那两格。
而现有代码写的是 `OwnedCustomPropertiesParams` 里的 GenICam `"Gain"` 节点（对话框的**自定义属性**页），跟**设置**页的「亮度」不是同一处。所以现场那套配方，本页目前一格都复现不了。

**缺口2 · 连接即强推界面默认值，会覆盖现场配方**
`btnCamConn_Click` 第 ④ 步无条件 `ApplyCameraParams(fifo, true)`：曝光写界面值（默认 `8.0`）、包写 9000、延迟写下拉值。现场相机里存的是 5 ms / 0.524 / 0。
→ **只要在本页点一次『连接相机』，相机曝光就从 5 ms 被改成 8 ms**；若生产 vpp 依赖相机自身设置取像，会连带影响生产件。这是"本页只是个调试页"与"相机参数是全局共享资源"之间的矛盾。

**缺口3 · 包大小超上限导致连接失败**
`if (packet > PacketSizeMax) throw …` → 连接整体失败。9000 只是调优参数，不该让它连不上相机。

### 12.3 视频格式 / 像素格式：现在能过，但属于"运气好"

- `GetAvailableVideoFormatOptions(0, null×5)` 的第一个参数是**过滤位掩码**（`CameraManufacturer=1 / CameraModel=2 / CameraResolution=4 / CameraDriveType=8 / CameraOptions=16`），传 `0` = **不过滤** → 返回该相机**全部**格式，`formats[0]` 是"列表里的第一项"，不保证是 Bayer Color。截图里下拉只有一项，所以恰好对——但代码既不校验也没打印。
- `CogAcqFifoPixelFormatConstants` 只有 `Format8Grey=1 / Format16Grey=2 / Format565RGB=3 / Format32RGB=4 / Format3Plane=5`，**没有 Bayer 项**；从 Bayer Color 格式请求 8Grey 能否成立，必须设备上问。
- **好消息**：`ICogFrameGrabber.GetSupportsPixelFormat(CogAcqFifoPixelFormatConstants)` 返回 bool——上机第一件事就能**无副作用**地问出"这台相机支不支持 8Grey"；`ICogAcqFifo.VideoFormat`（只读）与 `AcquiredPixelFormat()`（方法）能读回**实际生效**的格式。
- **兜底不痛**：万一只能拿到彩色图，PMA 侧 `PmaTemplateTrainer.EnsureGreyImage()`（已有，走 `CogImageConvertTool` 的 `Intensity` 模式）会自动转 8 位灰度，**训练路径一行都不用改**。

### 12.4 触发与参数接口（顺带锁定，供「视觉参数配置」那期直接用）

`CogAcqTriggerModelConstants` 实测：`Manual=0 / Auto=1 / Semi=2 / Slave=3 / FreeRun=4`
→ Designer 里那句静态文字"Semi 半自动"对应 `Semi=2`，而「视觉参数配置方案.md」v2.3 的 Q4（自动硬触发）要的就是 `Auto=1`；`OwnedTriggerParams.TriggerModel / TriggerEnabled / TriggerLowToHigh` 三者全部可写。**两期方案不冲突**，本文件不碰触发。

### 12.5 建议改法（推荐组合，等你点头再动手）

1. **连接改为"先读回、后显示，不主动写"**：连上后依次读 曝光 / 亮度 / 对比度 / 包大小 / 延迟 → 填进界面。用户没改就**一个字节都不写相机** → 现场 5 ms / 0.524 原样保留；顺带把"不落盘、每次启动要重填"这个老毛病也解决了（值本来就存在相机里）。用户改了才单项热应用（沿用现有 `Leave` / `SelectedIndexChanged` 套路）。
2. 界面补 **亮度 + 对比度** 两格，走 `OwnedBrightnessParams.Brightness` / `OwnedContrastParams.Contrast`（**标准接口，不是自定义节点**）。
3. 包大小超 `PacketSizeMax` → **夹到上限并提示**，不再让连接失败。
4. 连接时用 `GetSupportsPixelFormat(Format8Grey)` 预检、优先挑名字含 `Bayer` 的格式（没有则取第一项），并把 `VideoFormat` + `AcquiredPixelFormat()` 写进预览小窗标题，现场一眼看清到底跑在什么格式上。

> 亮度/对比度的量纲不猜：界面按"**原样读回、原样写回**"处理，不做换算（你看到的 `0.524` 就是相机/Grabber 里的原始值）。这条如果你没意见就按此执行，不单独设问。

### 12.6 需要你拍板（四条，编号回答即可）

| 题 | 选项 | 说明 |
|---|---|---|
| **Q1 亮度/对比度怎么进界面** | **A（推荐）** 新增一行面板 `pnlExp2`（Dock=Top）放 亮度 + 对比度，增益(db) 原样保留 | 改动最小，不动现有两行布局；代价是多一行（约 30px 高）+1 个 Panel |
| | B 用 亮度 + 对比度 替掉 增益(db)（同时删掉 GenICam `Gain` 那段代码） | 与 VisionPro 设置页 1:1，界面不增高；代价是丢掉 dB 增益 |
| | C 不动界面 | 本页仍然调不了这两个参数，只保证不覆盖现场配方（缺口1 不修） |
| **Q2 连接时的参数策略** | **A（推荐）** 读回相机现值填界面，用户不改就不写 | 不覆盖现场 5 ms / 0.524 配方，逼近"只读调试页"的语义 |
| | B 维持现状 | 连接即用界面默认值强推，每次连接都会把相机曝光改成 8 ms |
| **Q3 包大小超相机上限** | **A（推荐）** 夹到上限并提示"已按上限 X 应用" | 参数可调，连接不该失败 |
| | B 维持现状 | 抛异常，连接整体失败 |
| **Q4 视频格式/像素格式** | **A（推荐）** `GetSupportsPixelFormat` 预检 + 优先挑含 `Bayer` 的格式 + 连接后读回并在小窗标题显示实际格式 | 把 12.3 的"靠运气"变成"看得见" |
| | B 维持现状 | 盲取 `formats[0]`，不校验不显示；现场若出问题只能靠猜 |

> 已代为决定（如需否决直接说）：亮度/对比度按"原样读回、原样写回"不换算；不引入 ini/JSON 落盘（Q2 选 A 后已无必要）；触发逻辑仍归「视觉参数配置」那期，本文档不碰。

### 12.7 落地清单（Q1–Q4 定了之后照着改）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `Views/VisionConfigView.Camera.cs` | 连接流程改"读回优先"；新增亮度/对比度读写；包大小改夹断；格式预检 + 读回显示 |
| 2 | `Views/VisionConfigView.Designer.cs` | Q1=A 时：新增 `pnlExp2` + `lblBright` + `txtBrightness` + `lblContrast` + `txtContrast`（含事件绑定；VS 设计器保存会重写本文件，改完复查） |
| 3 | `Services/PmaTemplateTrainer.cs`、`Views/VisionConfigView.Pma.cs` | **不动**（彩色兜底已有 `EnsureGreyImage`） |
| 4 | 验收 | T2–T6 无相机项可全过；新增设备项：T-live5 连接后界面显示 5 / 0.524 / 0 且相机值未被改动；T-live6 改亮度后目视变化；T-live7 小窗标题显示 Bayer/实际像素格式 |

---

**版本流水**：v1.0 方案 → v1.1 代码详案（探针锁定 4 处）→ v1.2 落码 + 第一档自测通过 → v1.3 现场相机兼容性复核（§十二）→ v1.4 现场三处问题实修 + 自测 19/19（§十三）→ v1.5 现场诊断框回传 → 三段式试触发 + RunStatus 诊断（§十四）→ **v1.6 触发状态上界面 + `Auto` 时预览 `own=true`（§十五）**。

---

## 十五、v1.6 触发状态可见化（+ `Auto` 时的预览合规）

背景：触发是"连接时自动试出来"的（§14.2），但界面上那句 `lblTrigMode` 还是 Designer 里写死的
「触发模式：Semi 半自动（停带拍照）」—— 跟实际状态毫无关系，等于骗人；而且三段式里第①轮可能
留下 `TriggerModel = Auto`，此时 `StartLiveDisplay` 的 `own` 按文档**必须为 true**，v1.5 还写着 `false`。

### 15.1 改动

| # | 位置 | 改动 |
|---|---|---|
| 1 | `Designer.cs` | `lblTrigMode` 初值改成「触发模式：未连接」 |
| 2 | `Camera.cs` | 新增 `RefreshTriggerLabel()`：连上后读回 `TriggerModel` / `TriggerEnabled`，显示成<br>`触发模式：自由运行 · 触发关 · 相机原配置` / `触发模式：手动（软件触发） · 触发开 · 本页已切换`；未连接显示「触发模式：未连接」。连接成功、断开各刷一次 |
| 3 | `Camera.cs` | 新增 `TriggerModelName()`：枚举 → 人话（`Manual=0 手动（软件触发）` / `Auto=1 自动（外部硬触发）` / `Semi=2 半自动（停带触发）` / `Slave=3 从站（跟随主站）` / `FreeRun=4 自由运行`） |
| 4 | `Camera.cs` | 新增 `LiveDisplayOwnsFifo` 属性：`TriggerModel == Auto` 时返回 true，`StartLiveDisplay` 的 `own` 用它（原来写死 false） |
| 5 | `Camera.cs` | 连带加固 `btnSnap_Click`：**预览中一律不调 `Run()`**（`own=true` 期间文档禁止边预览边用 FIFO 取图），只读显示控件当前帧；画面还没出来就提示一句，不再退化成 `Run()` |
| 6 | `Camera.cs` | `ProbeOneFrame` 记录 `_triggerKeptAsIs`（第①轮成功＝相机原配置未被改动），供第 2 条显示 |

> "相机原配置 / 本页已切换"这一小段是专门用来看「我们到底动没动相机的触发」的 —— 现场连一次就能确认。

### 15.2 复测（第一档，无相机，9/9 通过）

| 项 | 结果 |
|---|---|
| 编译（VS MSBuild amd64，x64/net48） | ✅ 0 error / 0 warning |
| 启动时 `lblTrigMode` | ✅ 「触发模式：未连接」 |
| 未连接调 `RefreshTriggerLabel` | ✅ 仍「未连接」（走 null 分支），不抛异常 |
| 未连接时 `LiveDisplayOwnsFifo` | ✅ false |
| `TriggerModelName` 五个枚举 | ✅ FreeRun/Manual/Auto/Semi/Slave 全部映射正确 |
| 连接可用 / 断开置灰 | ✅ 未受影响 |
| 探针清理 | ✅ `bin` 与 `_tmp` 无残留 |

### 15.3 现场要看的一行

连上相机后看 `lblTrigMode`：

- 显示 **`· 相机原配置`** → 说明现场那套触发配置本程序直接能用，**相机的触发一个字都没被改**（最理想）；
- 显示 **`· 本页已切换`** → 说明相机原配置在我们这儿取不到图，是自动切到的那一档顶着用（这条请回报，要写进文档）。

---

## 十三、v1.4 现场问题实修（Hikrobot MV-CS060-10GC）

### 13.1 现场证据（三张截图 + 报错原文）

| 来源 | 内容 |
|---|---|
| 视频格式下拉 | **4 项**：`Generic GigEVision (Bayer Color / Mono / RGB8 / YUV422 Packed)` |
| VisionPro `GigE` 页（能出画面的那套配置） | 传输超时 **2000 毫秒**、包大小 **1508 字节**、延迟级别 **3** |
| 闪光灯和触发器 | 触发器 **手动** |
| 相机 | 型号 MV-CS060-10GC、序列号 DB1711685、相机 IP 169.254.203.125、适配器 IP 169.254.203.113 |

### 13.2 三个症状 → 根因 → 修法

**① 报错「视频格式不受此取相板卡的支持」**
根因：v1.2 用的是 `GetAvailableVideoFormatOptions(0, null×5)` 的返回值当格式名。官方 `FGGigE.xml` 写得明白——`CreateAcqFifo` 的 `videoFormat` **"Must be one of the strings returned by `ICogFrameGrabber.AvailableVideoFormats`"**；`GetAvailableVideoFormatOptions` 返回的是"选项组件"拼串，它的 `GetKey(i)` 才是 `AvailableVideoFormats` 的下标。**取错了数据源**。
修法：改用 `grabber.AvailableVideoFormats`，并按关键字优先挑 `Bayer`（现场那台就是 `Bayer Color`）。

**② 连上了、预览窗口弹出来了，但一片空白（"蓝屏"）**
根因有两层：
- `ApplyCameraParams` 无条件写 `PacketSize = 9000`，而现场可用值是 **1508**（标准 1500 MTU）。相机按 9000 发包 → 大包被丢 → **一帧都到不了主机**；
- 旧 `btnSnap_Click` 里 `Run()` 空手而归（`OutputImage == null`）时**照样开窗**，`ShowPreview` 的塞图判断又跳过 null → 空窗口 = 看到的"蓝屏"。
修法：包大小**夹到相机上限、默认 1508**；连接后**试取一帧**，拿不到就抛带诊断信息的异常（不再有"连上了却没图"的假状态）。

**③ 延迟级别切到 3 报 `camera caps or feature list`**
根因：v1.2 把下拉文本 `1/2/3/4` 直接当相机 `LatencyLevel`（0–3）用，这个偏移是设计时的假设、从未验证；而 VisionPro 自家 GigE 页显示的就是 **3**（同一页的包大小 1508、超时 2000 ms 也都是原值）→ 说明 VisionPro 显示的是**原始值**，我们的下拉应当 1:1。
修法：下拉改成 **`0/1/2/3`**（界面值 = 相机值），读回填界面，写失败回退到相机真值。

**顺带修掉的设计缺陷（比这三条更值钱）**：旧代码**连接时无条件强推界面四项参数**，意味着在生产机上点一次『连接相机』就会把现场配方（曝光 5 / 包 1508 / 延迟 3）改掉；且任一参数被驱动拒绝会让**整次连接失败**。本轮改成：

```
连上后：读回相机现值 → 填界面 → 不主动写（界面上显式改过的项才写）
已连接时：改一项只写这一项（旧写法会四项一起重写）
写失败/被夹断：提示 + 界面回退到相机真值（界面永远显示"实际生效值"）
增益：不读不写（GenICam 节点不在自定义属性列表里，读不到相机现值），只有用户改下拉才写
```

### 13.3 落盘清单

| # | 文件 | 改动 |
|---|---|---|
| 1 | `Views/VisionConfigView.Camera.cs` | 格式改 `AvailableVideoFormats`+挑 Bayer；显式 `TriggerModel = FreeRun`；连接后试取一帧（超时临时 3s）+ 失败诊断文案；参数改为"读回优先 + 单项写入 + 同步回退"；`_uiSyncing` 抑制读回触发的写回；小窗标题带实际视频格式 |
| 2 | `Views/VisionConfigView.Designer.cs` | `cmbPacket` 候选改 `1508/4096/8192/9000`、默认 **1508**；`cmbLatency` 候选改 `0/1/2/3`（1:1） |
| 3 | 其余文件 | **未动**（`Pma.cs` / `PmaTemplateTrainer.cs` / `Save.cs` / csproj 一行没改） |

新增私有成员：`PickVideoFormat` / `ProbeOneFrame` / `BuildNoFrameMessage` / `Safe` / `ApplyOrAdoptParams` / `AdoptExposure|PacketSize|LatencyLevel` / `WriteExposure|PacketSize|LatencyLevel` / `SyncPacketUi` / `SyncLatencyUi` / `FormatDouble` / `FormatTagOf`。
已删除：`ApplyCameraParams`、`ApplySingleParam`（旧的"四项一起重写"入口）。

### 13.4 自测记录（第一档，无相机，19/19 通过）

方法：临时探针引用真实 `VisionSort.exe` 驱动 `VisionConfigView`，看门狗自动关闭并记录提示框文本；跑完探针源码/exe 全删，`bin` 无残留。

| 组 | 结果 |
|---|---|
| T1 编译（VS MSBuild amd64，x64/net48） | ✅ 0 error / 0 warning |
| T6 默认值 | ✅ 包 1508（候选 1508/4096/8192/9000）、延迟 0/1/2/3 默认 0、曝光 8.0、增益 0、断开置灰 |
| T4d 未连接改下拉 | ✅ 无提示，界面值保留（latency=3 / packet=9000） |
| T4a/b/c 曝光 | ✅ `abc`/`-5` 提示并回退；`5` 无提示且记值 |
| T2 无相机点连接 | ✅ 提示"未发现 GigE 相机…"，仍未连接，借帧钩子未挂 |
| T3 未连接点拍照/预览/停止 | ✅ 分别提示"相机未连接"/"当前没有预览" |
| 窗体数 | ✅ 恒为 1，无预览小窗误弹 |

### 13.5 现场待验证（本机无相机，必须到现场打勾）

| # | 场景 | 期望 |
|---|---|---|
| L1 | 连接相机 | 连上不报错；**小窗标题显示 `· Bayer Color`**；界面四项显示相机现值（曝光 5 / 包 1508 / 延迟 3）而**相机值未被改动** |
| L2 | 若仍取不到图 | 弹框会直接列出 视频格式/曝光/包大小(含上限)/延迟/触发模式 —— 把该框内容发我即可定位 |
| L3 | 单次拍照 | 出图（3072×2048 量级），PMA『采集训练图』能借到同一帧 |
| L4 | 实时预览 → 停止 | 有画面且能动 → 停得住；连开连关 10 次不卡死 |
| L5 | 改曝光/包大小/延迟 | 热应用生效；故意填超上限的包大小 → 夹断提示且界面显示真值 |
| L6 | 拔网线再点拍照 | 弹诊断框或错误提示，不卡死、不崩 |

> 交叉影响提醒：「视觉参数配置」v2.3 的 Q4（自动硬触发 = `Auto`）实施时，`StartLiveDisplay` 的 `own` **必须传 true**（官方文档硬性要求）；本文件现在用的是"连接时自动试出来的触发配置"，那期落地时要一并改，别忘。

---

## 十四、v1.5 现场实测回传 + 追加修正

### 14.1 现场诊断框原文（v1.4 跑出来的）

```
连接相机失败:
已连上相机，但一帧图都取不到。
原因: 取像返回空图（相机没有把帧发过来）。
视频格式: Generic GigEVision (Bayer Color)
曝光: 5 ms
包大小: 1508 字节（相机上限 9156）
延迟级别: 3
触发模式: FreeRun，触发使能: False
```

**这一框排掉了两个假设、留下一条硬线索：**

| 读出的事实 | 结论 |
|---|---|
| `Generic GigEVision (Bayer Color)` | ✅ §13.2① 的修法有效，格式取对了 |
| 曝光 5 / 包大小 1508 / 延迟 3 —— **全是相机原值** | ✅ **"读回优先"生效，本页没改坏相机**；同时**排除"包大小 9000"这条假设**（FIFO 继承到的本来就是 1508，相机上限 9156） |
| `Run()` 既不抛超时、也不给图 | ❌ 卡在**触发/取流**环节；不是超时、不是参数、不是格式 |

### 14.2 v1.5 追加修正

1. **不再硬写 `TriggerModel = FreeRun`。** 现场在 VisionPro 里是**"手动"触发**才出图（§13.1），而 v1.4 强行改成"自由运行 + 关触发"，很可能就是"一帧不来"的直接原因 —— 相机若不吃自由运行，FIFO 以为在自由跑、相机却在等触发，两边僵住。
2. 连接时改为**三段式试触发**（同一个 FIFO，逐轮换触发配置再试取）：
   ```
   ① 保持相机原触发配置（一个字都不写）
   ② TriggerModel = FreeRun + TriggerEnabled = false
   ③ TriggerModel = Manual + TriggerEnabled = true（软件触发，Run() 自己发触发）
   ```
   哪种取到图就用哪种，并弹框告知"已自动切到 X"；三轮全失败则**还原相机原始触发状态**，不留半路状态。
3. `Run()` **不抛异常也不给图**时，改读 `CogAcqFifoTool.RunStatus`（`ICogRunStatus`：`Result` / `Message` / `Exception`）—— 那才是 VisionPro 真正想说的话，一并写进诊断框。
4. 诊断框追加：`op.VideoFormat`（读回）、`op.AcquiredPixelFormat()`（实际像素格式）、**三轮尝试的逐条结果**。
5. 每轮试取超时 **2000 ms**（对齐现场"传输超时 2000 毫秒"），最坏三轮约 6 秒，不会干等。

新增私有成员：`TryRunOnce` / `ApplyTriggerCandidate` / `SafeBool` / `SafeTriggerModel`；`BuildNoFrameMessage` 改为接收"三轮日志"。

### 14.3 复测

| 项 | 结果 |
|---|---|
| 编译（VS MSBuild amd64，x64/net48） | ✅ 0 error / 0 warning |
| 冒烟（构造视图 + 控件默认值） | ✅ 包大小 1508、延迟 0、曝光 8.0、窗体数 1 |
| 探针清理 | ✅ `bin` 与 `_tmp` 无残留 |

### 14.4 下次现场（有相机时）

1. 重新 Build 后点『连接相机』；
2. **若还弹诊断框，把整框内容发回** —— 里面现在有 `Result/Message/Exception` 和三路触发结果，基本能一次定位；
3. 若弹的是"已自动切到「手动软件触发」"这类提示，说明相机不吃自由运行，**记下来**（这条要写进文档，也影响「视觉参数配置」那期的触发设计）；
4. 然后按 §13.5 的 L3/L4 验拍照与预览。

> 仍未验证（本机无相机）：三段式试触发的实际命中情况、`RunStatus` 的真实文案、Bayer Color × `Format8Grey` 能否出灰度图（若最终出的是彩色图，PMA 侧 `EnsureGreyImage()` 会自动转灰度，训练路径不受影响）。
