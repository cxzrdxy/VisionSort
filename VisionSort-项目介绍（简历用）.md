# VisionSort · 项目介绍（简历素材）

> 依据：`04-WinForms\` 全部源码（59 个 .cs / 15026 行）+ 10 份设计方案文档（4031 行）。
> 文中所有数字均来自源码注释或方案文档的实测记录，来源可靠；**标 🔶 的地方需要你本人确认或补充**。

---

## 一、一页速览（写简历头部用）

| 项 | 内容 |
|---|---|
| **项目名称** | 视觉引导机械臂自动分拣系统（VisionSort） |
| **一句话定位** | 基于 Cognex VisionPro 的工业视觉分拣上位机：相机取像 → PatMax 模板定位 → OK/NG 判定 → 九点标定像素/毫米换算 → 机械臂抓取分拣 → 数据入库与报表导出，全流程无人干预 |
| **角色** | **独立开发（独自承担全部角色）**：需求梳理 → 技术方案设计 → 架构选型 → 全部编码 → 自测与联调，从零到可运行系统全程独立完成 |
| **周期** | 🔶【待填：起止年月。参考：工作区里这批方案与源码集中在 2026-09-14 ~ 2026-09-20，但物料从 8 月即已开始积累，实际周期应更长】 |
| **代码规模** | 3 个 C# 项目 + 1 个共享协议源，59 个 .cs 文件、约 **1.5 万行**代码；配套 10 份设计方案（4031 行） |
| **技术栈** | C# / .NET Framework 4.8 / WinForms / Cognex VisionPro 9.0 CR2（PatMax、N 点标定、QuickBuild VPP）/ GigE Vision 相机 / Dobot 四轴机械臂 + P/Invoke / 回环 UDP IPC / Modbus RTU（NModbus）/ MySQL 8.0（MySqlConnector）/ 多线程状态机 |

### 简历精简版（上位机软件岗 · 按此顺序粘贴）

> 按「上位机软件开发」岗位重排：先讲架构与设备通信、再讲并发与可靠性、最后用视觉专业性收尾。第 6 条可删（若版面只留 4–5 条）。

- **独立开发**基于 **Cognex VisionPro 9.0** 的视觉引导分拣上位机（**C# / .NET Framework 4.8 / WinForms，约 1.5 万行**），独立完成需求梳理、技术选型、架构设计与全部编码，打通「相机采集 → PatMax 模板定位 → OK/NG 判定 → 九点标定坐标换算 → 机械臂分拣 → MySQL 入库 / Excel 导出」全链路，涵盖 5 个功能页（主运行 / 设备调试 / 视觉配置 / 数据日志 / 系统设置）。
- **攻克 x64 视觉库与 32 位机器人原生 DLL 的位数冲突**：判断出"拆类库无法隔离、必须拆进程"，设计并落地独立 **x86 桥接进程** + 回环 **UDP 自定义 KV 协议**（一问一答信号量串行化、seq 序号校验、仅幂等命令超时重发、迟到包丢弃、断线事件通知）；协议源码双端 Link 共享杜绝两侧漂移，MSBuild 加 x86 硬门槛避免被 x64 平台静默带错。
- **主循环跑后台线程的多状态机设计**（11 个状态、单次/连续/急停/报警复位），逐格定义异常策略使生产节拍不被任何一步挂死：写库与存图失败只记备注、机械臂异常仍写库不丢事件、取像连续失败才判故障、检测加 2000ms 看门狗、急停走取消令牌 + 停带 + 状态锁存且不自动恢复。
- **高可靠数据链路与零依赖报表**：后台队列批量写库（1234 行 **283–332ms**）+ JSONL 断网补写 + uuid 唯一键幂等（`INSERT IGNORE`，断网重试不产生重复行）；手写 OOXML 实现**零第三方依赖** xlsx 流式导出（万级记录后台导出、不锁界面、内存不涨）；连接串 3 秒超时，绝不把生产节拍挂在数据库上。
- **并发与设备资源独占治理**：GigE 相机单 FIFO 抽为共享服务消除两页抢相机；传送带与机械臂在生产运行时对调试页整体**互锁**；NModbus 主站非线程安全，用信号量串行化访问并实测修复并发串包（"刚点正转但回读仍是停止"）。
- **视觉专业性支撑**：定位并解决两处**静默失效**——PatMax 模板原点默认落在图像左上角 (0,0)，判定正常但驱动机器人会整体偏移且全程不报错；N 点标定 API 返回的是**反向变换**（需 Invert），并用行列式判手性、镜像时拒绝保存。程序化防呆后标定数学精度 **RMSE 1.0×10⁻⁵ mm**、比例 **0.192308 mm/px**，换算值与桥接进程实收坐标偏差 <0.01mm（现场物理验收口径：反查偏差 <0.5mm）。

---

## 二、详细版（简历项目经历栏目）

### 项目背景与目标

小零件（项目样例为齿轮类零件）人工分拣效率低、漏检不可追溯。目标是做一套**视觉引导的自动分拣上位机**：传送带把零件送进相机视野，光电传感器硬触发相机取像，视觉定位并判定合格与否，不合格件由机械臂实时抓走，全过程数据落库可追溯。现场为工控机 + GigE 相机 + Dobot 四轴机械臂 + Modbus 传送带的单机部署形态。

### 系统架构

```
VisionSort.exe (x64, .NET Framework 4.8, WinForms)
├── 5 个功能页：主运行 │ 设备调试 │ 视觉配置 │ 数据日志 │ 系统设置
└── 服务层（静态单例，界面只订阅事件、不直接碰设备）
    ├── ProductionRunner     生产主循环状态机（跑在后台线程）
    ├── VisionScheme         VPP 加载 / PatMax 定位与判定
    ├── CalibrationService   九点标定：像素 ↔ 毫米
    ├── RobotPickPlace       NG 分拣动作编排（开机位/取料/放料/待机）
    ├── CameraService        共享 GigE 取像 FIFO（两页共用一个相机）
    ├── ConveyorSettings     传送带 Modbus RTU 主站（信号量串行化）
    └── ResultRepository     写库队列 + JSONL 断网补写 + xlsx 导出
          │
   ┌──────┼────────────────┬────────────────────┬──────────────────┐
   ▼      ▼                ▼                    ▼                  ▼
VisionPro 9.0        DobotArm.dll         NModbus 3.0.83    MySqlConnector 2.4.0
FGGigE + PatMax      (AnyCPU 客户端)      RTU 9600-8-N-1    （无 ORM，ADO.NET）
   │                     │ UDP 127.0.0.1:18899                 │
   ▼                     ▼                                     ▼
海康 MV-CS060-10GC   DobotArmBridge.exe (x86)             MySQL 8.0
GigE 相机            + DobotDll.dll + Qt5×3 + MSVC×2        vision_sort.vision_result
                         │ USB 串口 115200-8-N-1
                         ▼
                     Dobot Magician 四轴机械臂
```

### 我的工作（功能范围）

| 模块 | 实现内容 |
|---|---|
| **主运行页** | 生产主循环状态机（11 个状态：准备/等触发/停带/检测/分拣/记录/恢复/停止/故障/急停…），运行在后台线程、界面仅订阅事件；单次与连续两种模式；急停（取消令牌 + 停带 + 锁存）、报警复位；光电触发→停止延时→停带→停稳等待→检测→存图→NG 分拣→恢复带的完整节拍闭环；检测 OK/NG 灯、状态链、实时画面预览与统计计数 |
| **设备调试页** | 传送带 Modbus RTU 连接/正反转/停止/调速/故障复位 + 500ms 状态轮询；机械臂手动点动（按住连续 JOG / 单击走一步）、软件端步骤队列、吸盘控制、位姿与报警读取；**九点标定工作区**（九点表格、一键读机械臂位姿回填、拟合计算、RMSE/精度/旋转/手性四项指标、抓取几何与工作范围、操作指引弹窗）；生产运行时整页互锁置灰 |
| **视觉配置页** | GigE 相机枚举/视频格式/曝光/增益/包大小/延迟级别/触发模式三段式试触发与实时预览；VPP 方案加载与另存（兼容 CogJobManager / CogJob / CogToolBlock / CogToolGroup 四种根形态）；**PatMax 模板训练**（训练图采集、矩形/圆环训练区域、角度与缩放搜索范围、匹配阈值、粒度四档、训练后自检与精细特征叠加显示、模板原点核对）；图像保存配置（目录/格式/存 OK/存 NG） |
| **数据日志页** | 按时间范围与结果筛选查询、200 行/页倒序分页、四张统计卡（与列表同一 WHERE 保证数据一致）、当前查询结果全量后台导出 Excel |
| **系统设置页** | 4 组参数的输入校验与即时生效（Modbus 传送带、生产视觉参数、机械臂与抓取几何、MySQL 连接），统一落盘 `config\sysparam.json` |

---

## 三、技术难点与亮点（面试展开素材）

### 1. 进程位数隔离：x64 视觉库 × 32 位机器人 DLL

**问题**：VisionPro 9.0 是 x64 混合模式程序集，而 Dobot 官方 `DobotDll.dll` 只有 32 位版本（PE 头 `Machine=0x014C`，还连带 Qt5×3 + MSVC×2 共约 6.9MB 原生依赖，且官方 DLL 只提供 USB 串口接口、无网络接口）。x64 进程加载 x86 原生库必然 `BadImageFormatException`。

**关键判断**：拆成独立类库**不能**隔离——类库仍被加载进 `VisionSort.exe` 进程，P/Invoke 的原生 DLL 也进同一进程，拆项目只解决了编译期问题，运行时冲突原样存在。**必须用独立进程隔离。**

**方案**：新增 x86 控制台进程 `DobotArmBridge.exe` 作为唯一宿主原生 DLL 的地方（崩溃也带不走 VisionPro），VisionSort 侧通过 AnyCPU 客户端类库经**回环 UDP** 指挥它；桥接进程还能脱离 VisionPro 单独启动、用 PowerShell 手打一行报文验证。

**自研线协议**：`cmd=moveJ;seq=7;x=200.0;y=30.0;z=-50.0;r=0`（单行 key=value，UTF-8，值内 `;`/CR/LF 转义），12 条命令（ping/search/connect/pose/alarms/home/moveJ/jog/suction/shutdown…）。选 KV 而非 JSON 的理由：零依赖、零转义坑、人能读能手打。

**可靠性设计**：① 一问一答串行化（`SemaphoreSlim(1,1)`，同一时刻只允许一个请求在飞）；② **只对幂等命令**（读取/探活/断开）在无应答时重发，动作命令绝不盲目重发（安全优先）；③ 递增 `seq` 序号校验，丢弃 UDP 迟到包；④ 已连接状态下彻底失败即标记断线并触发 `ConnectionLost`；⑤ 连接失败后主动发 `disconnect` 对账，避免"界面显示未连接、串口却被占着"的状态分歧。

**工程防呆**：线协议源码由客户端与桥接进程两侧 `<Compile Include="..\Shared\BridgeProtocol.cs" Link=...>` 共享编译，永远只有一份定义；MSBuild 目标 `EnsureX86` 在 `CoreCompile` 前硬校验 `PlatformTarget=x86`，防止被 ProjectReference 传入的 x64 平台静默带错；桥接 exe 与原生 DLL 用 Target（而非 ItemGroup）在构建后拷贝，规避"项目评估 glob 时桥接还没构建过 → 静默漏拷"的部署坑。

**运行参数**：桥接进程 12 条命令、11 个错误码（5 个官方映射 + DLL 缺失/位数不符/未连接/命令非法/入口缺失），Mutex 单实例保护（同时跑两个会抢同一个串口，第二实例以退出码 3 退出）；命令超时 2000ms、连接超时 10000ms、无应答重试 ≤3 次（仅幂等命令）、到位容差 0.5mm、位姿轮询 80ms/超时 5000ms、抓取与复位延时 500/800ms；运动只用点对点 MOVJ（带队列执行），连接后先清队列并启动队列执行。

### 2. 九点标定：三个"不报错的错误"

标定做错时代码不会报错，只会安静地抓偏，因此把人工经验全部转成了程序化校验：

- **API 方向是反的**：`CogCalibNPointToNPointTool` 的两个 `GetComputed...Transform()` 返回的都是「未校正 ← 已校正」（像素 ← 毫米），要得到「像素 → 毫米」**必须 Invert**。正反用错，显示的精度会差 27 倍（实测正变换 5.200000 px/mm，反变换 0.192308 mm/px）。
- **手性检查**：反变换矩阵行列式 >0 表示两个坐标系同手性（实测 0.036982），为负说明 X/Y 有一个轴配反了，此时机械臂会往反方向走。人眼从九个数字里看不出镜像，**程序直接拒绝保存**。
- **模板原点陷阱**：PatMax 输出的"位置"是**模板原点**在图像中的位置，而原点默认值是 (0,0)，即图像左上角，不是工件。判定 OK/NG 完全不受影响，但拿它驱动机械臂会整体偏移且全程无报错。程序在训练后与标定计算前强制核对原点，不通过不允许保存；并进一步实测出"原点设到区域中心后，PMA 报出的位置仍与中心差约 20 像素（按 0.19mm/px 约 4mm）"，据此规定标定点必须以 PMA 实测位置为准。

另加多道保命校验：点对少于 3 对拒算（推荐 9 对）、九点共线等退化情形（反变换行列式绝对值低于 1e-12）拒算、非有限数拒收；算出的取料点必须落在工作范围内（默认 X 120–280mm、Y −80–80mm，宁可拒动让人来调，也不先撞一次），越界时给出"先查模板原点 / 再查手性 / 再查范围"的中文排查指引。标定结果**存点对而不存矩阵**（矩阵是派生数据，存点对则每次启动重算、参数一改自动一致，且人能打开 JSON 逐个数核对）。另外把「操作指引」以嵌入资源编译进 exe，保证随程序分发的说明与文档永远同一份，不会各说各话。

### 3. PatMax 阈值会"吃掉"结果集（全流程实测挖出的坑）

`CogPMAlignTool.RunParams.AcceptThreshold` 不只是标记 `Accepted=false`，它会**把结果从 `Results` 集合里直接滤掉**：同一张分数 0.9401 的图，阈值设 0.90 时 `Results.Count` 直接变成 **0**。后果是判 NG 时拿不到像素坐标 → 机械臂不知道该去哪儿抓 → **分拣永远不动作**，且没有任何报错。

解决：生产固定用 `AcceptThreshold = 0` 跑，判定与"值不值得抓"全部由程序自己算；并用实测数据标定「视野里到底有没有工件」的下限——有齿轮整幅 0.9401、只有背景裁剪 0.0000、全黑/全白/全灰 0.0000，分得非常开，取 `PickScoreFloor = 0.30`，作用是"别对着空带子空抓一次"。

### 4. 生产节拍不能被任何一步挂死

后台线程跑主循环（Auto 硬触发下取一帧要阻塞等光电，放 UI 线程上"急停"就按不动了），并逐格定义异常策略：

- 写库失败、存图失败 → **只记备注，不打断节拍**；
- 机械臂异常 → **仍然写库**（不丢事件），中文原因写进记录备注；
- 取像失败 → 连续 3 次才判故障（连续模式等不到工件属正常，每 20 次提示一次）；
- 检测加 2000ms 看门狗，超时则本轮仍落记录（`score=NULL`、备注写明）后停线等人；
- 急停 = 取消令牌 + 停带 + 状态锁存，**不自动恢复**；
- 恢复传送带只写启停寄存器，**不碰速度寄存器**（少一次写就少一个"把速度改回去"的风险）。

### 5. 数据可靠性：断网补写 + 幂等重试

- `MySqlConnection` 非线程安全 → **每次操作各自 new**，靠驱动连接池兜底，界面查询与后台补写两条线永不共用连接；队列用 `ConcurrentQueue`，后台泵用 `Interlocked` 防重入。
- 后台泵每 1 秒或积满批量刷多值 `INSERT`（实测 1234 行 **283–332ms**，2.6 秒内全部落库）。
- 写库失败落到 JSONL 暂存文件（`config\pending_results.jsonl`，一行一条：崩溃最多丢最后半行），DB 恢复后自动补齐，每 5 秒重试。
- 每行带客户端生成的 `uuid`，表上 `UNIQUE KEY uk_uuid` + `INSERT IGNORE`，**重试不产生重复行**（不用事务也能保证幂等）。
- 连接串 `ConnectionTimeout=3`：库不可达 3 秒内返回，绝不把生产节拍挂在数据库上。
- 密码在日志与提示中一律走 `SafeConnectionString` 打码。

### 6. 零依赖 Excel 导出

手写 OOXML（xlsx 本质是 zip，最少只需 5 个部件），字符串用 `inlineStr` 内联写，省掉 `sharedStrings.xml`；游标读一行 → 写一行 → 丢一行，**万级记录导出内存不涨、界面全程可点**（实测导出 1245 行×7 列、44775 字节）。整个项目坚持"零 NuGet 包源、Libs 本地引用"，保证现场能离线 restore。

### 7. 设备资源独占与并发治理

- **GigE 相机同一时刻只能被一个 FIFO 打开**，而 FIFO 原先挂在相机页私有字段上，主运行页拿不到，两页各开一个会互相抢相机（现场表现为"连不上/取不到图"，极难排查）→ 抽出 `CameraService` 共享单例，两个页面共用一个 FIFO；取像实现统一为"临时设超时→取完恢复"+"`Run()` 不抛异常也要读 `RunStatus` 才知道 VisionPro 想说什么"+"失败不抛异常、用中文原因回传"。
- **传送带与机械臂各只有一份物理资源**（一条串口 / 一个桥接进程）→ 生产运行时把设备调试页所有"能改状态"的控件整体置灰（保留只读的状态与操作指引），消除两处并发下发命令导致的"机械臂偶尔乱走/带子停不下来"这类难复现故障。
- **NModbus 主站非线程安全**，界面 500ms 轮询 + 按钮动作 + 生产循环会并发进入，实测并发读写会串包（问的响应被另一次调用吃掉，表现为"刚点正转但回读还是停止"）→ 用 `SemaphoreSlim` 串行化所有主站访问。
- **NModbus 3.x 已删除 2.x 的串口直连** → 自写 `IStreamResource` 适配器包装 `SerialPort.BaseStream`。
- **调速滑杆做 250ms 防抖**，避免拖动过程中刷几十条串口指令。

### 8. 可测试性：没有相机也能跑通全流程

抽象 `IImageSource`（生产用相机来源 / 自测用本地图片回放），并给回放源加了「每 N 次故意不给图」的开关——开发机上就能把包括"取不到图"在内的整条主流程与异常路径跑通，不必等到了现场才暴露问题。三层自测结果：数据层 24/24、界面 21/21、端到端 7/7，主运行页 39 项断言全过，编译 0 error / 0 warning。

---

## 四、可量化成果

**已实测、可直接写（数据来自本项目自测记录）**

| 类别 | 数据 |
|---|---|
| 代码规模 | 59 个 .cs 文件 / **15026 行**；4 个工程（3 项目 + 1 共享协议源）；设计方案 10 份 4031 行 |
| 视觉定位 | 同一方案样例图匹配分数 **0.9334 / 0.9401 / 0.9401** 全部 Accepted；无工件裁剪图 0.0000；PatMax 训练图自检 0.895 / 0.98938 |
| 标定精度 | 合成九点（旋转 3° + 缩放 5.2 + 平移 (320,240)）标定 **RMSE 1.02×10⁻⁵ mm**、九点回代最大误差 **7.8×10⁻¹⁴ mm**、比例 **0.192308 mm/px**、行列式 0.036982（手性 ✓）；实机数据像素 (620.2, 508.9) → 毫米 (129.27, −2.14)，桥接进程实收 (129.2738, −2.1390, −50.0000)，偏差 <0.01mm。**现场物理验收口径：反查偏差 <0.5mm** |
| 设备通信 | Modbus RTU 功能码 06、8 字节帧；真串口 COM1↔COM2 回环联调通过（正转 `02 06 00 64 00 01 09 E6`、停止 `02 06 00 64 00 00 C8 26`、调速 `02 06 00 66 00 FA E9 A5`）；NModbus 3.0.83 本地引用 |
| 数据链路 | 批量插入 1234 行 **283–332ms**；后台泵 2.6 秒落库；DB 不可达 **3.0 秒**返回；导出 1245 行×7 列 / 44775 字节 |
| 图像存储 | 同步存图几十毫秒/张；JPG 344,767 B / PNG 261,467 B |
| 质量 | Debug 与 Release 均 **0 error / 0 warning**；分模块探针断言 51/51、48/48、24/24、23/23、21/21、19/19、14/14、7/7，整机 29/29、39 项全过 |
| 稳定性 | 相机单轮试触发超时 2000ms（最坏约 6 秒）；软件启动 8 秒、内存 125.5MB、退出 15 秒内；桥接进程启动 5 秒存活 103MB |

**🔶 需要你补充（我无法从代码得出，不要凭空编）**

- **真机联调尚未完成（最重要的一条）**：项目记录明确写着"本机可做的验证已做完，只剩现场真机 T-live"。具体未验证项包括：真实机械臂动作、接真变频器（目前用 Modbus 从站模拟 + 串口回环联调）、相机触发方式写入相机本身、串口占用冲突、调速量程。**简历里请写"完成开发与自测 / 完成上位机与设备通信联调"，不要写"已在产线稳定运行"或任何良率数字。**
- 实际产线节拍（件/分钟）、分拣准确率或漏检率、替代人工数量、连续运行时长。
- 是否已在客户现场验收/上线。
- 起止年月（角色已是"独立开发"，此项已确定）。

---

## 五、简历填写建议

1. **量化数字只写第四节"已实测"那一档**。产线良率、效率提升这类数字必须有现场数据支撑，面试官追问细节时含糊会很扣分。
2. **优先讲这 3 个（上位机软件岗）**：① x86 桥接进程 + 自研 UDP 协议（架构判断力 + 设备通信）② 后台线程多状态机与逐格异常策略（并发与健壮性）③ 断网补写 + 幂等 + 零依赖导出（数据可靠性与工程完整度）。视觉标定/模板原点那两条留作面试展开——它证明你"能读懂设备 API 的隐含语义"，是上位机岗的差异化加分项，而不是主线。
3. **主动讲"我把设备 API 的坑变成了程序化校验"**。这个项目最有说服力的一点是：很多错误（模板原点、镜像标定、PatMax 阈值吃掉结果集）程序不报错、人眼看不出，你把它们逐条变成了代码里的拦截与中文提示。这体现的是"对上位机软件来说，设备行为的不可预期性必须被代码兜住"的工程意识。
4. **技术栈写法**：`C#/.NET Framework 4.8`、`WinForms`、`多线程与状态机`、`P/Invoke 与原生 DLL 互操作`、`UDP/TCP 自定义协议`、`Modbus RTU`、`MySQL（ADO.NET）`、`Cognex VisionPro 9.0`、`GigE Vision 相机`、`OOXML 报表导出`。
5. **独立开发要敢写"独立承担全部角色"**，并在面试里准备三个能讲 5 分钟的点：架构选型为什么这么做（进程隔离）、并发问题怎么定位的（Modbus 串包）、异常路径怎么设计的（节拍不挂死 + 断网补写）。独立完成一个 1.5 万行、跨 4 个工程、含原生互操作与数据库的系统，本身就是很强的信号。
6. **关于 AI 辅助**：简历上按常规写"独立开发/独立负责"即可——代码、架构决策与自测都是你交付的，AI 是工具，与用 IDE、调试器、搜索引擎没有区别，不必也不建议在简历上标注。面试被问到"怎么提效"时，可以大方说用 AI 辅助做方案推演、API 语义核对与文档整理，但**必须能讲清每一处关键技术决策的理由**（本文第三节就是为此准备的）。

> 面试准备提示：第三节 8 个难点里，**第 1、4、5、6、7 条是上位机软件岗的主线**（进程隔离与协议、节拍不挂死、数据可靠性、零依赖导出、并发与资源互斥）；**第 2、3 条是视觉侧的差异化加分**。第 8 条（可测试性 / 无相机也能跑通全流程）在上位机岗很讨喜，建议作为"工程完整度"的收尾。

---

*来源文件：`VisionSort.sln`、`VisionSort/`、`DobotArm/`、`DobotArmBridge/`、`Shared/BridgeProtocol.cs`，以及同目录 10 份设计方案文档。*
